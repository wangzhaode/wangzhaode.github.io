<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://zhaode.wang/feed.xml" rel="self" type="application/atom+xml"/><link href="https://zhaode.wang/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-13T01:34:21+00:00</updated><id>https://zhaode.wang/feed.xml</id><title type="html">Zhaode’s blog</title><subtitle>Expert insights on On-Device AI, LLM Optimization, and High-Performance Computing. </subtitle><entry><title type="html">MNN 支持 Gemma4 全系列：PLE、KV共享与多模态</title><link href="https://zhaode.wang/blog/2026/gemma4/" rel="alternate" type="text/html" title="MNN 支持 Gemma4 全系列：PLE、KV共享与多模态"/><published>2026-04-08T00:00:00+00:00</published><updated>2026-04-08T00:00:00+00:00</updated><id>https://zhaode.wang/blog/2026/gemma4</id><content type="html" xml:base="https://zhaode.wang/blog/2026/gemma4/"><![CDATA[<h2 id="一背景">一、背景</h2> <p>Google 最新发布的 Gemma4 是一个架构创新密度很高的系列模型。从端侧 E2B 到桌面级 31B，四种规格覆盖了从手机到服务器的全部场景。更有意思的是，这四种规格不只是简单的参数缩放——它们使用了<strong>不同的架构机制组合</strong>，给推理引擎的适配带来了不少挑战。</p> <p>本文记录 MNN 适配 Gemma4 全系列的过程，重点拆解三个最有特色的机制：<strong>PLE（Per-Layer Embeddings）</strong>、<strong>KV Cache 共享</strong>和<strong>多模态支持</strong>，并提供实际的导出测评数据和使用建议。</p> <h2 id="二从-gemma3-到-gemma4架构演进">二、从 Gemma3 到 Gemma4：架构演进</h2> <p>Gemma4 并非直接从 Gemma3 跳跃而来——中间还有一个重要的过渡版本 <strong>Gemma3n</strong>。Gemma3n 是 Google 专为端侧设备设计的多模态模型，首次引入了 PLE、KV 共享等关键机制（详见<a href="https://zhuanlan.zhihu.com/p/1929221256969945147">我之前的 Gemma3n 架构分析</a>）。Gemma4 在继承这些机制的基础上，进一步扩展和演进。</p> <p>三代模型的架构演进：</p> <table> <thead> <tr> <th>特性</th> <th style="text-align: center">Gemma3</th> <th style="text-align: center">Gemma3n</th> <th style="text-align: center">Gemma4</th> </tr> </thead> <tbody> <tr> <td><strong>注意力机制</strong></td> <td style="text-align: center">混合注意力</td> <td style="text-align: center">混合注意力</td> <td style="text-align: center">混合注意力</td> </tr> <tr> <td><strong>KV Cache</strong></td> <td style="text-align: center">每层独立</td> <td style="text-align: center"><strong>KV 共享（10层）</strong></td> <td style="text-align: center"><strong>KV 共享（18-20层）</strong></td> </tr> <tr> <td><strong>Embedding</strong></td> <td style="text-align: center">共享 embedding</td> <td style="text-align: center"><strong>PLE（查表直注入）</strong></td> <td style="text-align: center"><strong>PLE（查表+投影融合）</strong></td> </tr> <tr> <td><strong>QKV Norm</strong></td> <td style="text-align: center">Q/K Norm</td> <td style="text-align: center">Q/K/V Norm</td> <td style="text-align: center">Q/K/V Norm + <strong>attn_scale=1.0</strong></td> </tr> <tr> <td><strong>V 投影</strong></td> <td style="text-align: center">独立 v_proj</td> <td style="text-align: center">独立 v_proj</td> <td style="text-align: center">部分模型 <strong>K=V（无 v_proj）</strong></td> </tr> <tr> <td><strong>KV Head</strong></td> <td style="text-align: center">统一</td> <td style="text-align: center">统一</td> <td style="text-align: center"><strong>异构 KV Head</strong></td> </tr> <tr> <td><strong>视觉编码器</strong></td> <td style="text-align: center">SigLIP ViT</td> <td style="text-align: center"><strong>MobileNetV5</strong></td> <td style="text-align: center"><strong>SigLIP ViT + Pooler</strong></td> </tr> <tr> <td><strong>音频支持</strong></td> <td style="text-align: center">无</td> <td style="text-align: center"><strong>Conformer</strong></td> <td style="text-align: center"><strong>USM Conformer</strong></td> </tr> <tr> <td><strong>MoE</strong></td> <td style="text-align: center">无</td> <td style="text-align: center">无</td> <td style="text-align: center">26B-A4B：<strong>128 专家 Top-8</strong></td> </tr> <tr> <td><strong>独有机制</strong></td> <td style="text-align: center">—</td> <td style="text-align: center">AltUp、Laurel Block、激活稀疏性</td> <td style="text-align: center">K=V、异构KV Head、双RoPE</td> </tr> </tbody> </table> <p>可以看到，Gemma4 的演进路线：</p> <ul> <li><strong>继承自 Gemma3n</strong>：PLE、KV 共享、混合注意力、QKV Norm、多模态原生支持</li> <li><strong>Gemma4 新增</strong>：PLE 投影路径、更大规模的 KV 共享、K=V 机制、异构 KV Head、Decoder 级 MoE、双 RoPE（不同 head_dim + theta）</li> <li><strong>Gemma3n 独有（Gemma4 未继承）</strong>：AltUp（多路径并行学习）、Laurel Block（低秩增强残差）、95% 激活稀疏性</li> </ul> <h3 id="21-整体架构">2.1 整体架构</h3> <p><img src="/assets/img/gemma4/gemma4_architecture.svg" alt="Gemma4 Architecture"/></p> <h3 id="22-四种规格详览">2.2 四种规格详览</h3> <table> <thead> <tr> <th>模型</th> <th>Hidden</th> <th>Heads/KV Heads</th> <th>Layers</th> <th>Head Dim</th> <th>特殊机制</th> </tr> </thead> <tbody> <tr> <td><strong>E2B</strong></td> <td>1536</td> <td>8/1</td> <td>35</td> <td>S:256/F:512</td> <td>KV共享(20层)、PLE</td> </tr> <tr> <td><strong>E4B</strong></td> <td>2560</td> <td>8/2</td> <td>42</td> <td>S:256/F:512</td> <td>KV共享(18层)、PLE</td> </tr> <tr> <td><strong>31B</strong></td> <td>5376</td> <td>32/16(F:4)</td> <td>60</td> <td>S:256/F:512</td> <td>K=V、异构KV Head、PLE</td> </tr> <tr> <td><strong>26B-A4B</strong></td> <td>2816</td> <td>16/8(F:2)</td> <td>30</td> <td>S:256/F:512</td> <td>K=V、异构KV Head、MoE(128专家)、PLE</td> </tr> </tbody> </table> <p>所有模型共有的特征：</p> <ul> <li><strong>混合注意力</strong>：Sliding Attention + Full Attention 交替排列</li> <li><strong>双 RoPE</strong>：Sliding 层使用 head_dim=256 + theta=10000，Full 层使用 head_dim=512 + theta=1000000</li> <li><strong>QKV 三 Norm</strong>：<code class="language-plaintext highlighter-rouge">q_norm</code>、<code class="language-plaintext highlighter-rouge">k_norm</code>、<code class="language-plaintext highlighter-rouge">v_norm</code>，注意力缩放固定为 1.0</li> <li><strong>PLE</strong>：每层接收专属的 token embedding 调制信号</li> <li><strong>原生多模态</strong>：内置视觉和音频编码器</li> </ul> <p>值得注意的是，E2B/E4B 和 31B/26B-A4B 的设计思路截然不同：前者通过 <strong>KV 共享</strong>减少存储、用更多的层去分摊任务；后者通过 <strong>K=V + 异构 KV Head</strong> 压缩参数量，配合更少但更大的层来建模。</p> <hr/> <h2 id="三ple每一层看到不同的-token">三、PLE：每一层看到不同的 token</h2> <h3 id="31-从-gemma3n-到-gemma4-的-ple-演进">3.1 从 Gemma3n 到 Gemma4 的 PLE 演进</h3> <p>PLE（Per-Layer Embeddings）最早在 Gemma3n 中引入，其核心思想与 STEM（Scaling Transformers with Embedding Modules）、RWKV-8 的 DeepEmbed 以及华为的 MoLE 方案一脉相承：<strong>通过增加 Embedding 参数量来提升模型效果，而 Embedding 参数可以存放在 Flash 上不占用运行内存——接近”免费的午餐”</strong>。</p> <p>Gemma3n 的 PLE 实现比较直接——只有一条路径：用 token ID 在 <code class="language-plaintext highlighter-rouge">embed_tokens_per_layer</code> 表中查找，reshape 后按层切分，通过门控乘法注入每层 Decoder。</p> <p><strong>Gemma4 在此基础上增加了第二条路径</strong>：将主 embedding 通过一个 Linear + Scale + Norm 投影到 PLE 空间，与 PLE 查表结果融合后再注入。这使得 PLE 不仅包含 token 级的静态信息，还融入了主 embedding 经过投影后的上下文相关信号。</p> <h3 id="32-gemma4-的-ple-计算流程">3.2 Gemma4 的 PLE 计算流程</h3> <p>Gemma4 的 PLE 计算涉及两条路径的融合（相比 Gemma3n 多了左侧的投影路径）：</p> <p><img src="/assets/img/gemma4/gemma4_ple_flow.svg" alt="PLE Computation Flow"/></p> <p>关键步骤解读：</p> <ol> <li><strong>查表</strong>：<code class="language-plaintext highlighter-rouge">embed_tokens_per_layer</code> 是一个大表，维度为 <code class="language-plaintext highlighter-rouge">[vocab_size, num_layers × ple_dim]</code>，用 token ID 查找后 reshape 为 <code class="language-plaintext highlighter-rouge">[seq, num_layers, ple_dim]</code>，每个 token 在每一层都有独立的 embedding</li> <li><strong>投影</strong>：用 <code class="language-plaintext highlighter-rouge">per_layer_model_projection</code>（Linear）将主 embedding 从 hidden_size 投影到 ple_dim 空间，缩放系数为 <code class="language-plaintext highlighter-rouge">hidden_size^{-0.5}</code>，再做 RMSNorm 归一化</li> <li><strong>融合</strong>：两条路径的输出相加，乘以 <code class="language-plaintext highlighter-rouge">2^{-0.5}</code> 作为最终的 per-layer input</li> </ol> <h3 id="33-门控注入ple-如何进入-decoder">3.3 门控注入：PLE 如何进入 Decoder</h3> <p>每层 Decoder 通过<strong>门控乘法</strong>将 per_layer_input 注入隐状态，而不是简单的加法。这保证了模型可以学习”在这一层需要多少 PLE 信号”：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 每层 Decoder 内部
</span><span class="n">residual</span> <span class="o">=</span> <span class="n">hidden_states</span>

<span class="c1"># 门控路径：hidden_states 决定"开门多少"
</span><span class="n">hidden_states</span> <span class="o">=</span> <span class="nf">per_layer_input_gate</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">)</span>   <span class="c1"># Linear
</span><span class="n">hidden_states</span> <span class="o">=</span> <span class="nf">act_fn</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">)</span>                  <span class="c1"># 激活函数
</span>
<span class="c1"># 逐元素乘法：门控信号 × PLE 调制
</span><span class="n">hidden_states</span> <span class="o">=</span> <span class="n">hidden_states</span> <span class="o">*</span> <span class="n">per_layer_input</span>        <span class="c1"># 门控乘法
</span>
<span class="c1"># 投影回 hidden_size + 归一化
</span><span class="n">hidden_states</span> <span class="o">=</span> <span class="nf">per_layer_projection</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">)</span>    <span class="c1"># Linear
</span><span class="n">hidden_states</span> <span class="o">=</span> <span class="nf">post_per_layer_input_norm</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">)</span>

<span class="c1"># 残差连接
</span><span class="n">hidden_states</span> <span class="o">=</span> <span class="n">residual</span> <span class="o">+</span> <span class="n">hidden_states</span>
</code></pre></div></div> <p>门控设计的好处是：如果某一层不需要 PLE 信号，门控可以学习为接近 0，不影响原有的信息流；如果需要强调某些 token 的层级特异性，门控就会打开。这比简单的加法更灵活，也更容易训练。</p> <h3 id="34-导出策略ple-embedding-的量化">3.4 导出策略：PLE Embedding 的量化</h3> <p>PLE Embedding 表体积不小——<code class="language-plaintext highlighter-rouge">vocab_size(262144) × num_layers × ple_dim</code>。以 E4B（42 层）为例，bf16 下约 1.3GB，占整个模型不小的比例。</p> <p>我们支持用 <code class="language-plaintext highlighter-rouge">--embed_bit</code> 参数将 PLE Embedding 量化到 int4/int8，复用主 embedding 已有的量化/反量化路径：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># bf16（默认）</span>
python llmexport.py <span class="nt">--path</span> model_path <span class="nt">--export</span> mnn

<span class="c"># int4 量化，PLE embedding 体积缩小约 4x</span>
python llmexport.py <span class="nt">--path</span> model_path <span class="nt">--export</span> mnn <span class="nt">--embed_bit</span> 4

<span class="c"># int8 量化</span>
python llmexport.py <span class="nt">--path</span> model_path <span class="nt">--export</span> mnn <span class="nt">--embed_bit</span> 8
</code></pre></div></div> <p>量化格式与主 embedding 完全一致——使用分块量化（default block_size=64），存储 <code class="language-plaintext highlighter-rouge">[weight, alpha]</code> 二进制格式，配置通过 <code class="language-plaintext highlighter-rouge">ple_quant = [offset, weight_size, alpha_size, quant_bit, quant_block]</code> 传递给 C++ 推理端。</p> <p>C++ 端的 <code class="language-plaintext highlighter-rouge">DiskEmbedding</code> 类同时支持 bf16 和量化格式的 PLE 加载，推理时 <code class="language-plaintext highlighter-rouge">embedding()</code> 阶段同时完成主 embedding 和 PLE embedding 的查表：</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// PLE embedding lookup（C++）</span>
<span class="k">if</span> <span class="p">(</span><span class="n">mPleEmbedding</span> <span class="o">&amp;&amp;</span> <span class="p">(</span><span class="o">!</span><span class="n">mPleInput</span><span class="p">.</span><span class="n">get</span><span class="p">()</span> <span class="o">||</span> <span class="n">seq_len</span> <span class="o">==</span> <span class="mi">1</span><span class="p">))</span> <span class="p">{</span>
    <span class="n">mPleInput</span> <span class="o">=</span> <span class="n">_Input</span><span class="p">({</span><span class="mi">1</span><span class="p">,</span> <span class="n">seq_len</span><span class="p">,</span> <span class="n">ple_dim</span><span class="p">},</span> <span class="n">NCHW</span><span class="p">);</span>
    <span class="n">mPleEmbedding</span><span class="o">-&gt;</span><span class="n">embedding</span><span class="p">(</span><span class="n">input_ids</span><span class="p">,</span> <span class="n">mPleInput</span><span class="o">-&gt;</span><span class="n">writeMap</span><span class="o">&lt;</span><span class="kt">float</span><span class="o">&gt;</span><span class="p">());</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">ple_scale</span> <span class="o">!=</span> <span class="mf">1.0f</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">mPleInput</span> <span class="o">=</span> <span class="n">mPleInput</span> <span class="o">*</span> <span class="n">_Scalar</span><span class="o">&lt;</span><span class="kt">float</span><span class="o">&gt;</span><span class="p">(</span><span class="n">ple_scale</span><span class="p">);</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>分块 prefill 时，PLE 需要跟 input embedding 一起按 block 切分，确保每个 block 的 PLE 与 token 对齐。</p> <hr/> <h2 id="四kv-cache-共享用-2-层的-kv-撑-20-层的推理">四、KV Cache 共享：用 2 层的 KV 撑 20 层的推理</h2> <h3 id="41-机制解析">4.1 机制解析</h3> <p>KV 共享同样继承自 Gemma3n。在 Gemma3n-E2B（30 层）中，最后 10 层（层 20-29）共享层 18 和层 19 的 KV，苹果的端侧 3B 模型也使用了类似方案。</p> <p><strong>Gemma4 将 KV 共享的规模大幅扩展</strong>——E2B 从 10 层共享扩展到 20 层共享，共享比例从 33% 提升到 57%。</p> <p>以 Gemma4 E2B（35 层）为例：</p> <ul> <li><strong>层 0-14</strong>：每层独立计算和存储自己的 KV Cache（15 层独立 KV）</li> <li><strong>层 15-34</strong>：<strong>不再计算和存储自己的 KV</strong>，而是直接复用层 13 或层 14 的 KV Cache</li> </ul> <p>具体的共享拓扑：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>独立 KV 层（layers 0-14）：每层计算并存储自己的 K/V

Source 层（提供 KV 给他人复用）：
  Layer 13 (sliding_attention) → 被 16 个层复用
  Layer 14 (full_attention)    → 被 4 个层复用

Shared 层（借用 source 层的 KV）：
  15→13, 16→13, 17→13, 18→13, 19→14,
  20→13, 21→13, 22→13, 23→13, 24→14,
  25→13, 26→13, 27→13, 28→13, 29→14,
  30→13, 31→13, 32→13, 33→13, 34→14
</code></pre></div></div> <p>规律非常清晰：<strong>每 5 层中 4 层共享层 13（sliding attention），1 层共享层 14（full attention）</strong>。共享层依然有自己的 Q 投影和 O 投影，只是 KV 不再独立计算。</p> <p>这意味着 35 层模型只需要 <strong>15 份独立的 KV Cache</strong>（层 0-14），而不是 35 份。KV 内存<strong>直降 57%</strong>。</p> <p>E4B 的情况类似——42 层中有 18 层共享 KV，同样只需要 24 份独立 KV Cache，KV 内存降低约 <strong>43%</strong>。</p> <h3 id="42-为什么选层-13-和-14">4.2 为什么选层 13 和 14</h3> <p>这并非随机选择。层 13 是 sliding attention 层，层 14 是 full attention 层。共享的 20 层中：</p> <ul> <li><strong>Sliding attention 层</strong>（窗口注意力）共享层 13 的 KV——因为窗口注意力本身只关注局部上下文，共享一份局部 KV 就够了</li> <li><strong>Full attention 层</strong>（全局注意力）共享层 14 的 KV——全局注意力需要看到完整的历史 KV</li> </ul> <p>这种”分类型共享”的设计比简单的”所有层共享同一份 KV”更精细，保留了混合注意力的局部/全局分工。</p> <h3 id="43-实现细节">4.3 实现细节</h3> <p>HuggingFace 模型在每个 Attention 上标注了三个属性：</p> <ul> <li><code class="language-plaintext highlighter-rouge">is_kv_shared_layer</code>：是否是”借用方”</li> <li><code class="language-plaintext highlighter-rouge">kv_shared_layer_index</code>：借用哪一层的 KV</li> <li><code class="language-plaintext highlighter-rouge">store_full_length_kv</code>：是否是”提供方”</li> </ul> <p>Python 导出时，这些属性传递给 <code class="language-plaintext highlighter-rouge">FusedAttention</code> 自定义算子，写入 ONNX 图：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">kv_shared_idx</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">kv_shared_layer_index</span> <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">is_kv_shared_layer</span> <span class="k">else</span> <span class="o">-</span><span class="mi">1</span>
<span class="n">self</span><span class="p">.</span><span class="n">fused_attn</span> <span class="o">=</span> <span class="nc">FusedAttention</span><span class="p">(</span>
    <span class="p">...,</span>
    <span class="n">layer_index</span><span class="o">=</span><span class="n">layer_id</span><span class="p">,</span>
    <span class="n">kv_shared_layer_index</span><span class="o">=</span><span class="n">kv_shared_idx</span>
<span class="p">)</span>
</code></pre></div></div> <p>C++ 推理端，<code class="language-plaintext highlighter-rouge">CPUAttention</code> 通过一个<strong>层级注册表</strong>（<code class="language-plaintext highlighter-rouge">kv_registry</code>）管理 KV 共享关系：</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 创建 Attention 时注册到 kv_registry</span>
<span class="k">if</span> <span class="p">(</span><span class="n">layerIndex</span> <span class="o">&gt;=</span> <span class="mi">0</span> <span class="o">&amp;&amp;</span> <span class="n">meta</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">meta</span><span class="o">-&gt;</span><span class="n">kv_registry</span><span class="p">[</span><span class="n">layerIndex</span><span class="p">]</span> <span class="o">=</span> <span class="n">attn</span><span class="p">;</span>
<span class="p">}</span>

<span class="c1">// 共享层在 forward 时直接引用 source 层的 KV Cache</span>
<span class="k">if</span> <span class="p">(</span><span class="n">mKVSharedLayerIndex</span> <span class="o">&gt;=</span> <span class="mi">0</span>
    <span class="o">&amp;&amp;</span> <span class="n">mMeta</span><span class="o">-&gt;</span><span class="n">kv_registry</span><span class="p">[</span><span class="n">mKVSharedLayerIndex</span><span class="p">]</span> <span class="o">!=</span> <span class="nb">nullptr</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">auto</span> <span class="n">sourceKV</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">CPUAttention</span><span class="o">*&gt;</span><span class="p">(</span>
        <span class="n">mMeta</span><span class="o">-&gt;</span><span class="n">kv_registry</span><span class="p">[</span><span class="n">mKVSharedLayerIndex</span><span class="p">])</span><span class="o">-&gt;</span><span class="n">getKVCacheManager</span><span class="p">();</span>
    <span class="c1">// 使用 sourceKV 而非自己的 KV Cache</span>
<span class="p">}</span>
</code></pre></div></div> <p>为了支持 KV 共享，我们还把原来散布在 4 个文件中的 <code class="language-plaintext highlighter-rouge">KVMeta</code> 结构定义统一到了 <code class="language-plaintext highlighter-rouge">source/core/KVMeta.hpp</code>，新增 <code class="language-plaintext highlighter-rouge">kv_registry</code>（层级注册表）和 <code class="language-plaintext highlighter-rouge">kv_shared_map</code>（名称映射表）两个字段。同时给 FlatBuffers 的 <code class="language-plaintext highlighter-rouge">AttentionParam</code> 新增了 <code class="language-plaintext highlighter-rouge">layer_index</code> 和 <code class="language-plaintext highlighter-rouge">kv_shared_layer_index</code> 两个字段。</p> <h3 id="44-内存节省量化分析">4.4 内存节省量化分析</h3> <p>以 E2B 模型为例（head_dim=256, kv_heads=1, fp16 KV Cache）：</p> <table> <thead> <tr> <th>指标</th> <th>无 KV 共享</th> <th>KV 共享</th> <th>节省</th> </tr> </thead> <tbody> <tr> <td>独立 KV 层数</td> <td>35</td> <td>15</td> <td>-57%</td> </tr> <tr> <td>1K token KV Cache</td> <td>~17.5 MB</td> <td>~7.5 MB</td> <td>-57%</td> </tr> <tr> <td>4K token KV Cache</td> <td>~70 MB</td> <td>~30 MB</td> <td>-57%</td> </tr> <tr> <td>16K token KV Cache</td> <td>~280 MB</td> <td>~120 MB</td> <td>-57%</td> </tr> </tbody> </table> <p>对于 E2B 这样定位手机端侧的模型来说，KV Cache 的 57% 缩减意义重大——直接决定了在 4GB 内存的设备上能处理多长的上下文。</p> <hr/> <h2 id="五多模态视觉与音频">五、多模态：视觉与音频</h2> <p>Gemma3n 就已经是原生多模态模型（文本+视觉+音频），Gemma4 延续了这一设计，但<strong>视觉编码器架构发生了重大变化</strong>——从 Gemma3n 的 MobileNetV5（轻量级卷积网络）切换到了 SigLIP ViT（Vision Transformer），音频编码器则延续了 Conformer 架构。</p> <h3 id="51-视觉编码器">5.1 视觉编码器</h3> <p>Gemma3n 使用 MobileNetV5 作为视觉编码器，输出 256 个 soft token（<code class="language-plaintext highlighter-rouge">vision_soft_tokens_per_image=256</code>），hidden_size=2048。Gemma4 则切换到 <strong>ViT + Pooler + Embedder</strong> 三段式架构，引入了 2D position_ids 和 3×3 pooling：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Image → Patchify (16×16) → ViT Encoder → Pooler (3×3) → RMSNorm + Linear → image_embeds
</code></pre></div></div> <p><strong>Step 1: Patchify</strong></p> <p>图像按 16×16 切 patch，但在切之前会做两件事：</p> <ul> <li>保持原始宽高比（不做强制 resize 到正方形）</li> <li>宽高对齐到 48 像素（<code class="language-plaintext highlighter-rouge">patch_size(16) × pooling_kernel_size(3)</code>），确保后续 pooling 整除</li> </ul> <p><strong>Step 2: ViT Encoder</strong></p> <p>标准 Vision Transformer，但使用 <strong>2D position_ids</strong>（行列坐标）而非 1D 序号。这意味着同一行的 patch 共享行坐标，同一列的共享列坐标，比 1D 序号更好地保留了空间结构信息。</p> <p><strong>Step 3: Pooler</strong></p> <p>3×3 pooling 将 patch 数压缩 <strong>9 倍</strong>。例如一张 672×480 的图片产生 42×30=1260 个 patch，pooling 后变成 14×10=140 个 soft token。默认最大输出为 280 个 soft token。</p> <p><strong>Step 4: Embedder</strong></p> <p><code class="language-plaintext highlighter-rouge">RMSNorm + Linear</code> 将视觉特征投影到文本 hidden_size 空间，使图像 token 可以与文本 token 在同一个 Decoder 中处理。</p> <p>导出时手动展开了编码器 forward，避免 HF 代码中 <code class="language-plaintext highlighter-rouge">unfold</code> 和动态 mask 的 ONNX trace 问题。C++ 端的视觉处理同样手动实现了 patchify 和 position_ids 生成，并处理了 padding 对齐：</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// C++ 端视觉处理核心参数</span>
<span class="kt">int</span> <span class="n">patch_size</span> <span class="o">=</span> <span class="mi">16</span><span class="p">;</span>
<span class="kt">int</span> <span class="n">pooling_kernel_size</span> <span class="o">=</span> <span class="mi">3</span><span class="p">;</span>
<span class="kt">int</span> <span class="n">default_output_length</span> <span class="o">=</span> <span class="mi">280</span><span class="p">;</span>  <span class="c1">// 最大 soft token 数</span>
<span class="kt">int</span> <span class="n">max_patches</span> <span class="o">=</span> <span class="n">default_output_length</span> <span class="o">*</span> <span class="n">pooling_kernel_size</span> <span class="o">*</span> <span class="n">pooling_kernel_size</span><span class="p">;</span>  <span class="c1">// 2520</span>
</code></pre></div></div> <h3 id="52-音频编码器">5.2 音频编码器</h3> <p>Gemma3n 和 Gemma4 的音频编码器都基于 Conformer 架构，Gemma4 使用的是 <strong>USM（Universal Speech Model）</strong> 变体，核心差异在于 <strong>chunked attention</strong> 的引入——音频被分成固定长度的 chunk，每个 chunk 只关注自身和前后 context 窗口内的帧。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Audio → Mel Spectrogram → Subsample Conv → Conformer Encoder → RMSNorm + Linear → audio_embeds
</code></pre></div></div> <p><strong>Mel 特征提取</strong>采用 USM 格式：</p> <ul> <li>采样率：16000 Hz</li> <li>帧长：320 点（20ms）</li> <li>帧移：160 点（10ms）</li> <li>FFT 点数：512</li> <li>Mel 滤波器：128 维</li> <li>频率刻度：HTK</li> </ul> <p><strong>Conformer Encoder</strong> 是 USM 的标准结构：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>FeedForward1 → Attention (chunked) → LightConv1D → FeedForward2
</code></pre></div></div> <p>其中 Attention 使用分块滑窗——音频帧被切成等长 chunk，每个 chunk 只关注自身以及左右各若干帧的 context。这避免了对长音频做全局 attention 导致的 O(n²) 开销。</p> <p>ONNX 导出中最大的挑战是 HF 实现中使用了大量 <code class="language-plaintext highlighter-rouge">unfold</code> 操作来构建 chunked attention 的滑动窗口。<code class="language-plaintext highlighter-rouge">unfold</code> 在 ONNX trace 时会产生不可控的动态维度。我们实现了 <code class="language-plaintext highlighter-rouge">Gemma4AudioExportModel</code>，用 <strong>index-gather</strong> 替代 <code class="language-plaintext highlighter-rouge">unfold</code>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 用 gather 替代 unfold 构建 chunk context
</span><span class="n">offsets</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="n">context_size</span><span class="p">)</span>
<span class="n">block_starts</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="n">num_blocks</span><span class="p">)</span> <span class="o">*</span> <span class="n">chunk_size</span>
<span class="n">indices</span> <span class="o">=</span> <span class="p">(</span><span class="n">block_starts</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span> <span class="o">+</span> <span class="n">offsets</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="mi">0</span><span class="p">)).</span><span class="nf">reshape</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
<span class="n">result</span> <span class="o">=</span> <span class="n">padded</span><span class="p">[:,</span> <span class="n">indices</span><span class="p">].</span><span class="nf">reshape</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="n">num_blocks</span><span class="p">,</span> <span class="n">context_size</span><span class="p">,</span> <span class="n">H</span><span class="p">,</span> <span class="n">D</span><span class="p">)</span>
</code></pre></div></div> <h3 id="53-多模态与-ple-的交互">5.3 多模态与 PLE 的交互</h3> <p>多模态输入时，PLE 需要特殊处理：<strong>图像/音频位置的 token 不能使用对应的多模态 token ID 查 PLE embedding 表</strong>。因为 <code class="language-plaintext highlighter-rouge">&lt;image_token&gt;</code> 和 <code class="language-plaintext highlighter-rouge">&lt;audio_token&gt;</code> 这些特殊 token 在 PLE 表中的 embedding 没有经过有意义的训练，直接使用会引入噪声。</p> <p>解决方案是将多模态位置的 token ID 替换为 <code class="language-plaintext highlighter-rouge">pad_token_id</code>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">ple_ids</span> <span class="o">=</span> <span class="n">input_ids</span><span class="p">.</span><span class="nf">clone</span><span class="p">()</span>
<span class="k">for</span> <span class="n">attr</span> <span class="ow">in</span> <span class="p">[</span><span class="sh">'</span><span class="s">image_token_id</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">audio_token_id</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">video_token_id</span><span class="sh">'</span><span class="p">]:</span>
    <span class="n">token_id</span> <span class="o">=</span> <span class="nf">getattr</span><span class="p">(</span><span class="n">config</span><span class="p">,</span> <span class="n">attr</span><span class="p">,</span> <span class="bp">None</span><span class="p">)</span>
    <span class="k">if</span> <span class="nf">isinstance</span><span class="p">(</span><span class="n">token_id</span><span class="p">,</span> <span class="nb">int</span><span class="p">):</span>
        <span class="n">ple_ids</span><span class="p">[</span><span class="n">ple_ids</span> <span class="o">==</span> <span class="n">token_id</span><span class="p">]</span> <span class="o">=</span> <span class="n">pad_token_id</span>
</code></pre></div></div> <p>主 embedding 的投影输入也做同样处理——多模态位置使用 pad embedding 而非视觉/音频 embedding 来计算 PLE 投影。这保证了 PLE 在多模态场景下的数值稳定性。</p> <hr/> <h2 id="六其他架构适配">六、其他架构适配</h2> <h3 id="61-qkv-三-norm--attn_scale10">6.1 QKV 三 Norm + attn_scale=1.0</h3> <p>Gemma4 的注意力模块有一个独特设计：不仅对 Q 和 K 做 RMSNorm（这在 Gemma3 中就有），还对 <strong>V 也做了 RMSNorm</strong>（<code class="language-plaintext highlighter-rouge">v_norm</code>）。同时，注意力缩放因子固定为 1.0，不再使用传统的 <code class="language-plaintext highlighter-rouge">1/√head_dim</code>。</p> <p>这背后的逻辑是：三个 Norm 已经将 Q、K、V 的范数控制在稳定范围内，注意力 score 的数值就自然稳定了，不再需要额外的缩放。这也是为什么 Gemma4 的 head_dim 可以做到 512（Full Attention）——传统做法下 head_dim=512 意味着 <code class="language-plaintext highlighter-rouge">1/√512 ≈ 0.044</code> 的极小缩放因子，容易导致梯度消失。</p> <h3 id="62-kv-与异构-kv-head31b--26b-a4b">6.2 K=V 与异构 KV Head（31B / 26B-A4B）</h3> <p>31B 和 26B-A4B 的 Full Attention 层没有独立的 <code class="language-plaintext highlighter-rouge">v_proj</code>——K 和 V 源自同一个线性投影 <code class="language-plaintext highlighter-rouge">k_proj</code>，之后 K 走 <code class="language-plaintext highlighter-rouge">k_norm + RoPE</code>，V 走 <code class="language-plaintext highlighter-rouge">v_norm</code>（不经过 RoPE）。导出时在 <code class="language-plaintext highlighter-rouge">k_proj</code> 之后 clone 一份给 Value：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 自动检测 K=V
</span><span class="k">if</span> <span class="ow">not</span> <span class="nf">hasattr</span><span class="p">(</span><span class="n">attn</span><span class="p">,</span> <span class="sh">'</span><span class="s">v_proj</span><span class="sh">'</span><span class="p">):</span>
    <span class="n">k_eq_v</span> <span class="o">=</span> <span class="bp">True</span>
    <span class="c1"># k_proj output → clone → k_norm + RoPE (for K)
</span>    <span class="c1">#                       → v_norm (for V, no RoPE)
</span></code></pre></div></div> <p>这两个模型还有<strong>异构 KV Head</strong>：Sliding 层和 Full 层使用不同的 KV Head 数。以 31B 为例，Sliding 层 16 个 KV Head，Full 层仅 4 个 KV Head。我们从 <code class="language-plaintext highlighter-rouge">k_proj.out_features // head_dim</code> 自动推断，不需要在配置中硬编码。</p> <h3 id="63-decoder-级-moe26b-a4b">6.3 Decoder 级 MoE（26B-A4B）</h3> <p>26B-A4B 是 Gemma4 中唯一的 MoE 模型，采用”<strong>稠密 MLP 与 128 专家 Top-8 并行</strong>“的设计——两个分支独立处理 input，各自做 LayerNorm 后相加：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hidden_states
    ├─→ dense MLP → post_feedforward_layernorm_1 ─→ +
    └─→ MoE(128 experts, top-8) → post_feedforward_layernorm_2 ─→ +
                                                                  │
                                                            residual add
</code></pre></div></div> <p>这与 Qwen3.5 的 Shared Expert 设计思路类似——稠密 MLP 提供”保底能力”，MoE 专家提供”专业能力”。但 Gemma4 的 MoE 规模更大：128 个专家、Top-8 路由（Qwen3.5 MoE 是 64 专家 Top-8）。</p> <p>HuggingFace 原始模型中专家权重是 3D Parameter（<code class="language-plaintext highlighter-rouge">[num_experts, hidden, intermediate]</code>），导出时拆解为 ModuleList，复用 MNN 已有的 MoE 自定义算子管线。路由权重经过 <code class="language-plaintext highlighter-rouge">softmax → topk → normalize → per_expert_scale</code> 的标准流程。</p> <h3 id="64-双-rope-与部分旋转">6.4 双 RoPE 与部分旋转</h3> <p>Gemma4 的 Sliding 和 Full Attention 层不仅 head_dim 不同（256 vs 512），连 RoPE 的 theta 也不同：</p> <table> <thead> <tr> <th>层类型</th> <th>head_dim</th> <th>rope_theta</th> <th>含义</th> </tr> </thead> <tbody> <tr> <td>Sliding</td> <td>256</td> <td>10,000</td> <td>短距离、高频、局部编码</td> </tr> <tr> <td>Full</td> <td>512</td> <td>1,000,000</td> <td>长距离、低频、全局编码</td> </tr> </tbody> </table> <p>Full Attention 层还支持 <code class="language-plaintext highlighter-rouge">partial_rotary_factor</code>（部分旋转），仅对 head_dim 中的一部分维度应用旋转编码，其余维度保持位置无关。实现上复用了 Phi 系列模型的分支：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">model_type</span> <span class="ow">in</span> <span class="p">[</span><span class="sh">'</span><span class="s">phi-msft</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">gemma4</span><span class="sh">'</span><span class="p">]:</span>
    <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">phi_rotary_pos</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">cos</span><span class="p">,</span> <span class="n">sin</span><span class="p">)</span>  <span class="c1"># 部分旋转
</span></code></pre></div></div> <h3 id="65-cuda-oom-修复">6.5 CUDA OOM 修复</h3> <p>大模型导出时，量化阶段会将权重搬到 GPU 加速。原来的实现只在 <code class="language-plaintext highlighter-rouge">weight.cuda()</code> 外套了 try/except 捕获 OOM，但实际 OOM 往往发生在后续的计算步骤中（如 <code class="language-plaintext highlighter-rouge">(q_weight * multipliers).sum(axis=1)</code>）。</p> <p>修复方案是将 GPU 量化拆分为独立的 <code class="language-plaintext highlighter-rouge">_quant_on_device()</code> 函数，整个 GPU 计算路径包裹在 try/except 中：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">quant</span><span class="p">(</span><span class="n">weight</span><span class="p">,</span> <span class="n">quant_bit</span><span class="p">,</span> <span class="n">quant_block</span><span class="p">,</span> <span class="n">symmetric</span><span class="p">,</span> <span class="n">awq</span><span class="p">,</span> <span class="n">hqq</span><span class="p">):</span>
    <span class="k">if</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">is_available</span><span class="p">():</span>
        <span class="k">try</span><span class="p">:</span>
            <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">empty_cache</span><span class="p">()</span>
            <span class="k">return</span> <span class="nf">_quant_on_device</span><span class="p">(</span><span class="n">weight</span><span class="p">.</span><span class="nf">cuda</span><span class="p">(),</span> <span class="p">...)</span>
        <span class="k">except</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="n">OutOfMemoryError</span><span class="p">:</span>
            <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">empty_cache</span><span class="p">()</span>  <span class="c1"># 释放 GPU 内存，fallback 到 CPU
</span>    <span class="k">return</span> <span class="nf">_quant_on_device</span><span class="p">(</span><span class="n">weight</span><span class="p">,</span> <span class="p">...)</span>  <span class="c1"># CPU fallback
</span></code></pre></div></div> <p>这样大部分权重仍在 GPU 上快速完成量化，只有超大权重（如 PLE Embedding）自动回退到 CPU，兼顾速度和稳定性。</p> <hr/> <h2 id="七导出与使用">七、导出与使用</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># E2B 模型导出（int4 量化，PLE int4 量化）</span>
python llmexport.py <span class="nt">--path</span> gemma-4-E2B-it <span class="nt">--export</span> mnn <span class="nt">--hqq</span> <span class="nt">--embed_bit</span> 4

<span class="c"># E4B 模型导出</span>
python llmexport.py <span class="nt">--path</span> gemma-4-E4B-it <span class="nt">--export</span> mnn <span class="nt">--hqq</span> <span class="nt">--embed_bit</span> 4

<span class="c"># 推理</span>
./llm_demo /path/to/exported/model/config.json prompt.txt
</code></pre></div></div> <hr/> <h2 id="八e2b--e4b-端侧性能测评">八、E2B / E4B 端侧性能测评</h2> <p>测试环境：<strong>Apple M3 Pro</strong> 芯片 macOS 设备，MNN 最新版本，HQQ 4-bit 量化 + PLE int4 量化，CPU 后端，4 线程。</p> <h3 id="81-转换大小">8.1 转换大小</h3> <table> <thead> <tr> <th style="text-align: left">模型</th> <th style="text-align: center">LLM 权重</th> <th style="text-align: center">PLE Embedding</th> <th style="text-align: center">视觉编码器</th> <th style="text-align: center">音频编码器</th> <th style="text-align: center">总大小</th> </tr> </thead> <tbody> <tr> <td style="text-align: left"><strong>E2B</strong></td> <td style="text-align: center">1.3 GB</td> <td style="text-align: center">1.4 GB</td> <td style="text-align: center">215 MB</td> <td style="text-align: center">563 MB</td> <td style="text-align: center"><strong>3.5 GB</strong></td> </tr> <tr> <td style="text-align: left"><strong>E4B</strong></td> <td style="text-align: center">2.7 GB</td> <td style="text-align: center">1.6 GB</td> <td style="text-align: center">216 MB</td> <td style="text-align: center">575 MB</td> <td style="text-align: center"><strong>5.2 GB</strong></td> </tr> </tbody> </table> <p>值得注意的是，PLE Embedding 即使在 int4 量化后仍然占据了相当大的比例——E2B 的 PLE 甚至比 LLM 权重本身还大。这是 PLE 架构”用 Flash 存储换运行内存”设计思路的直接体现：这些参数从磁盘按需加载，不常驻运行内存。</p> <p>音频编码器（USM Conformer）在两个模型间大小几乎相同（~570 MB），视觉编码器（SigLIP ViT）同样如此（~215 MB），说明多模态编码器是共享架构、独立于 LLM 主干缩放的。</p> <h3 id="82-推理速度">8.2 推理速度</h3> <blockquote> <p><strong>注意</strong>：Gemma4 对激活精度要求较高，<strong>必须使用 Normal（fp32）精度</strong>才能保证输出质量（详见 8.5 节精度分析）。以下数据均在 Normal 精度下测得。作为对比，Qwen3.5 在 Low（fp16）精度下即可正常工作。</p> </blockquote> <p>在 CPU（Apple Silicon M3 Pro）后端、4 线程、Normal 精度、KV Cache 开启的条件下：</p> <table> <thead> <tr> <th style="text-align: left">模型</th> <th style="text-align: center">模型加载时间</th> <th style="text-align: center">Prefill 速度 (提示词处理)</th> <th style="text-align: center">Decode 速度 (生成)</th> </tr> </thead> <tbody> <tr> <td style="text-align: left"><strong>E2B</strong></td> <td style="text-align: center">~1.3 s</td> <td style="text-align: center">~186 tok/s</td> <td style="text-align: center">~35 tok/s</td> </tr> <tr> <td style="text-align: left"><strong>E4B</strong></td> <td style="text-align: center">~2.1 s</td> <td style="text-align: center">~102 tok/s</td> <td style="text-align: center">~24 tok/s</td> </tr> </tbody> </table> <p><strong>解读：</strong></p> <ul> <li>E2B 的 prefill 速度达到 186 tok/s，decode 约 35 tok/s，对于端侧模型来说流畅度良好。KV 共享（57% KV Cache 缩减）在这里功不可没——更少的 KV Cache 意味着更少的内存带宽消耗。</li> <li>E4B 由于 hidden_size 从 1536 增加到 2560、层数从 35 增加到 42，整体速度约为 E2B 的 60-70%，但 24 tok/s 的 decode 速度仍然可以提供可接受的流式输出体验。</li> <li>由于 Gemma4 必须使用 fp32 激活（而 Qwen3.5 只需 fp16），速度差距会进一步拉大。对比同级别的 Qwen3.5 系列（M3 Pro + 4线程 + fp16），Qwen3.5-2B 的 decode 为 70 tok/s（E2B 的 2 倍），Qwen3.5-4B 为 60 tok/s（E4B 的 2.5 倍）。</li> </ul> <h3 id="83-问题测试">8.3 问题测试</h3> <p>为了评估端侧模型的实际能力，我们用与 Qwen3.5 评测相同的题目进行测试。测试使用 <strong>Normal（fp32）精度</strong>。</p> <h4 id="逻辑陷阱测试">逻辑陷阱测试</h4> <p><strong>1. 洗车逻辑陷阱</strong></p> <blockquote> <p>题目：”距离我 30 米有家洗车店，我是开车去洗好还是走路去好？”</p> </blockquote> <ul> <li><strong>E2B</strong>：从便利性、体力、天气等多角度做了详细的利弊分析，条理清晰、没有重复，但<strong>未能识破题目的核心陷阱</strong>——洗车必须把车带过去，所以只能开车。</li> <li><strong>E4B</strong>：同样做了非常详细的分析，列出了开车和走路各自的优缺点，甚至给出了”快速决策树”，但<strong>同样未识破陷阱</strong>，没有意识到”洗车需要车”这个前提。</li> </ul> <p><strong>2. Strawberry 有几个字母 r？</strong></p> <ul> <li><strong>E2B</strong>：回答”2 个 r”。<strong>错误</strong>（正确答案是 3 个）。</li> <li><strong>E4B</strong>：回答”3 个 r”。<strong>正确</strong>。</li> </ul> <p><strong>3. 树上有 10 只鸟，猎人开枪打死 1 只，树上还剩几只？</strong></p> <ul> <li><strong>E2B</strong>：直接回答”9 只”，用数学 10-1=9 解题，<strong>未能理解枪声会吓跑其他鸟的常识逻辑</strong>。</li> <li><strong>E4B</strong>：给出了两种理解——”如果作为数学题是 9 只，如果作为脑筋急转弯是 0 只”，展现了较好的语境理解能力，但最终倾向 9 只。<strong>部分正确</strong>。</li> </ul> <p><strong>4. 鲁迅和周树人是什么关系？</strong></p> <ul> <li><strong>E2B</strong>：<strong>正确回答</strong>——”鲁迅就是周树人，周树人是鲁迅的本名”，并补充了生卒年份等信息。</li> <li><strong>E4B</strong>：<strong>正确回答</strong>——”鲁迅和周树人是同一个人，周树人是鲁迅本名，鲁迅是他的笔名”，简洁准确。</li> </ul> <h4 id="通用能力测试">通用能力测试</h4> <p><strong>1. 自我介绍</strong></p> <ul> <li><strong>E2B</strong>：简洁准确——”我是一个大型语言模型，由 Google DeepMind 开发。”</li> <li><strong>E4B</strong>：自称”Gemma，由 Google 训练”，并描述了自己的功能，完整流畅。</li> </ul> <p><strong>2. 数学计算：(17 × 23) + (45 ÷ 9) - 18 = ?</strong>（正确答案：378）</p> <ul> <li><strong>E2B</strong>：<strong>正确</strong>。使用分配律展开 17×23=391，45÷9=5，最终 391+5-18=378，过程清晰。</li> <li><strong>E4B</strong>：<strong>正确</strong>。同样给出了详细的分步计算，结果 378，并附上了步骤回顾。</li> </ul> <p><strong>3. 代码生成：Python 判断质数</strong></p> <ul> <li><strong>E2B</strong>：<strong>正确完整</strong>。生成了优化版的 <code class="language-plaintext highlighter-rouge">is_prime</code> 函数，包含偶数快速排除和只检查奇数的优化，附带测试用例。</li> <li><strong>E4B</strong>：<strong>正确完整</strong>。同样生成了带完整注释和测试用例的 <code class="language-plaintext highlighter-rouge">is_prime</code> 函数，代码质量高。</li> </ul> <p><strong>4. 中英翻译</strong></p> <ul> <li><strong>E2B</strong>：提供了三个翻译版本，核心翻译准确流畅：”Deep learning is changing the way we interact with computers, from speech recognition to autonomous driving; its applications are ubiquitous.” 输出完整无重复。</li> <li><strong>E4B</strong>：同样给出三个翻译选项和关键词汇解释，输出完整流畅。</li> </ul> <h4 id="测试总结">测试总结</h4> <table> <thead> <tr> <th>测试项</th> <th style="text-align: center">E2B</th> <th style="text-align: center">E4B</th> </tr> </thead> <tbody> <tr> <td>洗车逻辑</td> <td style="text-align: center">△ 分析详细但未识破陷阱</td> <td style="text-align: center">△ 分析详细但未识破陷阱</td> </tr> <tr> <td>Strawberry</td> <td style="text-align: center">✗ 回答 2 个</td> <td style="text-align: center">✓ 正确(3个)</td> </tr> <tr> <td>打鸟常识</td> <td style="text-align: center">✗ 回答 9 只</td> <td style="text-align: center">△ 提到 0 只但倾向 9 只</td> </tr> <tr> <td>鲁迅=周树人</td> <td style="text-align: center">✓ 正确</td> <td style="text-align: center">✓ 正确</td> </tr> <tr> <td>自我介绍</td> <td style="text-align: center">✓ 简洁准确</td> <td style="text-align: center">✓ 完整流畅</td> </tr> <tr> <td>数学计算</td> <td style="text-align: center">✓ 正确(378)</td> <td style="text-align: center">✓ 正确(378)</td> </tr> <tr> <td>代码生成</td> <td style="text-align: center">✓ 正确完整</td> <td style="text-align: center">✓ 正确完整</td> </tr> <tr> <td>翻译</td> <td style="text-align: center">✓ 质量好</td> <td style="text-align: center">✓ 质量好</td> </tr> <tr> <td><strong>通过率</strong></td> <td style="text-align: center"><strong>5/8</strong></td> <td style="text-align: center"><strong>6/8</strong></td> </tr> </tbody> </table> <p><strong>关键发现：</strong></p> <ul> <li><strong>E4B 整体略优于 E2B</strong>，尤其在 Strawberry 计数和打鸟常识等需要更强推理能力的题目上，更大的模型容量带来了可见的提升。</li> <li><strong>两个模型在基础任务上表现出色</strong>——自我介绍、数学计算、代码生成、翻译均通过，输出完整流畅，没有重复循环问题。</li> <li><strong>逻辑陷阱仍然是短板</strong>，洗车题两个模型都未能识破”洗车必须带车”的核心前提，这与端侧小模型的常识推理能力限制有关。</li> <li><strong>翻译是两个模型的共同强项</strong>，英文输出质量尤为出色，这与 Gemma 系列以英文为主的训练数据分布一致。</li> </ul> <h3 id="84-与-qwen35-同级别模型横向对比">8.4 与 Qwen3.5 同级别模型横向对比</h3> <p>Gemma4 E2B / E4B 分别对标 Qwen3.5-2B / Qwen3.5-4B，同为端侧定位的小参数量多模态模型。以下数据均来自同一台 <strong>Apple M3 Pro</strong> 设备、<strong>CPU 4 线程</strong>、<strong>HQQ 4-bit 量化</strong>的测试环境。注意：<strong>Gemma4 使用 Normal（fp32）精度，Qwen3.5 使用 Low（fp16）精度</strong>——两者均为各自能正常工作的最低精度配置。</p> <h4 id="模型规格对比">模型规格对比</h4> <table> <thead> <tr> <th style="text-align: left">对比项</th> <th style="text-align: center">Gemma4 E2B</th> <th style="text-align: center">Qwen3.5-2B</th> <th style="text-align: center">Gemma4 E4B</th> <th style="text-align: center">Qwen3.5-4B</th> </tr> </thead> <tbody> <tr> <td style="text-align: left"><strong>层数</strong></td> <td style="text-align: center">35</td> <td style="text-align: center">24</td> <td style="text-align: center">42</td> <td style="text-align: center">32</td> </tr> <tr> <td style="text-align: left"><strong>隐藏维度</strong></td> <td style="text-align: center">1536</td> <td style="text-align: center">2048</td> <td style="text-align: center">2560</td> <td style="text-align: center">2560</td> </tr> <tr> <td style="text-align: left"><strong>注意力机制</strong></td> <td style="text-align: center">混合(Sliding+Full)</td> <td style="text-align: center">混合(Linear+Full)</td> <td style="text-align: center">混合(Sliding+Full)</td> <td style="text-align: center">混合(Linear+Full)</td> </tr> <tr> <td style="text-align: left"><strong>KV Cache 优化</strong></td> <td style="text-align: center">KV 共享(57%)</td> <td style="text-align: center">Linear Attn(75%无KV)</td> <td style="text-align: center">KV 共享(43%)</td> <td style="text-align: center">Linear Attn(75%无KV)</td> </tr> <tr> <td style="text-align: left"><strong>所需激活精度</strong></td> <td style="text-align: center">fp32</td> <td style="text-align: center">fp16</td> <td style="text-align: center">fp32</td> <td style="text-align: center">fp16</td> </tr> <tr> <td style="text-align: left"><strong>Embedding</strong></td> <td style="text-align: center">PLE(每层独立)</td> <td style="text-align: center">共享 Embedding</td> <td style="text-align: center">PLE(每层独立)</td> <td style="text-align: center">共享 Embedding</td> </tr> <tr> <td style="text-align: left"><strong>视觉</strong></td> <td style="text-align: center">SigLIP ViT</td> <td style="text-align: center">内置视觉</td> <td style="text-align: center">SigLIP ViT</td> <td style="text-align: center">内置视觉</td> </tr> <tr> <td style="text-align: left"><strong>音频</strong></td> <td style="text-align: center">USM Conformer</td> <td style="text-align: center">无</td> <td style="text-align: center">USM Conformer</td> <td style="text-align: center">无</td> </tr> </tbody> </table> <p>两个系列的端侧 KV 优化思路截然不同：Qwen3.5 通过 75% Linear Attention 直接省掉大部分层的 KV Cache，而 Gemma4 通过层间 KV 共享减少独立 KV 份数。Gemma4 独有的 PLE 和音频模态使其在模型体积上显著更大。此外，Gemma4 对激活精度的要求更高（fp32 vs fp16），这进一步影响了推理速度和内存占用。</p> <h4 id="导出大小对比">导出大小对比</h4> <table> <thead> <tr> <th style="text-align: left">模型</th> <th style="text-align: center">量化后总大小</th> <th style="text-align: center">纯 LLM 权重</th> <th style="text-align: left">额外开销</th> </tr> </thead> <tbody> <tr> <td style="text-align: left"><strong>Gemma4 E2B</strong></td> <td style="text-align: center"><strong>3.5 GB</strong></td> <td style="text-align: center">1.3 GB</td> <td style="text-align: left">PLE 1.4GB + 视觉 215MB + 音频 563MB</td> </tr> <tr> <td style="text-align: left"><strong>Qwen3.5-2B</strong></td> <td style="text-align: center"><strong>1.37 GB</strong></td> <td style="text-align: center">~1.37 GB</td> <td style="text-align: left">视觉权重含在总大小内</td> </tr> <tr> <td style="text-align: left"><strong>Gemma4 E4B</strong></td> <td style="text-align: center"><strong>5.2 GB</strong></td> <td style="text-align: center">2.7 GB</td> <td style="text-align: left">PLE 1.6GB + 视觉 216MB + 音频 575MB</td> </tr> <tr> <td style="text-align: left"><strong>Qwen3.5-4B</strong></td> <td style="text-align: center"><strong>2.59 GB</strong></td> <td style="text-align: center">~2.59 GB</td> <td style="text-align: left">视觉权重含在总大小内</td> </tr> </tbody> </table> <p>Gemma4 E2B 的总大小是 Qwen3.5-2B 的 <strong>2.6 倍</strong>，E4B 是 Qwen3.5-4B 的 <strong>2.0 倍</strong>。这主要由三部分额外开销构成：PLE Embedding（~1.4-1.6 GB）、音频编码器（~570 MB）和独立的视觉编码器权重（~215 MB）。如果不需要音频能力，PLE 仍然是不可忽略的体积增量。</p> <h4 id="推理速度对比">推理速度对比</h4> <table> <thead> <tr> <th style="text-align: left">模型</th> <th style="text-align: center">激活精度</th> <th style="text-align: center">Prefill (tok/s)</th> <th style="text-align: center">Decode (tok/s)</th> <th style="text-align: center">首 Token 延迟</th> </tr> </thead> <tbody> <tr> <td style="text-align: left"><strong>Gemma4 E2B</strong></td> <td style="text-align: center">fp32</td> <td style="text-align: center">~186</td> <td style="text-align: center">~35</td> <td style="text-align: center">~1.3 s</td> </tr> <tr> <td style="text-align: left"><strong>Qwen3.5-2B</strong></td> <td style="text-align: center">fp16</td> <td style="text-align: center">~317</td> <td style="text-align: center">~58</td> <td style="text-align: center">~0.9 s</td> </tr> <tr> <td style="text-align: left"><strong>Gemma4 E4B</strong></td> <td style="text-align: center">fp32</td> <td style="text-align: center">~102</td> <td style="text-align: center">~24</td> <td style="text-align: center">~2.1 s</td> </tr> <tr> <td style="text-align: left"><strong>Qwen3.5-4B</strong></td> <td style="text-align: center">fp16</td> <td style="text-align: center">~127</td> <td style="text-align: center">~33</td> <td style="text-align: center">~1.1 s</td> </tr> </tbody> </table> <p>Qwen3.5 在推理速度上全面领先：</p> <ul> <li><strong>2B 级别</strong>：Qwen3.5-2B 的 decode 速度（58 tok/s）是 Gemma4 E2B（35 tok/s）的 <strong>1.5 倍</strong></li> <li><strong>4B 级别</strong>：Qwen3.5-4B 的 decode 速度（33 tok/s）是 Gemma4 E4B（24 tok/s）的 <strong>1.3 倍</strong></li> </ul> <p>Gemma4 更慢的原因：(1) <strong>需要 fp32 激活</strong>而 Qwen3.5 只需 fp16，这是最大的速度差距来源；(2) 层数更多（35/42 层 vs 24/32 层），意味着更多的矩阵运算；(3) 双 RoPE（Sliding head_dim=256 + Full head_dim=512）比 Qwen3.5 的统一 RoPE 计算量更大；(4) PLE 的门控注入每层都有额外的 Linear + 激活 + 乘法开销；(5) QKV 三 Norm 比 Qwen3.5 的 QK Norm 多一次归一化。</p> <h4 id="功能质量对比">功能质量对比</h4> <table> <thead> <tr> <th>测试项</th> <th style="text-align: center">Gemma4 E2B (fp32)</th> <th style="text-align: center">Qwen3.5-2B (fp16)</th> <th style="text-align: center">Gemma4 E4B (fp32)</th> <th style="text-align: center">Qwen3.5-4B (fp16)</th> </tr> </thead> <tbody> <tr> <td>洗车逻辑</td> <td style="text-align: center">△ 详细分析未识破</td> <td style="text-align: center">△ 结论对理由错</td> <td style="text-align: center">△ 详细分析未识破</td> <td style="text-align: center">✗ 建议走路</td> </tr> <tr> <td>Strawberry</td> <td style="text-align: center">✗ 回答 2 个</td> <td style="text-align: center">✗ 死循环</td> <td style="text-align: center">✓ 正确(3个)</td> <td style="text-align: center">✓ 正确(3个)</td> </tr> <tr> <td>打鸟常识</td> <td style="text-align: center">✗ 回答 9 只</td> <td style="text-align: center">✗ 回答 9 只</td> <td style="text-align: center">△ 提到 0 只但倾向 9 只</td> <td style="text-align: center">✓ 正确(0只)</td> </tr> <tr> <td>鲁迅=周树人</td> <td style="text-align: center">✓ 正确</td> <td style="text-align: center">✗ 完全错误</td> <td style="text-align: center">✓ 正确</td> <td style="text-align: center">✓ 正确</td> </tr> <tr> <td>自我介绍</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> </tr> <tr> <td>数学计算</td> <td style="text-align: center">✓ 正确(378)</td> <td style="text-align: center">△</td> <td style="text-align: center">✓ 正确(378)</td> <td style="text-align: center">✓</td> </tr> <tr> <td>代码生成</td> <td style="text-align: center">✓ 正确完整</td> <td style="text-align: center">△</td> <td style="text-align: center">✓ 正确完整</td> <td style="text-align: center">✓</td> </tr> <tr> <td>翻译</td> <td style="text-align: center">✓ 质量好</td> <td style="text-align: center">△</td> <td style="text-align: center">✓ 质量好</td> <td style="text-align: center">✓</td> </tr> <tr> <td><strong>通过率</strong></td> <td style="text-align: center"><strong>5/8</strong></td> <td style="text-align: center"><strong>1/8</strong></td> <td style="text-align: center"><strong>6/8</strong></td> <td style="text-align: center"><strong>7/8</strong></td> </tr> </tbody> </table> <p><strong>对比结论：</strong></p> <ol> <li> <p><strong>同为 fp16 时 Qwen3.5 优势明显</strong>：Qwen3.5 在 fp16 精度下即可正常工作且表现良好，而 Gemma4 在 fp16 下会出现灾难性的重复循环（详见 8.5 节），必须回退到 fp32 才能获得可用的输出质量。仅从”同精度可用性”角度看，Qwen3.5 的架构对低精度更友好。</p> </li> <li> <p><strong>即使 Gemma4 用 fp32，语言能力仍略逊一筹</strong>：4B 级别上 E4B（6/8）接近但未超过 Qwen3.5-4B（7/8）；2B 级别上 E2B（5/8）虽大幅领先 Qwen3.5-2B（1/8），但 Qwen3.5-2B 本身定位偏低，这一优势参考价值有限。整体而言，<strong>纯语言任务上 Qwen3.5 的小模型系列表现更好</strong>。</p> </li> <li> <p><strong>速度和体积差距显著</strong>：Gemma4 依赖 fp32 激活，decode 速度仅为 Qwen3.5（fp16）的 40-50%；加上 PLE 和音频编码器，模型体积是 Qwen3.5 的 2-2.6 倍。</p> </li> <li> <p><strong>Gemma4 的差异化优势在多模态</strong>：Gemma4 独有<strong>原生音频理解</strong>能力，这是 Qwen3.5 端侧系列不具备的。如果应用场景涉及语音交互或音频分析，Gemma4 是目前端侧唯一的选择。纯文本场景下，Qwen3.5 在速度、体积和语言质量上综合更优。</p> </li> </ol> <h3 id="85-精度敏感性为什么-gemma4-必须用-fp32">8.5 精度敏感性：为什么 Gemma4 必须用 fp32</h3> <p>前文所有测试均在 Normal（fp32）精度下进行。这并非随意选择——<strong>Gemma4 在 Low（fp16）精度下会出现严重的 token 级重复循环</strong>，几乎所有任务都无法正常完成。</p> <h4 id="fp16-下的灾难性退化">fp16 下的灾难性退化</h4> <p>将 config.json 中的 <code class="language-plaintext highlighter-rouge">precision</code> 设为 <code class="language-plaintext highlighter-rouge">"low"</code>（fp16）后，两个模型的表现：</p> <table> <thead> <tr> <th>测试项</th> <th style="text-align: center">E2B (fp16)</th> <th style="text-align: center">E4B (fp16)</th> </tr> </thead> <tbody> <tr> <td>洗车逻辑</td> <td style="text-align: center">✗ 重复循环</td> <td style="text-align: center">✗ 重复循环</td> </tr> <tr> <td>Strawberry</td> <td style="text-align: center">✗ 回答 2 个</td> <td style="text-align: center">✗ 回答 1 个</td> </tr> <tr> <td>打鸟常识</td> <td style="text-align: center">✗ 识别陷阱后重复循环</td> <td style="text-align: center">✗ 无法理解中文输入</td> </tr> <tr> <td>鲁迅=周树人</td> <td style="text-align: center">✗ 完全错误+重复循环</td> <td style="text-align: center">✗ 重复循环</td> </tr> <tr> <td>自我介绍</td> <td style="text-align: center">✓</td> <td style="text-align: center">△ 简繁混杂不完整</td> </tr> <tr> <td>数学计算</td> <td style="text-align: center">✗ 切换到印地语+重复</td> <td style="text-align: center">✗ 未算出结果</td> </tr> <tr> <td>代码生成</td> <td style="text-align: center">△ 有语法错误+重复</td> <td style="text-align: center">✗ 未完成+重复</td> </tr> <tr> <td>翻译</td> <td style="text-align: center">✓ 但末尾轻微重复</td> <td style="text-align: center">△ 翻译好但术语部分重复</td> </tr> <tr> <td><strong>通过率</strong></td> <td style="text-align: center"><strong>2/8</strong></td> <td style="text-align: center"><strong>0/8</strong></td> </tr> </tbody> </table> <p>对比 fp32 下 E2B 5/8、E4B 6/8 的通过率，差距是毁灭性的。fp16 下 E4B 甚至不如 E2B，多次出现语言混杂（简繁混杂、中英切换、甚至冒出印地语）和完全无法理解中文输入的情况。</p> <h4 id="fp16-速度更快但不可用">fp16 速度更快，但不可用</h4> <table> <thead> <tr> <th style="text-align: left">模型</th> <th style="text-align: center">精度</th> <th style="text-align: center">Prefill (tok/s)</th> <th style="text-align: center">Decode (tok/s)</th> </tr> </thead> <tbody> <tr> <td style="text-align: left"><strong>E2B</strong></td> <td style="text-align: center">fp16</td> <td style="text-align: center">~229</td> <td style="text-align: center">~44</td> </tr> <tr> <td style="text-align: left"><strong>E2B</strong></td> <td style="text-align: center">fp32</td> <td style="text-align: center">~186</td> <td style="text-align: center">~35</td> </tr> <tr> <td style="text-align: left"><strong>E4B</strong></td> <td style="text-align: center">fp16</td> <td style="text-align: center">~114</td> <td style="text-align: center">~24</td> </tr> <tr> <td style="text-align: left"><strong>E4B</strong></td> <td style="text-align: center">fp32</td> <td style="text-align: center">~102</td> <td style="text-align: center">~24</td> </tr> </tbody> </table> <p>fp16 确实快 10-20%，但输出质量的崩塌使这个速度优势毫无意义。</p> <h4 id="原因分析为什么-gemma4-对精度敏感而-qwen35-不敏感">原因分析：为什么 Gemma4 对精度敏感而 Qwen3.5 不敏感</h4> <p><strong>Qwen3.5 在 fp16 下即可正常工作</strong>，不存在类似的精度退化。两者的关键架构差异解释了这一现象：</p> <ol> <li> <p><strong>attn_scale=1.0</strong>：Gemma4 不做传统的 <code class="language-plaintext highlighter-rouge">1/√head_dim</code> 注意力缩放，完全依赖 QKV 三 Norm 来稳定 attention score。fp16 下 Norm 的精度损失会导致 attention score 范围失控。Qwen3.5 使用标准的 <code class="language-plaintext highlighter-rouge">1/√head_dim</code> 缩放，对 Norm 精度的依赖更低。</p> </li> <li> <p><strong>层数更深</strong>：E2B 35 层、E4B 42 层，比 Qwen3.5 同级别多 11-10 层。fp16 的误差在更多层中累积，最终导致注意力分布退化为重复模式。</p> </li> <li> <p><strong>PLE 门控注入</strong>：每层额外的 Linear → 激活 → 逐元素乘法路径引入了更多的浮点运算环节，每个环节都会在 fp16 下损失精度。</p> </li> <li> <p><strong>双 RoPE</strong>：Full Attention 层使用 head_dim=512 + theta=1000000 的大尺度旋转编码，在 fp16 下容易出现三角函数计算的精度问题。</p> </li> </ol> <p><strong>结论</strong>：Gemma4 对激活精度的要求显著高于 Qwen3.5，这是其架构设计的固有特性。部署时<strong>必须使用 Normal（fp32）精度</strong>，config.json 中设置 <code class="language-plaintext highlighter-rouge">"precision": "normal"</code>。</p> <hr/> <h2 id="九各版本选型建议">九、各版本选型建议</h2> <h3 id="e2b极致端侧手机优先">E2B：极致端侧，手机优先</h3> <ul> <li><strong>定位</strong>：4-8GB 内存设备的首选模型</li> <li><strong>优势</strong>：KV 共享带来极低内存占用；35 层架构在端侧参数量下仍保持不错的推理能力</li> <li><strong>劣势</strong>：hidden_size 仅 1536，知识容量有限</li> <li><strong>推荐场景</strong>：手机助手、离线问答、IoT 设备、嵌入式应用</li> </ul> <h3 id="e4b桌面端侧平衡之选">E4B：桌面端侧，平衡之选</h3> <ul> <li><strong>定位</strong>：8-16GB 内存设备的主力模型</li> <li><strong>优势</strong>：相比 E2B 显著提升的模型能力，42 层 + hidden=2560 的深度和宽度组合带来更好的推理质量</li> <li><strong>劣势</strong>：需要更多内存，不适合低端手机</li> <li><strong>推荐场景</strong>：平板电脑、笔记本、桌面端 AI 助手、本地多模态分析</li> </ul> <h3 id="31b服务器级高质量推理">31B：服务器级，高质量推理</h3> <ul> <li><strong>定位</strong>：16GB+ 内存的高性能部署</li> <li><strong>优势</strong>：60 层、hidden=5376 提供接近旗舰模型的推理质量；K=V 机制减少了参数量</li> <li><strong>劣势</strong>：资源需求高，端侧不可行</li> <li><strong>推荐场景</strong>：服务端推理、企业级应用、专业级多模态理解</li> </ul> <h3 id="26b-a4bmoe-架构高效推理">26B-A4B：MoE 架构，高效推理</h3> <ul> <li><strong>定位</strong>：26B 总参数、仅 4B 激活参数</li> <li><strong>优势</strong>：128 专家 Top-8 MoE 设计实现了”大模型知识、小模型算力”的效果；每次推理只激活约 4B 参数</li> <li><strong>劣势</strong>：模型文件体积大（所有专家权重都需要存储）；MoE 路由开销</li> <li><strong>推荐场景</strong>：服务端需要高质量但低推理延迟的场景、多任务并发</li> </ul> <hr/> <h2 id="十总结">十、总结</h2> <table> <thead> <tr> <th>机制</th> <th style="text-align: center">E2B</th> <th style="text-align: center">E4B</th> <th style="text-align: center">31B</th> <th style="text-align: center">26B-A4B</th> </tr> </thead> <tbody> <tr> <td>混合注意力 + 双RoPE</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> </tr> <tr> <td>PLE（每层调制信号）</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> </tr> <tr> <td>QKV 三 Norm</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> </tr> <tr> <td>KV 共享</td> <td style="text-align: center">✓(20层)</td> <td style="text-align: center">✓(18层)</td> <td style="text-align: center">—</td> <td style="text-align: center">—</td> </tr> <tr> <td>K=V (无 v_proj)</td> <td style="text-align: center">—</td> <td style="text-align: center">—</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> </tr> <tr> <td>异构 KV Head</td> <td style="text-align: center">—</td> <td style="text-align: center">—</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> </tr> <tr> <td>MoE (128专家 top-8)</td> <td style="text-align: center">—</td> <td style="text-align: center">—</td> <td style="text-align: center">—</td> <td style="text-align: center">✓</td> </tr> <tr> <td>视觉</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> </tr> <tr> <td>音频</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> <td style="text-align: center">✓</td> </tr> </tbody> </table> <p>Gemma4 的适配共涉及 34 个文件、+1816/-401 行代码。设计原则是<strong>从权重形状自动推断</strong>而非硬编码——<code class="language-plaintext highlighter-rouge">k_eq_v</code> 通过检测 <code class="language-plaintext highlighter-rouge">v_proj</code>、<code class="language-plaintext highlighter-rouge">num_key_value_heads</code> 从 <code class="language-plaintext highlighter-rouge">k_proj</code> 推断、MoE 检测 <code class="language-plaintext highlighter-rouge">experts</code> 属性——一套代码自动适配四种规格。</p> <p>三个核心创新的适配总结：</p> <ol> <li><strong>PLE</strong>：两路查表 + 门控注入，支持 bf16/int4/int8 量化导出，C++ DiskEmbedding 统一加载</li> <li><strong>KV 共享</strong>：层级注册表 + FlatBuffers 扩展，E2B 节省 57% KV 内存，使端侧长上下文成为可能</li> <li><strong>多模态</strong>：视觉（ViT + 3×3 Pooler）+ 音频（USM Conformer），unfold→gather 解决 ONNX trace 问题</li> </ol> <p>值得一提的是，本次 Gemma4 全系列的适配<strong>代码 100% 由 AI 生成</strong>，基于我们内部研发的 <strong>s-coder 平台</strong>（MNN LLM支持 Agent）完成。人工参与集中在关键节点验证、debug 思路分析和部分实现方案的引导上——从架构分析、需求理解、代码生成、调试修复到最终验证，Agent 全程自主完成了 34 个文件、2000+ 行代码的编写。这也是我们在大型推理引擎项目上验证 AI Coding 落地效果的一次实践。</p> <h2 id="资源链接">资源链接</h2> <ul> <li>Github Issue 需求: https://github.com/alibaba/MNN/issues/4340</li> <li>Github 代码实现: https://github.com/alibaba/MNN/commit/ba76938ec0935a6bf7f01f348d975f9c8d8b5816</li> <li>HuggingFace 模型合集: https://huggingface.co/collections/taobao-mnn/gemma-4-mnn</li> <li>ModelScope 模型合集: https://modelscope.cn/collections/MNN/gemma-4-MNN</li> <li>Gemma3n 架构分析: https://zhuanlan.zhihu.com/p/1929221256969945147</li> </ul>]]></content><author><name></name></author><category term="llm"/><category term="mnn"/><summary type="html"><![CDATA[一、背景]]></summary></entry><entry><title type="html">Claude有多大？从推理速度反推参数量</title><link href="https://zhaode.wang/blog/2026/claude-parameter-analysis/" rel="alternate" type="text/html" title="Claude有多大？从推理速度反推参数量"/><published>2026-03-28T00:00:00+00:00</published><updated>2026-03-28T00:00:00+00:00</updated><id>https://zhaode.wang/blog/2026/claude-parameter-analysis</id><content type="html" xml:base="https://zhaode.wang/blog/2026/claude-parameter-analysis/"><![CDATA[<h2 id="起因">起因</h2> <p>3 月 26 日，一次 CMS 配置错误让 Anthropic 下一代旗舰模型 <strong>Claude Capybara</strong>（内部代号 Mythos）意外曝光，Anthropic 随后向 Fortune 杂志确认了它的存在，称其为”我们构建过的最强大模型”。要知道现在的 Opus 4.6 已经是我日常编程的主力工具了，而 Capybara 在 Terminal-Bench 2.0 等编程基准上还要远超 Opus，网络安全领域的漏洞利用能力更是被描述为”远超任何其他 AI 模型”——强到 Anthropic 只敢先给少数网络防御客户测试。</p> <p>不过能力提升的代价也很直观：<strong>Capybara 的推理速度可能只有 Opus 4.6 的 40%</strong>，这背后大概率是参数量的大幅增加。这篇文章就是尝试从推理速度反推 Claude 的参数规模——结论是 <strong>Opus 4.6 激活参数约 80–110B，总参数约 1.5–2.5T</strong>；而 <strong>Capybara 激活参数可能在 150–250B，总参数突破 4T</strong>。</p> <h2 id="数据与方法">数据与方法</h2> <p>Anthropic 不公开参数量，但参数量可以从推理速度反推。思路很简单：在 Google Vertex、Amazon Bedrock 这类第三方托管平台上，同一个服务商为所有模型提供的是同一套推理基础设施，没有理由针对某个特定模型故意降速或加速。那么不同模型之间的吞吐量差异，反映的就是模型本身参数规模的差异。</p> <p>所以我的做法是：在同一平台上收集一批参数量已知的开源模型（DeepSeek、GLM、Kimi、Llama 等）的推理速度，建立参数和速度的对应关系，再用这个关系去反推 Claude 的参数量。</p> <h3 id="google-vertex-平台">Google Vertex 平台</h3> <table> <thead> <tr> <th>模型</th> <th>Total Params</th> <th>Active Params</th> <th>Architecture</th> <th>Precision</th> <th>Throughput (TPS)</th> <th>Latency</th> </tr> </thead> <tbody> <tr> <td>DeepSeek V3.1</td> <td>685B</td> <td>37B</td> <td>MoE</td> <td>FP8</td> <td>110</td> <td>0.78s</td> </tr> <tr> <td>GLM-4.7</td> <td>355B</td> <td>32B</td> <td>MoE</td> <td>FP8</td> <td>108</td> <td>0.78s</td> </tr> <tr> <td>Kimi K2 Thinking</td> <td>1.04T</td> <td>32B</td> <td>MoE</td> <td>FP4</td> <td>206.5</td> <td>0.37s</td> </tr> <tr> <td>MiniMax M2</td> <td>230B</td> <td>10B</td> <td>MoE</td> <td>FP8</td> <td>131.5</td> <td>3.25s</td> </tr> <tr> <td>GPT-OSS-120B</td> <td>117B</td> <td>5.1B</td> <td>MoE</td> <td>FP8</td> <td>297</td> <td>0.33s</td> </tr> <tr> <td>GPT-OSS-20B</td> <td>21B</td> <td>3.6B</td> <td>MoE</td> <td>FP8</td> <td>267</td> <td>0.30s</td> </tr> <tr> <td>Llama 3.3 70B</td> <td>70B</td> <td>70B</td> <td><strong>Dense</strong></td> <td>FP8</td> <td>61</td> <td>0.20s</td> </tr> <tr> <td><strong>Claude Opus 4.6</strong></td> <td>?</td> <td>?</td> <td>?</td> <td>FP8</td> <td><strong>41</strong></td> <td>1.37s</td> </tr> <tr> <td><strong>Claude Sonnet 4.6</strong></td> <td>?</td> <td>?</td> <td>?</td> <td>FP8</td> <td><strong>46</strong></td> <td>1.35s</td> </tr> </tbody> </table> <h3 id="amazon-bedrock-平台">Amazon Bedrock 平台</h3> <table> <thead> <tr> <th>模型</th> <th>Total Params</th> <th>Active Params</th> <th>Architecture</th> <th>Throughput (TPS)</th> <th>Latency</th> </tr> </thead> <tbody> <tr> <td>GPT-OSS-20B</td> <td>21B</td> <td>3.6B</td> <td>MoE</td> <td>210</td> <td>1.02s</td> </tr> <tr> <td>GPT-OSS-120B</td> <td>117B</td> <td>5.1B</td> <td>MoE</td> <td>171</td> <td>1.32s</td> </tr> <tr> <td><strong>Claude Opus 4.6</strong></td> <td>?</td> <td>?</td> <td>?</td> <td><strong>35</strong></td> <td>2.49s</td> </tr> <tr> <td><strong>Claude Sonnet 4.6</strong></td> <td>?</td> <td>?</td> <td>?</td> <td><strong>39</strong></td> <td>1.99s</td> </tr> </tbody> </table> <blockquote> <p>Bedrock 上 Claude 的速度比 Vertex 慢约 15%，这是 provider 端的队列策略差异，不影响模型本身的判断。后续分析以 Vertex 数据为准。</p> </blockquote> <h2 id="推导逻辑">推导逻辑</h2> <p>LLM 的 Decode 阶段是逐 token 自回归生成，瓶颈在显存带宽（memory-bound）。理论吞吐量满足：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>TPS ≈ Memory_Bandwidth / (Active_Params × Bytes_Per_Param)
→ Active_Params × TPS × Precision_Factor ≈ Constant
</code></pre></div></div> <p>简单说：在同一硬件平台、同一量化精度下，<strong>激活参数 × 吞吐量</strong> 应该近似等于一个常数。如果这个常数靠谱，用它除以 Claude 的 TPS，就能算出 Claude 的激活参数。</p> <p>先用开源模型验证一下。设 FP8 factor = 1.0，FP4 factor = 0.5，归一化后的乘积：</p> <table> <thead> <tr> <th>模型</th> <th>Active</th> <th>TPS</th> <th>Raw Product</th> <th>Precision</th> <th>Normalized</th> </tr> </thead> <tbody> <tr> <td>DeepSeek V3.1</td> <td>37B</td> <td>110</td> <td>4,070</td> <td>FP8</td> <td><strong>4,070</strong></td> </tr> <tr> <td>GLM-4.7</td> <td>32B</td> <td>108</td> <td>3,456</td> <td>FP8</td> <td><strong>3,456</strong></td> </tr> <tr> <td>Kimi K2 Thinking</td> <td>32B</td> <td>206.5</td> <td>6,608</td> <td><strong>FP4</strong></td> <td><strong>3,304</strong></td> </tr> <tr> <td>MiniMax M2</td> <td>10B</td> <td>131.5</td> <td>1,315</td> <td>FP8</td> <td>1,315</td> </tr> <tr> <td>GPT-OSS-120B</td> <td>5.1B</td> <td>297</td> <td>1,515</td> <td>FP8</td> <td>1,515</td> </tr> <tr> <td>GPT-OSS-20B</td> <td>3.6B</td> <td>267</td> <td>961</td> <td>FP8</td> <td>961</td> </tr> <tr> <td><strong>Llama 3.3 70B</strong></td> <td><strong>70B</strong></td> <td>61</td> <td><strong>4,270</strong></td> <td>FP8</td> <td><strong>4,270</strong></td> </tr> </tbody> </table> <p>几个观察：</p> <ol> <li><strong>大模型的乘积比较稳定</strong>：Llama 3.3 70B (4,270)、DeepSeek (4,070)、GLM (3,456)、Kimi FP4 归一化后 (3,304)，四者都在 3,300–4,300 区间。这说明 30B+ 激活参数的模型（不管是 Dense 还是 MoE），decode 确实是 memory-bound 主导，乘积近似常数的假设成立。</li> <li><strong>Llama 3.3 70B 是最有价值的参考点</strong>：它是 Dense 架构，Active = Total = 70B，没有 MoE 的 expert routing 开销，也没有激活参数不确定性的问题。而且 70B 正好接近 Claude 的预测区间，作为锚点比 30B 级别的 MoE 模型更直接。</li> <li><strong>小模型的乘积明显偏低</strong>：GPT-OSS-120B (1,515)、GPT-OSS-20B (961)、MiniMax M2 (1,315)，active 越小乘积越低。这是因为小模型的 compute-bound 占比更高，达不到 memory-bound 的理论上限——换句话说，小模型跑得比”应该”的速度慢，乘积就偏低了。</li> <li><strong>Kimi K2 Thinking 的原始乘积 (6,608) 远高于 FP8 模型</strong>，按 FP4 归一化后落回 3,304，和 DeepSeek、GLM 一个区间。这说明 Kimi 大概率用了 FP4 或混合精度部署。</li> </ol> <p>Claude 的激活参数预计在 70B+（后面会算出来），属于大模型范畴，用大模型的乘积作为基准是合理的。<strong>基准常数取 3,300–4,300 这个区间</strong>，中心值约 3,800。</p> <h2 id="claude-激活参数计算">Claude 激活参数计算</h2> <h3 id="原始反推">原始反推</h3> <p>用基准常数的范围来算：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Opus Active  ≈ 3,300~4,300 / 41 ≈ 80~105B
Sonnet Active ≈ 3,300~4,300 / 46 ≈ 72~93B
</code></pre></div></div> <h3 id="thinking-tokens">Thinking Tokens</h3> <p>Vertex 标注的 TPS “Includes reasoning tokens”。不过 thinking tokens 的生成和普通 tokens 一样，都是 decode 阶段的自回归操作，计算模式完全相同。所以 TPS 已经反映了模型的真实 decode 性能，不需要额外修正。</p> <h3 id="context-length-修正">Context Length 修正</h3> <p>Opus/Sonnet 支持 1M context，KV cache 和 attention 的开销比 128K 模型多约 3% 的带宽消耗，小幅上调：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Opus  ≈ (3,300~4,300 / 41) × 1.03 ≈ 83~108B
Sonnet ≈ (3,300~4,300 / 46) × 1.03 ≈ 74~96B
</code></pre></div></div> <p>综合两个平台的数据：</p> <ul> <li><strong>Opus 激活参数</strong>: 80–110B（中心值约 95B）</li> <li><strong>Sonnet 激活参数</strong>: 70–95B（中心值约 83B）</li> </ul> <p>值得注意的是，Llama 3.3 70B 的 Dense 70B 在 Vertex 上只有 61 TPS，而 Claude Opus 只有 41 TPS——如果 Claude 也是 Dense 架构，那它的参数至少在 70 × (61/41) ≈ 104B。但 Claude 大概率是 MoE（从 Opus 和 Sonnet 速度接近但能力差距明显来看），MoE 的 routing overhead 会让实际 TPS 略低于纯 memory-bound 预测，所以激活参数的真实值可能在这个范围的中下部。</p> <h2 id="总参数估计">总参数估计</h2> <p>MoE 架构的 Total / Active ratio 在不同模型之间差异很大，看一下开源模型的数据：</p> <table> <thead> <tr> <th>模型</th> <th>Active</th> <th>Total</th> <th>Ratio</th> </tr> </thead> <tbody> <tr> <td>GPT-OSS-20B</td> <td>3.6B</td> <td>21B</td> <td>5.8x</td> </tr> <tr> <td>GLM-4.7</td> <td>32B</td> <td>355B</td> <td>11.1x</td> </tr> <tr> <td>DeepSeek V3.1</td> <td>37B</td> <td>685B</td> <td>18.5x</td> </tr> <tr> <td>MiniMax M2</td> <td>10B</td> <td>230B</td> <td>23x</td> </tr> <tr> <td>Kimi K2</td> <td>32B</td> <td>1.04T</td> <td>32.5x</td> </tr> </tbody> </table> <p>这个 ratio 没有简单的规律——同样是 32B 激活参数，GLM 取 11x 而 Kimi 取 32.5x，差了 3 倍。ratio 的大小更多取决于各家的设计选择：expert 数量、expert 粒度、是否共享 expert 等等，不能简单地从激活参数推断。</p> <p>不过可以缩小范围。从工程实践看，ratio 的主流区间在 <strong>10x–25x</strong> 之间（Kimi 的 32.5x 属于比较极端的设计）。以 Opus 80–110B 的激活参数为基础：</p> <ul> <li>取保守的 <strong>15x</strong>：Total ≈ 1.2–1.7T</li> <li>取中间的 <strong>20x</strong>：Total ≈ 1.6–2.2T</li> <li>取激进的 <strong>25x</strong>：Total ≈ 2.0–2.8T</li> </ul> <p>考虑到 Anthropic 的工程风格偏稳健，不太可能走 Kimi 那种极端路线，<strong>Opus 4.6 的总参数大概在 1.5–2.5T 这个量级</strong>，2T 附近是一个合理的中心估计。Sonnet 4.6 则略小，大概在 1.0–2.0T。</p> <h2 id="capybara-参数预测">Capybara 参数预测</h2> <p>泄露信息显示 Capybara 定位是超越 Opus 的下一代旗舰，在编程（Terminal-Bench 2.0 新纪录）、推理和网络安全方面都有大幅提升，目前仅对少数”网络防御”方向的客户开放早期测试。</p> <p>如果 Opus 4.6 的激活参数约 95B、总参数约 2T，那 Capybara 有多大？</p> <p>最直接的线索是推理速度。泄露信息暗示 Capybara 的 TPS 约为 Opus 的 40%，也就是大约 <strong>16–17 TPS</strong>。用同样的反推方法：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Capybara Active ≈ 3,800 / 16.5 ≈ 230B
</code></pre></div></div> <p>当然这个估计的不确定性更大——Capybara 可能用了不同的量化精度、不同的硬件集群、甚至不同的架构设计。但数量级上，<strong>激活参数 150–250B、总参数 4–6T</strong> 是一个合理的推测范围。</p> <p>这也解释了为什么 Anthropic 选择先给少数客户测试——这个规模的模型，推理成本不是一般的高。</p> <h3 id="不确定性">不确定性</h3> <p>需要强调的是，Capybara 的预测比 Opus 的估计多了一层不确定性。Opus 的数据至少能从两个平台交叉验证，而 Capybara 目前只有泄露信息中”速度约为 Opus 40%”这一个数据点，且 Anthropic 完全可能在架构上做了创新（比如更激进的量化、混合精度、或者全新的 attention 机制），使得参数-速度的对应关系发生变化。</p> <h2 id="结论">结论</h2> <p><img src="/assets/img/claude/claude-param-analysis.png" alt="Throughput Analysis"/> <em>左：激活参数与吞吐量的关系，红色虚线为 memory-bound 理论曲线；中：各模型的 Active × TPS 乘积验证；右：MoE 模型的 Total/Active Ratio 分布。</em></p> <p>通过收集 Google Vertex 平台上多款开源模型（包括 Dense 架构的 Llama 3.3 70B 和多款 MoE 模型）的推理速度，验证了 memory-bound 条件下”激活参数 × TPS ≈ 常数”的关系在大模型上成立。用这个关系反推 Claude 的参数量：<strong>Opus 4.6 的激活参数约 80–110B，总参数约 1.5–2.5T</strong>；<strong>Sonnet 4.6 略小，激活参数约 70–95B，总参数约 1.0–2.0T</strong>。两者都采用 FP8 量化的 MoE 架构。</p> <p>至于下一代的 Capybara，如果泄露的速度数据可信，其激活参数可能在 150–250B 区间，总参数有望突破 4T——这也直接解释了它为什么这么慢。当然，这些数字本身的误差不小（基准常数就有约 ±15% 的波动），对 Capybara 的推测更是多了一层假设，权当一个数量级的参考。</p> <p>说实话，作为一个每天都在用 Opus 4.6 写代码的人，我很难想象还能比现在强多少。但如果 Capybara 真的能兑现泄露信息里描述的那些能力，那 AI 辅助编程这件事可能又要翻一个台阶了。期待它正式开放的那天。</p>]]></content><author><name></name></author><category term="AI"/><category term="Analysis"/><category term="LLM"/><category term="MoE"/><category term="Claude"/><category term="Performance"/><summary type="html"><![CDATA[起因]]></summary></entry><entry><title type="html">TurboQuant：一篇论文砸崩存储股价？MNN在手机上跑通了</title><link href="https://zhaode.wang/blog/2026/turboquant/" rel="alternate" type="text/html" title="TurboQuant：一篇论文砸崩存储股价？MNN在手机上跑通了"/><published>2026-03-27T00:00:00+00:00</published><updated>2026-03-27T00:00:00+00:00</updated><id>https://zhaode.wang/blog/2026/turboquant</id><content type="html" xml:base="https://zhaode.wang/blog/2026/turboquant/"><![CDATA[<h2 id="一问题背景">一、问题背景</h2> <p>最近几天，Google 的一篇量化论文 TurboQuant（2025 年 4 月挂 arXiv，已被 ICLR 2026 接收）突然在社区火了。没有财报暴雷、没有供应链断裂，但美光和西部数据两家存储芯片巨头的股价应声大跌——一句”不需要那么多显存”的暗示足以让市场敏感（当然也有人猜测是被人拿来做空存储的由头）。很快就有人在 GitHub 上给 MNN 提了 <a href="https://github.com/alibaba/MNN/issues/4321">issue #4321</a>，希望我们能支持这个量化方法。MNN 一直聚焦端侧推理，手机内存寸土寸金，我们自己也急需优化 KV Cache 的内存占用。于是我抓紧读了论文，结合端侧的计算特点做了适配和优化实现。</p> <p>为什么这篇论文引起这么大关注？因为它切中了大模型推理的核心痛点——<strong>KV Cache 的内存占用</strong>。</p> <p>大语言模型推理时，每生成一个 token 都要和之前所有 token 的 Key/Value 向量做 Attention 计算。这些向量被缓存在 KV Cache 中，内存占用随上下文长度<strong>线性增长</strong>——Qwen3-4B 跑 8K 上下文，光 KV Cache 就要吃掉约 2GB。如果能把 fp16 的 KV Cache 压缩到 3~4 bit，内存立省 <strong>30%~60%</strong>。</p> <p>问题在于：<strong>怎么压得更小，又不把模型压坏？</strong></p> <p>TurboQuant 给出了一个数学上优雅、工程上简洁的方案：先用正交旋转把向量的统计分布变成”已知形状”，再用针对该分布预先算好的最优码本进行量化。不需要训练数据、不需要校准集、码本直接硬编码——整个量化过程<strong>纯在线计算，零数据依赖</strong>。</p> <p>本文从算法原理、MNN 工程实现、端侧实验三个维度，完整拆解这一方案。</p> <hr/> <h2 id="二论文解析">二、论文解析</h2> <h3 id="21-一张图看懂完整流程">2.1 一张图看懂完整流程</h3> <p>下图展示了 TurboQuant 对一个 32 维 block 的完整量化过程：</p> <p><img src="/assets/img/turboquant/tq_algorithm_flow.png" alt="TurboQuant 算法流程"/></p> <p>整个过程可以用三行伪代码概括：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>输入：d 维向量 x（一个 Key 或 Value 向量）

Step 1. norm = ||x||₂；x_hat = x / norm        ← 归一化到单位球面，单独存 norm
Step 2. y = Π · x_hat                           ← 用正交矩阵旋转
Step 3. indices[i] = codebook_search(y[i])       ← 逐坐标标量量化
</code></pre></div></div> <p>反量化就是反过来：查码本 → 逆旋转 → 乘回 norm。</p> <p>每一步都有清晰的目的：</p> <table> <thead> <tr> <th>步骤</th> <th>操作</th> <th>目的</th> </tr> </thead> <tbody> <tr> <td>①</td> <td><strong>L2 归一化</strong></td> <td>分离”大小”和”方向”——方向用码本量化，大小（L2 范数）用 fp16 精确存储</td> </tr> <tr> <td>②</td> <td><strong>正交旋转</strong></td> <td>让每个坐标的分布变成<strong>已知且固定</strong>的 Beta 分布</td> </tr> <tr> <td>③</td> <td><strong>码本量化</strong></td> <td>利用已知分布，用预计算的 Lloyd-Max 最优码本逐坐标量化</td> </tr> </tbody> </table> <p><strong>关键洞察：步骤②是核心。</strong> 旋转不仅”打散离群值”（QuaRot 也这么做），更关键的是让旋转后每个坐标的统计分布变成<strong>精确已知</strong>的形状——这使得我们可以预先计算信息论意义上的最优量化码本，而不是使用次优的均匀量化。</p> <h3 id="22-为什么旋转能让分布变已知">2.2 为什么旋转能让分布变已知</h3> <p>这是整个算法最关键的洞察。</p> <p><strong>地球仪类比</strong>：你在地球仪上标了一个点（原始向量归一化后的方向），然后随机转动地球仪（乘正交矩阵）。不管你一开始标在哪里，转完之后这个点在球面上的位置是<strong>完全随机</strong>的——等价于在球面上均匀选一个点。</p> <p>数学上说：归一化后的向量在<strong>单位球面</strong>上，乘以随机正交矩阵相当于在球面上做均匀随机旋转。旋转后，每个坐标的分布由<strong>纯几何</strong>决定：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>第 j 个坐标 y_j 的概率密度：f(t) ∝ (1 - t²)^((d-3)/2)，  t ∈ [-1, 1]
</code></pre></div></div> <p>这就是 <strong>Beta 分布</strong>，形状只取决于维度 d，跟输入向量完全无关。</p> <p><strong>直觉理解</strong>：在 d 维单位球面上，满足”第 j 个坐标 = t”的点构成一个 (d-2) 维的低维球面，其半径为 √(1-t²)，”面积”正比于 (1-t²)^((d-2)/2)。t 越接近 0，低维球面越大，落在这个区间的概率越高；t 接近 ±1 时，低维球面缩成一个点，概率趋向于零。这就是 Beta 分布的几何来源。</p> <p>下图展示了不同维度下 Beta 分布的形状，以及在此分布上设计的 Lloyd-Max 最优码本：</p> <p><img src="/assets/img/turboquant/tq_beta_codebook.png" alt="Beta 分布与码本"/></p> <p><strong>高维效应</strong>：维度越高，分布越集中在 0 附近。当 d=128（典型的 head_dim）时，每个坐标的标准差仅 ≈ 1/√128 ≈ 0.088，Beta 分布几乎退化为高斯分布 N(0, 1/d)。</p> <p><strong>关键推论</strong>：</p> <ul> <li><strong>维度 d 确定 → Beta 分布确定 → 最优码本确定 → 预计算一次，永久使用</strong></li> <li>高维球面上各坐标<strong>近似独立</strong>，因此 d 维向量量化问题可以拆成 d 个独立的一维标量量化——VQ 退化为 SQ，实现极其简单</li> </ul> <h3 id="23-lloyd-max-最优码本一维-k-means">2.3 Lloyd-Max 最优码本：一维 k-means</h3> <p>既然知道了每个坐标的精确分布，就可以用经典的 <strong>Lloyd-Max 算法</strong> 计算信息论意义上的最优码本。</p> <p>Lloyd-Max 本质上是<strong>一维连续 k-means</strong>：</p> <ul> <li>把值域分成 2^b 个区间（b 是 bit 数）</li> <li>每个区间的”代表值”（centroid）是该区间内的概率加权中心</li> <li>区间边界是相邻代表值的中点</li> <li>迭代直到收敛</li> </ul> <p>从上面的码本图可以看到一个重要特征：<strong>中心密集，边缘稀疏</strong>。这正是 Lloyd-Max 相对于均匀量化的优势——把更多的码本资源分配给概率密度高的区域。</p> <p>因为分布的公式已知，可以用数值积分精确计算，<strong>不需要训练数据、不需要校准集</strong>。码本可以直接硬编码：</p> <table> <thead> <tr> <th>bit 数</th> <th>码本大小</th> <th>代表值（对称）</th> </tr> </thead> <tbody> <tr> <td>3-bit</td> <td>8 个</td> <td>±0.245, ±0.756, ±1.344, ±2.152</td> </tr> <tr> <td>4-bit</td> <td>16 个</td> <td>±0.128, ±0.388, ±0.657, ±0.942, ±1.256, ±1.618, ±2.069, ±2.733</td> </tr> </tbody> </table> <p>论文证明了 MSE 的理论上界：<strong>MSE ≤ 2.7 / 4^b</strong>，信息论下界是 1/4^b，仅差 2.7 倍常数——<strong>接近理论最优</strong>。</p> <h3 id="24-有偏内积与-qjl-修正">2.4 有偏内积与 QJL 修正</h3> <p>在 Attention 中，我们真正关心的不是向量重建误差（MSE），而是 <strong>Q·K 内积</strong>的准确性。MSE 最优的量化器在估计内积时是<strong>有偏</strong>的——内积会被系统性地缩小：</p> <p><img src="/assets/img/turboquant/tq_bias.png" alt="内积偏差"/></p> <p>可以看到，<strong>3-bit 时偏差仅 4%，4-bit 时几乎无偏（1%）</strong>。对于实际使用的 3~4 bit 量化，这个偏差足够小，不需要额外修正。</p> <p>对于极低 bit（1~2 bit），论文提出了<strong>两阶段方法</strong>：用 (b-1) bit 做 MSE 量化，剩余 1 bit 用 <strong>QJL</strong>（Quantized Johnson-Lindenstrauss）做无偏内积修正。QJL 是一种基于随机投影取符号的 1-bit 方案——不追求重建精度，但<strong>保证内积估计的期望无偏</strong>。</p> <blockquote> <p><strong>工程启示</strong>：3-bit 和 4-bit 场景下，偏差足够小，<strong>不需要 QJL 第二阶段</strong>。MNN 的实现正是采用了这一策略——全部 bit 预算给 MSE，省掉 QJL 的存储和计算开销。</p> </blockquote> <hr/> <h2 id="三mnn-工程实现">三、MNN 工程实现</h2> <p>MNN 的实现代码在这个 <a href="https://github.com/alibaba/MNN/commit/244f5d10df5a95b4f4e6f3d9251c6fe3dc0e7c83">commit</a> 中，包含 TQ3（3-bit）和 TQ4（4-bit）两种 KV Cache 量化模式，核心代码在 <code class="language-plaintext highlighter-rouge">TurboQuant.hpp</code>（约 780 行）。</p> <p>这个实现是在 Claude 的协助下完成的。一开始我问 Claude 这个方案对 MNN 是否有价值，它直接从论文判断——认为随机正交矩阵和 QJL 会显著拖慢推理速度，在端侧不太划算。在我的建议下，我们做了两个关键调整：用 Hadamard 变换替代随机矩阵，以及只实现 3/4-bit 从而完全避开 QJL。最终实测性能影响很小，decode 速度甚至还有提升。</p> <p>下面分析几个关键的工程决策。</p> <h3 id="31-用-hadamard-替代随机正交矩阵">3.1 用 Hadamard 替代随机正交矩阵</h3> <p>论文用随机正交矩阵 Π 进行旋转，需要 O(d²) 存储和 O(d²) 乘加运算，工程上成本太高。</p> <p>MNN 借鉴 <strong>QuaRot</strong> 的思路，采用 <strong>Walsh-Hadamard 变换（WHT）</strong> 替代。为什么 Hadamard 矩阵可以替代随机正交矩阵？因为 Hadamard 矩阵本身就是<strong>只有 ±1 元素的正交方阵</strong>（归一化后行列两两正交），满足 TurboQuant 所需的正交性条件。配合<strong>符号翻转</strong>（用黄金比例哈希生成的确定性 ±1 序列逐坐标翻转），可以引入足够的伪随机性来打散输入结构。</p> <p>这一替代对端侧计算极其友好：</p> <table> <thead> <tr> <th>对比项</th> <th>随机正交矩阵</th> <th>Hadamard + 符号翻转</th> </tr> </thead> <tbody> <tr> <td>存储</td> <td>d×d 浮点数</td> <td><strong>零存储</strong>（原地计算）</td> </tr> <tr> <td>计算</td> <td>O(d²) 乘加</td> <td><strong>O(d log d)</strong> 纯加减法</td> </tr> <tr> <td>元素值</td> <td>任意浮点数</td> <td><strong>只有 ±1</strong>（乘法变加减法）</td> </tr> <tr> <td>实现</td> <td>矩阵乘法</td> <td>蝶形运算（类似 FFT）</td> </tr> </tbody> </table> <p>WHT 的蝶形运算结构如下图所示（以 8 点为例，实际实现为 32 点 = 5 级）：</p> <p><img src="/assets/img/turboquant/tq_wht_butterfly.png" alt="WHT 蝶形运算"/></p> <p><strong>block_size = 32 的选择</strong>：不对整个 head_dim（如 128）做 WHT，而是分成 32 维的小块独立处理。每个 block 有自己的 norm（L2 范数），能部分补偿小块维度不够高带来的近似误差。32 维的 WHT 只需 5 级蝶形（log₂32 = 5），计算极快且内存友好。</p> <h3 id="32-只做第一阶段mse-量化">3.2 只做第一阶段（MSE 量化）</h3> <p>论文的完整方案是两阶段（MSE + QJL），但 MNN 只实现了第一阶段：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>论文：  (b-1)-bit MSE + 1-bit QJL = b-bit 无偏
MNN：   b-bit MSE                  = b-bit 有偏但精度更高
</code></pre></div></div> <p>理由：3-bit 偏差仅 4%、4-bit 仅 1%，实际可忽略；省掉 QJL 的随机投影矩阵存储和额外计算；同样 b-bit 预算全部给 MSE，重建精度更高。</p> <h3 id="33-内存布局与-attention-融合优化">3.3 内存布局与 Attention 融合优化</h3> <h4 id="紧凑的-bit-压缩存储">紧凑的 bit 压缩存储</h4> <p>每个 32 维 block 的存储格式：</p> <table> <thead> <tr> <th>方案</th> <th>格式</th> <th>总字节</th> <th>等效 bits/value</th> </tr> </thead> <tbody> <tr> <td>TQ3</td> <td>[2B fp16 norm] + [12B packed 3-bit×32]</td> <td>14 字节</td> <td>3.5</td> </tr> <tr> <td>TQ4</td> <td>[2B fp16 norm] + [16B packed 4-bit×32]</td> <td>18 字节</td> <td>4.5</td> </tr> <tr> <td>fp16</td> <td>[64B fp16×32]</td> <td>64 字节</td> <td>16</td> </tr> </tbody> </table> <p>3-bit 索引的 pack 方式是把 8 个 3-bit 值塞进 3 个字节（24 bit），通过位移和掩码操作实现紧凑存储。4-bit 则直接两个索引共用一个字节。</p> <p>压缩比：TQ3 ≈ <strong>4.6x</strong>，TQ4 ≈ <strong>3.6x</strong>。</p> <h4 id="key-路径旋转域点积跳过逆-wht">Key 路径：旋转域点积（跳过逆 WHT）</h4> <p>计算 Q·K 时，利用正交矩阵的性质 <code class="language-plaintext highlighter-rouge">⟨Q, Π^T·y⟩ = ⟨Π·Q, y⟩</code>，将 Q 预先旋转到 WHT 域，然后直接和量化后的 K 在旋转域做点积。<strong>K 不需要逆 WHT 变换</strong>——每个 KV token 只需 unpack 索引、查码本、做乘加。Q 的 WHT 变换在每个 head 计算开始时做一次，对该 head 所有 KV token 复用。</p> <h4 id="value-路径旋转域累加n-次逆-wht--1-次">Value 路径：旋转域累加（N 次逆 WHT → 1 次）</h4> <p>计算 Softmax(QK)·V 时，先在旋转域累加所有 V 的加权和（每个 V 只需查码本乘以 attention weight），最后只做<strong>一次</strong>逆 WHT 变换。从 N 次逆 WHT（N = KV 序列长度，可达数千）减少到 1 次，性能收益显著。</p> <h4 id="量化写入路径">量化写入路径</h4> <p>KV Cache 在每次 Attention 写入时就地量化——从 fp16/fp32 源读取 32 个值到 float buffer，计算 L2 范数并归一化，执行 WHT 正变换，码本搜索得到索引，pack 为紧凑格式写入 cache。不需要额外的 fp16 缓冲区。</p> <h3 id="34-neon-simd-加速">3.4 NEON SIMD 加速</h3> <p>在 aarch64 平台上，MNN 对三个热点路径做了 NEON 向量化优化：</p> <ul> <li><strong>码本查找</strong>：利用 <code class="language-plaintext highlighter-rouge">vqtbl1q_u8</code>（向量表查找指令）一次性将 8 个 3-bit 索引转换为 8 个 fp16 码本值，避免逐个标量查表</li> <li><strong>3-bit 解包</strong>：利用 <code class="language-plaintext highlighter-rouge">vshlq_u32</code>（变量移位）配合掩码，一条 SIMD 指令提取 4 个 3-bit 索引</li> <li><strong>码本搜索</strong>：利用 <code class="language-plaintext highlighter-rouge">vcgtq_f32</code>（向量比较）做 7 次边界比较，通过累加比较结果得到索引，4 个值并行处理</li> </ul> <p>实测 NEON 优化对 prefill 提升有限（+1~5%），因为 prefill 瓶颈在上游的 GEMM 计算而非量化本身。</p> <hr/> <h2 id="四实验结果与分析">四、实验结果与分析</h2> <p>在 macOS ARM64（Apple Silicon）上测试了两个模型：Qwen3-4B（2.34 GiB）和 Qwen3-0.6B（355 MiB），使用 FlashAttention + ChunkPrefill（chunk=256），4 线程。</p> <h3 id="41-精度pplperplexity">4.1 精度：PPL（Perplexity）</h3> <p><img src="/assets/img/turboquant/tq_ppl.png" alt="PPL 对比"/></p> <table> <thead> <tr> <th>模型</th> <th style="text-align: center">Baseline (fp16)</th> <th style="text-align: center">INT8</th> <th style="text-align: center">TQ4 (4-bit)</th> <th style="text-align: center">TQ3 (3-bit)</th> </tr> </thead> <tbody> <tr> <td><strong>Qwen3-4B</strong></td> <td style="text-align: center">13.67</td> <td style="text-align: center">13.46 (-1.5%)</td> <td style="text-align: center"><strong>16.45 (+20%)</strong></td> <td style="text-align: center">25.49 (+87%)</td> </tr> <tr> <td><strong>Qwen3-0.6B</strong></td> <td style="text-align: center">33.02</td> <td style="text-align: center">32.49 (-1.6%)</td> <td style="text-align: center">243.81 (+638%)</td> <td style="text-align: center">6253.83 (崩坏)</td> </tr> </tbody> </table> <ul> <li><strong>4B 模型上 TQ4 PPL 增长 20%</strong>，考虑到从 16bit 压缩到 4.5bit（压缩 3.6x），这是合理的精度-压缩权衡</li> <li><strong>4B 模型上 TQ3 PPL 增长 87%</strong>，压缩更极致（4.6x）但精度损失显著增大</li> <li><strong>0.6B 模型上 TQ3/TQ4 均严重崩坏</strong>——原因见后面分析</li> </ul> <h3 id="42-速度与内存">4.2 速度与内存</h3> <p><img src="/assets/img/turboquant/tq_memory_speed.png" alt="内存与速度"/></p> <p><strong>Qwen3-4B 详细数据：</strong></p> <table> <thead> <tr> <th>上下文</th> <th>指标</th> <th style="text-align: center">Baseline</th> <th style="text-align: center">INT8</th> <th style="text-align: center">TQ4</th> <th style="text-align: center">TQ3</th> </tr> </thead> <tbody> <tr> <td><strong>4K</strong></td> <td>内存 (MB)</td> <td style="text-align: center">4,731</td> <td style="text-align: center">4,728</td> <td style="text-align: center">3,674 (<strong>-22%</strong>)</td> <td style="text-align: center">3,617 (<strong>-24%</strong>)</td> </tr> <tr> <td><strong>4K</strong></td> <td>Prefill (tok/s)</td> <td style="text-align: center">92.55</td> <td style="text-align: center">99.34</td> <td style="text-align: center">31.43</td> <td style="text-align: center">27.83</td> </tr> <tr> <td><strong>4K</strong></td> <td>Decode (tok/s)</td> <td style="text-align: center">40.08</td> <td style="text-align: center">40.76</td> <td style="text-align: center">~40</td> <td style="text-align: center">~40</td> </tr> <tr> <td><strong>8K</strong></td> <td>内存 (MB)</td> <td style="text-align: center">6,172</td> <td style="text-align: center">6,008</td> <td style="text-align: center">4,131 (<strong>-33%</strong>)</td> <td style="text-align: center">3,948 (<strong>-36%</strong>)</td> </tr> <tr> <td><strong>8K</strong></td> <td>Prefill (tok/s)</td> <td style="text-align: center">70.86</td> <td style="text-align: center">81.65</td> <td style="text-align: center">20.01</td> <td style="text-align: center">16.44</td> </tr> <tr> <td><strong>8K</strong></td> <td>Decode (tok/s)</td> <td style="text-align: center">~40</td> <td style="text-align: center">40.35</td> <td style="text-align: center">~40</td> <td style="text-align: center">~40</td> </tr> </tbody> </table> <p>核心结论：</p> <ul> <li><strong>内存节省随上下文长度增大而增大</strong>：4K 省 22~24%，8K 省 33~36%，上下文越长优势越大</li> <li><strong>Decode 速度完全无损</strong>：独立测试 Baseline 38.19、TQ4 40.72、TQ3 39.61 tok/s，量化后反而略快（KV Cache 更小 → 访存更少）</li> <li><strong>Prefill 约慢 3~4x</strong>：瓶颈在量化计算（WHT + 码本搜索），是在线量化的固有开销</li> </ul> <h3 id="43-输出质量">4.3 输出质量</h3> <p>问题：”中国的首都是哪里？”（greedy decoding, max_new_tokens=100）</p> <table> <thead> <tr> <th>模型</th> <th>Config</th> <th>输出</th> </tr> </thead> <tbody> <tr> <td><strong>4B</strong></td> <td>Baseline</td> <td>中国的首都是北京。</td> </tr> <tr> <td><strong>4B</strong></td> <td>TQ4</td> <td>中国的首都是北京。 ✅</td> </tr> <tr> <td><strong>4B</strong></td> <td>TQ3</td> <td>中国的首都是北京。北京位于中国北部……（语义正确但略发散）</td> </tr> <tr> <td><strong>0.6B</strong></td> <td>TQ4</td> <td>（输出全是换行符，无有效内容）❌</td> </tr> <tr> <td><strong>0.6B</strong></td> <td>TQ3</td> <td>“中国 中国 中国”……（重复乱码）❌</td> </tr> </tbody> </table> <h3 id="44-小模型为什么崩了">4.4 小模型为什么崩了？</h3> <p>TurboQuant 的数学保证在 0.6B 上仍然成立，但论文隐含了一个前提：<strong>模型有足够冗余度来消化量化噪声</strong>。</p> <p>通过实测 KV Cache 数值分布发现了根因：</p> <p><img src="/assets/img/turboquant/tq_kv_distribution.png" alt="KV Cache 数值分布"/></p> <table> <thead> <tr> <th>指标</th> <th>Qwen3-4B</th> <th>Qwen3-0.6B</th> <th>差距</th> </tr> </thead> <tbody> <tr> <td>方差</td> <td>10.69</td> <td>36.87</td> <td><strong>3.4x</strong></td> </tr> <tr> <td>绝对最大值</td> <td>7.42</td> <td>14.70</td> <td><strong>2.0x</strong></td> </tr> <tr> <td>峰度</td> <td>3.98</td> <td>5.95</td> <td><strong>1.5x</strong></td> </tr> <tr> <td>离群率 (&gt;3σ)</td> <td>1.52%</td> <td>2.01%</td> <td>1.3x</td> </tr> </tbody> </table> <p>小模型崩坏的三重原因：</p> <ol> <li><strong>绝对误差放大</strong>：0.6B 的 KV Cache 方差是 4B 的 3.4x，即使 MSE/Var 比值不变，绝对 MSE 也大 3.4 倍</li> <li><strong>纠错能力不足</strong>：0.6B head 更少、layer 更少，每个 KV 值对输出的贡献权重更大，误差无法通过多 head/layer 平均消除</li> <li><strong>误差累积</strong>：自回归生成中量化误差通过 attention 传播到后续 token，小模型更难修正</li> </ol> <h3 id="45-06b-的内存节省">4.5 0.6B 的内存节省</h3> <p><img src="/assets/img/turboquant/tq_06b_memory.png" alt="0.6B 内存"/></p> <p>虽然 0.6B 上精度崩坏，但内存压缩本身是有效的——TQ3 在 8K 上下文节省 <strong>62%</strong> 内存（3343→1278 MB），比 4B 的 36% 节省更多，因为小模型中 KV Cache 占总内存比例更高。</p> <p>有趣的是，0.6B TQ3 的 decode 速度反而超过 baseline（217.64 vs 170.99 tok/s），因为 KV Cache 压缩后访存带宽需求大幅降低。</p> <h3 id="46-各方案综合对比">4.6 各方案综合对比</h3> <p>综合精度、内存、速度三个维度：</p> <ul> <li><strong>Baseline</strong>：精度和 prefill 最优，但零压缩</li> <li><strong>INT8</strong>：全面均衡（精度无损 + prefill 更快），但压缩率有限（仅 2x）</li> <li><strong>TQ4</strong>：在内存节省和 decode 速度上明显优于 INT8，代价是精度下降 20% 和 prefill 变慢 3x</li> <li><strong>TQ3</strong>：比 TQ4 多省约 3% 内存，但 PPL 从 +20% 跳到 +87%，性价比不高</li> </ul> <h3 id="47-实验结论">4.7 实验结论</h3> <table> <thead> <tr> <th>结论</th> <th>详细说明</th> </tr> </thead> <tbody> <tr> <td><strong>TQ4 是 4B+ 模型的推荐方案</strong></td> <td>PPL +20%，内存 -33%（8K），decode 无损，输出质量良好</td> </tr> <tr> <td><strong>TQ3 更极致但需谨慎</strong></td> <td>内存多省 ~3%，但 PPL +87%，需评估具体任务可接受性</td> </tr> <tr> <td><strong>小模型 (&lt;1B) 不适用</strong></td> <td>0.6B 上 TQ4 PPL +638%，TQ3 完全崩坏，建议保持 fp16</td> </tr> <tr> <td><strong>Decode 是”免费”的</strong></td> <td>量化后 KV Cache 更小，decode 访存更快，速度甚至略有提升</td> </tr> <tr> <td><strong>Prefill 是代价</strong></td> <td>WHT + 码本搜索导致 prefill 慢 ~3x，是在线量化的固有开销</td> </tr> <tr> <td><strong>上下文越长越划算</strong></td> <td>4K 省 22%，8K 省 33%，长上下文场景收益更大</td> </tr> <tr> <td><strong>INT8 仍是保守首选</strong></td> <td>几乎无损 + prefill 更快，适合对精度要求高的场景</td> </tr> </tbody> </table> <p><strong>使用建议：</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>模型规模：   ≥ 4B → TQ4（推荐）    1-4B → 谨慎评估    &lt; 1B → fp16
场景选择：   长上下文（≥8K）→ TQ4 收益最大    短对话 → INT8 或 fp16
</code></pre></div></div> <hr/> <h2 id="五总结">五、总结</h2> <p>TurboQuant 确实是一个有效的 KV Cache 量化方案——理论优雅、实现简洁、不依赖校准数据，在 4B 及以上模型上取得了不错的内存-精度平衡。它和 QuaRot 等基于旋转的量化方法有一定的相似性（都利用正交变换打散离群值），但 TurboQuant 更进一步：利用旋转后分布的可知性来设计信息论最优码本。</p> <p>但客观来说，它并非银弹。AI 对存储和算力的需求依然庞大且在持续增长——模型在变大、上下文在变长、多模态在普及。TurboQuant 在 KV Cache 这个局部点上提供了一个有效的压缩手段，但和其他量化方法相比，并不构成突破性的代差。真正的存储瓶颈缓解，仍然需要从模型架构、量化算法、硬件协同等多个层面共同推进。</p> <hr/> <h2 id="参考">参考</h2> <ul> <li>论文：<a href="https://arxiv.org/pdf/2504.19874">TurboQuant: Online Vector Quantization for KV Cache Quantization</a></li> <li>MNN 实现：<a href="https://github.com/alibaba/MNN/commit/244f5d10df5a95b4f4e6f3d9251c6fe3dc0e7c83">commit 244f5d1</a></li> </ul>]]></content><author><name></name></author><category term="llm"/><category term="mnn"/><summary type="html"><![CDATA[一、问题背景]]></summary></entry><entry><title type="html">Google Stitch 体验：解决 CC 写 UI 的痛</title><link href="https://zhaode.wang/blog/2026/stitch-claude-code/" rel="alternate" type="text/html" title="Google Stitch 体验：解决 CC 写 UI 的痛"/><published>2026-03-23T00:00:00+00:00</published><updated>2026-03-23T00:00:00+00:00</updated><id>https://zhaode.wang/blog/2026/stitch-claude-code</id><content type="html" xml:base="https://zhaode.wang/blog/2026/stitch-claude-code/"><![CDATA[<h2 id="痛点claude-code-写功能一流ui-设计一言难尽">痛点：Claude Code 写功能一流，UI 设计一言难尽</h2> <p>用 Claude Code 写带 UI 的桌面应用已经是我的日常工作流了。在功能实现上，Claude Code 几乎无可挑剔——给出需求，它能快速生成完整的业务逻辑、API 对接和状态管理代码。</p> <p>但 UI 设计是它的短板。</p> <p>Claude Code 直接生成的界面往往是”能用但不好看”的状态：布局单调、配色平淡、缺乏视觉层次。你可以不断描述”我要扁平化风格”、”加点科技感”、”间距再大一点”，但这种纯文字沟通的效率极低——你脑海中的画面和 AI 理解的画面之间总有一道鸿沟。</p> <p>正好最近想给 MNN 的命令行工具套一层 UI 做成桌面应用，拿这个项目（<strong>omnn</strong>）来试试。下面是 Claude Code 直接生成的 UI：</p> <p><img src="/assets/images/stitch/cc_before.png" alt="Claude Code 直出的 UI —— 功能完整但设计平淡"/> <em>Claude Code 直出的 UI：Status、Models、Chat、Benchmark、Settings、Logs 六个页面。功能齐全，但视觉上就是一个标准的”程序员审美”界面。</em></p> <p>能用吗？完全能用。好看吗？差点意思。</p> <h2 id="解法让专业的工具做专业的事">解法：让专业的工具做专业的事</h2> <p>关键转变在于<strong>分工</strong>：把 UI 设计从 Claude Code 的职责中剥离出来，交给专门的设计工具。</p> <p>3 月 18 日，Google 宣布旗下 AI 设计工具 <strong>Stitch</strong> 支持 Vibe Design——用自然语言描述需求就能生成完整的 UI 设计稿。虽然 Stitch 与 Google 自家的 AI 编程工具配合更紧密，但我日常主力是 Claude Code，所以决定试试跨家搭配。工作流非常简单：</p> <h3 id="第一步让-claude-code-写设计提示词">第一步：让 Claude Code 写设计提示词</h3> <p>不要让 Claude Code 直接画 UI，而是让它发挥所长——<strong>描述功能</strong>。我给 Claude Code 的指令是：</p> <blockquote> <p>我要使用设计工具为这个 app 设计 UI，请根据现有的功能帮我写一下 UI 的提示词；你只介绍功能，不要介绍设计细节，颜色等；设计交给设计工具负责；另外加一个设计风格的引导：扁平化设计，风格简洁有科技感</p> </blockquote> <p>Claude Code 很快输出了一份结构清晰的设计文档：包含 6 个页面的功能描述、交互逻辑、以及”Flat design, clean and minimal with a tech-forward aesthetic”的风格引导。<strong>注意：这里只描述功能和风格方向，不规定具体的颜色、字号、间距——这些交给设计工具决策。</strong></p> <h3 id="第二步把提示词喂给-stitch">第二步：把提示词喂给 Stitch</h3> <p>将 Claude Code 生成的提示词粘贴到 Google Stitch，它会自动生成完整的设计稿和对应的前端代码。</p> <p>Stitch 的能力不止于生成初稿。下面是 Stitch 的工作界面：</p> <p><img src="/assets/images/stitch/stitch_ui.png" alt="Stitch 工作界面"/> <em>Stitch 的可视化编辑界面：左侧是所有页面的设计预览，底部可以输入修改指令，右侧可以设置主题颜色等全局样式。</em></p> <p>这才是 Stitch 真正解决痛点的地方——<strong>可视化的细节微调</strong>。用 Claude Code 时，你想把某个按钮的颜色从蓝色改成紫色，需要描述”把 Status 页面右上角的按钮颜色改成紫色”，然后祈祷 AI 改对了地方。而在 Stitch 中，你可以：</p> <ul> <li><strong>圈选修改</strong>：直接在设计图上框选某个区域，针对性地提出修改建议，比如”这个卡片的圆角再大一点”</li> <li><strong>主题颜色设置</strong>：一键调整全局主题色、背景色、强调色，所有页面同步更新</li> <li><strong>字体微调</strong>：调整字体族、字号、字重，实时预览效果</li> <li><strong>颜色精调</strong>：针对单个元素修改颜色，不需要用文字描述位置</li> </ul> <p>这种”看到哪里不满意就改哪里”的交互方式，比用文字反复描述 UI 修改高效太多了。对于我们这些非设计专业的开发者来说，很多时候是”看到了才知道要改什么”，Stitch 的可视化编辑恰好满足了这个需求。</p> <p>生成的效果如下：</p> <p><img src="/assets/images/stitch/stitch_after.png" alt="Stitch 生成的设计稿"/> <em>Stitch 生成的设计：同样的六个页面，但视觉层次分明、配色统一、布局专业。深色主题搭配紫色强调色，信息密度高但不杂乱。</em></p> <p>对比一目了然——Stitch 的设计有明确的视觉层级、合理的信息密度、统一的色彩体系，以及恰到好处的留白。</p> <h3 id="第三步让-claude-code-按设计图编码">第三步：让 Claude Code 按设计图编码</h3> <p>把 Stitch 生成的设计图和 HTML 代码下载到项目目录，然后告诉 Claude Code：</p> <blockquote> <p>根据你的提示词，Google Stitch 已经完成了设计，设计图和网页都放在了 stitch 目录下，请根据这个设计修改 app</p> </blockquote> <p>Claude Code 拿到视觉参考后，编码工作就回到了它的舒适区。它会参照设计稿中的配色、布局、组件样式来重新实现界面，最终输出的 app 在视觉上和设计稿高度一致。</p> <h2 id="为什么这个工作流有效">为什么这个工作流有效</h2> <p>这个组合之所以好用，核心在于<strong>各取所长</strong>：</p> <table> <thead> <tr> <th>环节</th> <th>工具</th> <th>原因</th> </tr> </thead> <tbody> <tr> <td>功能描述</td> <td>Claude Code</td> <td>它最懂代码和功能逻辑</td> </tr> <tr> <td>视觉设计</td> <td>Stitch</td> <td>专业设计工具，所见即所得</td> </tr> <tr> <td>编码实现</td> <td>Claude Code</td> <td>有设计稿做参考，实现精准</td> </tr> </tbody> </table> <p>传统方式是让 Claude Code 一人包办”设计 + 编码”，结果就是两头都不够精。现在加入 Stitch 做中间层，等于给 AI 编程流程补上了”设计师”这个角色。</p> <h2 id="总结">总结</h2> <p>如果你也在用 Claude Code 或其他 AI 编程工具写带 UI 的应用，遇到”功能没问题但界面不好看”的困境，试试这个三步工作流：</p> <ol> <li><strong>Claude Code 写功能描述</strong> —— 发挥 AI 的语言和逻辑能力</li> <li><strong>Stitch 做视觉设计</strong> —— 利用专业设计工具的视觉能力，支持圈选修改、字体颜色微调</li> <li><strong>Claude Code 按设计编码</strong> —— 有了设计稿，编码实现就是它的主场</li> </ol> <p>工具之间的分工协作，往往比让单一工具硬扛所有任务更高效。在 AI 辅助开发的时代，知道什么时候该用什么工具，本身就是一种重要的能力。</p>]]></content><author><name></name></author><category term="tool"/><summary type="html"><![CDATA[痛点：Claude Code 写功能一流，UI 设计一言难尽]]></summary></entry><entry><title type="html">MiMo-V2-Pro 测评：同一模型，两种结局</title><link href="https://zhaode.wang/blog/2026/mimov2pro/" rel="alternate" type="text/html" title="MiMo-V2-Pro 测评：同一模型，两种结局"/><published>2026-03-21T00:00:00+00:00</published><updated>2026-03-21T00:00:00+00:00</updated><id>https://zhaode.wang/blog/2026/mimov2pro</id><content type="html" xml:base="https://zhaode.wang/blog/2026/mimov2pro/"><![CDATA[<h2 id="起因">起因</h2> <p>上一篇 MiniMax-M2.7 的评测发出后，很多读者留言希望我测试 MiMo-V2-Pro。作为当前国产推理模型的代表之一，MiMo-V2-Pro 在各大榜单上表现亮眼，自然是大家最关注的选手之一。</p> <p>任务和上次完全一致：在 MNN 推理引擎中从零添加 LFM2 模型支持。这是一个 Tier 6 级别的任务——需要跨语言（Python 导出 + C++ 推理）、理解新的模型架构（ShortConv 短卷积门控线性注意力）、实现自定义算子，端到端跑通模型导出和推理。</p> <p>先说结论：MiMo-V2-Pro 在 Claude Code 环境下 <strong>28 分钟完成实现，提醒一次后自主修复通过</strong>，在这个任务上接近 Claude Opus 4.6，<strong>代表了当前国产模型在复杂工程任务上的领先水平</strong>。</p> <blockquote> <p>声明：我先后使用了 opencode 和 Claude Code 两个工具进行测试。opencode 使用的是免费额度，速度较慢；Claude Code 是自费充值 token，速度快且效果稳定。我对 opencode 使用经验有限，首测结果可能不完全反映 MiMo-V2-Pro 在该工具上的最佳表现，这里仅做记录对比。</p> </blockquote> <h2 id="测试结果">测试结果</h2> <p>MiMo-V2-Pro 经历了两次测试，结果截然不同。</p> <h3 id="首测opencode免费额度">首测：opencode（免费额度）</h3> <table> <thead> <tr> <th>指标</th> <th>数据</th> </tr> </thead> <tbody> <tr> <td>工具</td> <td>opencode</td> </tr> <tr> <td>耗时</td> <td>约 11 小时</td> </tr> <tr> <td>Token</td> <td>755K input + 141K output + 58M cache read</td> </tr> <tr> <td>花费</td> <td>¥0（免费额度）</td> </tr> <tr> <td>代码变更</td> <td>8 文件，+1618 / -1024</td> </tr> <tr> <td><strong>结果</strong></td> <td><strong>导出失败</strong></td> </tr> </tbody> </table> <p>opencode 环境下的 MiMo-V2-Pro 产出了大量代码，但质量堪忧：<code class="language-plaintext highlighter-rouge">model_mapper.py</code> 多了 1329 行（大部分是格式化噪音），<code class="language-plaintext highlighter-rouge">transformers.py</code> 多了 675 行，还动了 <code class="language-plaintext highlighter-rouge">model.py</code> 265 行。代码量大但有效改动少，最终连 ONNX 导出都没跑通。</p> <p>如果只看这个结果，MiMo-V2-Pro 的表现甚至不如 M2.7（后者至少导出成功了）。但我总觉得这不像是模型本身的能力问题——代码里的架构理解是对的（选择复用 LinearAttention、卷积方向正确），只是执行质量差。于是我决定换个环境重测。</p> <h3 id="重测claude-code充值-token">重测：Claude Code（充值 token）</h3> <table> <thead> <tr> <th>指标</th> <th>数据</th> </tr> </thead> <tbody> <tr> <td>工具</td> <td>Claude Code</td> </tr> <tr> <td>耗时（API）</td> <td>28 分 40 秒</td> </tr> <tr> <td>耗时（Wall）</td> <td>38 分 44 秒</td> </tr> <tr> <td>Token</td> <td>11.1M input + 85.7K output + 9.6M cache read</td> </tr> <tr> <td>花费</td> <td>¥25.68（账户实际扣费）</td> </tr> <tr> <td>代码变更</td> <td>6 文件，+297 / -44</td> </tr> <tr> <td><strong>结果</strong></td> <td><strong>提醒一次后通过</strong></td> </tr> </tbody> </table> <p>第一次实现就能导出、能推理，但输出乱码。我只告诉它”测试输出是乱码”，<strong>没有做任何方向性引导</strong>（没有指出哪个文件有问题、没有提示 bug 类型）——它自己定位了问题并修复，第二次输出完全正确：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>我是一个人工智能助手。它通过自然语言处理技术来理解和回应用户的询问。
</code></pre></div></div> <p><strong>同一个模型，换了工具环境，从”11 小时导出失败”变成”28 分钟提醒一次通过”。</strong></p> <h2 id="代码分析">代码分析</h2> <p>以下分析基于重测（Claude Code）的代码，修改共 6 个文件、+297/-44 行。</p> <p><strong>架构决策正确</strong>：复用现有的 <code class="language-plaintext highlighter-rouge">FusedLinearAttention</code> 管线，通过 <code class="language-plaintext highlighter-rouge">attn_type == "short_conv"</code> 分支区分，没有创建新算子、没有动 schema。相比 M2.7 创建全新 ShortConv 算子的方案，MiMo 的路径更轻量，也更符合 MNN 的设计理念。</p> <p><strong>Python 导出完整</strong>：模型映射、ONNX 自定义算子、导出路径三个环节都正确处理，仅用 +46 行完成映射、+4 行改动 custom_op。</p> <p><strong>C++ 实现规范</strong>：卷积方向正确（cross-correlation，与 PyTorch <code class="language-plaintext highlighter-rouge">nn.Conv1d</code> 一致），多线程并行，状态管理完备。存在 batch &gt; 1 时的指针偏移小 bug，不影响标准 LLM 推理。</p> <h2 id="三模型对比">三模型对比</h2> <table> <thead> <tr> <th>指标</th> <th>Claude Opus 4.6</th> <th>MiMo-V2-Pro（重测）</th> <th>MiniMax-M2.7</th> </tr> </thead> <tbody> <tr> <td><strong>最终结果</strong></td> <td><strong>完全通过</strong></td> <td><strong>提醒一次后通过</strong></td> <td>导出成功，推理失败</td> </tr> <tr> <td>工具</td> <td>Claude Code</td> <td>Claude Code</td> <td>Claude Code</td> </tr> <tr> <td>耗时（API）</td> <td>14 分 30 秒</td> <td>28 分 40 秒</td> <td>53 分 31 秒</td> </tr> <tr> <td>Token 用量</td> <td>15.5K in + 40.1K out</td> <td>11.1M in + 85.7K out</td> <td>35.6M in + 107.8K out</td> </tr> <tr> <td>花费</td> <td>~¥45</td> <td>$62.44</td> <td>¥29（Coding Plan）</td> </tr> <tr> <td>代码变更</td> <td>+414 / -40（~7 文件）</td> <td>+297 / -44（6 文件）</td> <td>+451 / -14（9 文件）</td> </tr> <tr> <td>架构决策</td> <td>复用 LinearAttention</td> <td>复用 LinearAttention</td> <td>创建新 ShortConv 算子</td> </tr> <tr> <td>卷积方向</td> <td>✅ 正确</td> <td>✅ 正确</td> <td>❌ 内外重复</td> </tr> <tr> <td>ONNX 导出</td> <td>✅</td> <td>✅</td> <td>✅</td> </tr> <tr> <td>C++ 推理</td> <td>✅</td> <td>✅</td> <td>❌ 推理失败</td> </tr> <tr> <td>自主调试</td> <td>不需要（一次通过）</td> <td>✅ 仅告知乱码，自行定位修复</td> <td>未验证</td> </tr> </tbody> </table> <h3 id="关键差异分析">关键差异分析</h3> <p><strong>Opus 4.6 vs MiMo-V2-Pro</strong>：两者的架构决策一致（都复用 LinearAttention），代码质量接近。核心差距在于 Opus 一次通过不需要人工干预，而 MiMo 首次输出乱码需要提醒一次。这说明 Opus 在实现完成后会主动验证结果的正确性，而 MiMo 缺少这一步。此外，Opus 的 token 效率远高于 MiMo（15.5K vs 11.1M input）。</p> <p><strong>MiMo-V2-Pro vs M2.7</strong>：差距体现在架构决策上。M2.7 选择创建全新的 ShortConv 算子，修改了 FlatBuffers schema、新增了 ONNX 自定义算子和 op 注册——这条路更重、更容易出错。M2.7 的 Python 导出管线实际上更完整（处理了 <code class="language-plaintext highlighter-rouge">full_attn_idxs</code>、<code class="language-plaintext highlighter-rouge">layer_types</code> 等细节），但 C++ 卷积实现有 bug，最终推理失败。MiMo 选了更轻的路，反而走通了。</p> <h2 id="两次测试的代码对比">两次测试的代码对比</h2> <table> <thead> <tr> <th>维度</th> <th>opencode 首测</th> <th>Claude Code 重测</th> </tr> </thead> <tbody> <tr> <td>代码量</td> <td>+1618/-1024（8 文件）</td> <td>+297/-44（6 文件）</td> </tr> <tr> <td>格式噪音</td> <td>大量（model_mapper +1329 行）</td> <td>几乎没有</td> </tr> <tr> <td>架构决策</td> <td>正确（复用 LinearAttention）</td> <td>正确（复用 LinearAttention）</td> </tr> <tr> <td>ONNX 导出</td> <td>不完整</td> <td>完整</td> </tr> <tr> <td>C++ 卷积方向</td> <td>正确</td> <td>正确</td> </tr> <tr> <td>多线程</td> <td>无</td> <td>有</td> </tr> <tr> <td>导出成功</td> <td>❌</td> <td>✅</td> </tr> <tr> <td>推理成功</td> <td>❌</td> <td>✅（提醒一次）</td> </tr> </tbody> </table> <p>两次测试中 MiMo 的架构理解是一致的（都选择复用 LinearAttention，卷积方向都正确），说明模型本身的能力没有问题。差距主要体现在<strong>代码组织和执行效率</strong>上——opencode 环境下产生了大量低效的格式化改动，有效代码被淹没；Claude Code 环境下改动精准，每一行都有意义。</p> <h2 id="结论">结论</h2> <p>MiMo-V2-Pro 在 Claude Code 环境下的表现让人刮目相看：28 分钟完成实现，提醒一次后自主修复并通过——这是继 Claude Opus 4.6 之后，<strong>第二个在这个任务上成功跑通的模型</strong>。</p> <p>与 M2.7 相比，MiMo 的架构决策更轻量准确，代码更精简，最终走完了 M2.7 没走完的路。与 Opus 相比，MiMo 的差距主要在”最后一公里”的自主验证上——如果它能在输出结果后自己跑一遍验证，可能就不需要那次提醒了。</p> <p>同时，这次测试也再次验证了一个规律：<strong>工具环境对模型发挥的影响是决定性的。</strong> 同一个模型在 opencode 下 11 小时导出失败，在 Claude Code 下 28 分钟提醒一次通过。模型能力是基础，但工具链的质量决定了这个能力能发挥几成。当然，我对 opencode 使用经验有限，这个结论仅供参考。</p> <p>下一篇文章将加入 GPT-5.4、GLM-5、Kimi-K2.5、Qwen 等更多模型的测试数据，做一次完整的横向对比。敬请期待。</p>]]></content><author><name></name></author><category term="llm"/><summary type="html"><![CDATA[起因]]></summary></entry><entry><title type="html">MNN 任务实测：七个模型，三个梯队</title><link href="https://zhaode.wang/blog/2026/mnntask-eval/" rel="alternate" type="text/html" title="MNN 任务实测：七个模型，三个梯队"/><published>2026-03-21T00:00:00+00:00</published><updated>2026-03-21T00:00:00+00:00</updated><id>https://zhaode.wang/blog/2026/mnntask-eval</id><content type="html" xml:base="https://zhaode.wang/blog/2026/mnntask-eval/"><![CDATA[<h2 id="起因">起因</h2> <p>前两篇文章发出后，大家对这类真实工程任务的测评很感兴趣，评论区也提出了不少想看的模型。这里挑了几个（也是我用过的）做了一次补测和对比：GPT-5.4、GLM-5、Kimi-K2.5 和 Qwen3-Max，加上此前的 Claude Opus 4.6、MiniMax-M2.7 和 MiMo-V2-Pro，同一个任务现在有七个模型的实测数据。</p> <p>任务不变：在 MNN 推理引擎中从零添加 LFM2 模型支持（Tier 6 级别），需要跨语言实现 Python 导出 + C++ 推理，端到端跑通。</p> <blockquote> <p>声明：不同模型使用了不同的工具和配置（Claude Code / Codex），我对部分工具使用经验有限，测试结果仅供参考。此外，不同 API 提供方可能提供的是量化版本，这一点我无法判断，可能对结果有影响。</p> </blockquote> <h3 id="模型接入方案">模型接入方案</h3> <table> <thead> <tr> <th>模型</th> <th>接入方式</th> <th>工具</th> </tr> </thead> <tbody> <tr> <td>Claude Opus 4.6</td> <td>AWS Anthropic API（Bedrock），按 token 付费</td> <td>Claude Code</td> </tr> <tr> <td>GPT-5.4</td> <td>首次：Azure OpenAI API，按 token 付费；重测：GPT 订阅</td> <td>Codex</td> </tr> <tr> <td>MiMo-V2-Pro</td> <td>首次：opencode free 版本；重测：MiMo 官网 API，按 token 付费</td> <td>opencode / Claude Code</td> </tr> <tr> <td>GLM-5</td> <td>联通元景 Coding Plan，¥30/月</td> <td>Claude Code</td> </tr> <tr> <td>Kimi-K2.5</td> <td>阿里云百炼 Coding Plan，首月 ¥7.9，之后 ¥40/月</td> <td>Claude Code</td> </tr> <tr> <td>Qwen3-Max</td> <td>阿里云百炼 Coding Plan，首月 ¥7.9，之后 ¥40/月</td> <td>Claude Code</td> </tr> <tr> <td>MiniMax-M2.7</td> <td>MiniMax Token Plan Starter，¥29/月</td> <td>Claude Code</td> </tr> </tbody> </table> <h2 id="测试结果总览">测试结果总览</h2> <table> <thead> <tr> <th>模型</th> <th>工具</th> <th>结果</th> <th>耗时（API）</th> <th>花费</th> <th>代码变更</th> </tr> </thead> <tbody> <tr> <td>Claude Opus 4.6</td> <td>Claude Code</td> <td><strong>完全通过</strong></td> <td>14m 30s</td> <td>~¥45</td> <td>+414/-40</td> </tr> <tr> <td>GPT-5.4</td> <td>Codex xhigh</td> <td><strong>完全通过</strong></td> <td>20 min</td> <td>$3.34</td> <td>+351/-19</td> </tr> <tr> <td>MiMo-V2-Pro</td> <td>Claude Code</td> <td><strong>提醒后通过</strong></td> <td>28m 40s</td> <td>¥25.68</td> <td>+297/-44</td> </tr> <tr> <td>GLM-5</td> <td>Claude Code</td> <td><strong>提醒后通过</strong></td> <td>46m 25s</td> <td>$152.84</td> <td>+934/-139</td> </tr> <tr> <td>Kimi-K2.5</td> <td>Claude Code</td> <td>推理失败</td> <td>1h 55m</td> <td>$108.57</td> <td>+486/-28</td> </tr> <tr> <td>MiniMax-M2.7</td> <td>Claude Code</td> <td>推理失败</td> <td>53m 31s</td> <td>¥29</td> <td>+451/-14</td> </tr> <tr> <td>Qwen3-Max</td> <td>Claude Code</td> <td>推理失败</td> <td>1h 3m 39s</td> <td>$117.05</td> <td>+386/-28</td> </tr> </tbody> </table> <blockquote> <p>Claude Opus 4.6、MiniMax-M2.7、MiMo-V2-Pro 的详细分析见前两篇文章，本文重点展开 GPT-5.4、GLM-5、Kimi-K2.5 和 Qwen3-Max。</p> </blockquote> <h2 id="gpt-54334一次通过">GPT-5.4：$3.34，一次通过</h2> <h3 id="一段小插曲">一段小插曲</h3> <p>GPT-5.4 的测试经历了一个戏剧性的反转。</p> <p>我最初的测试跑了 3 小时、花费 ¥820.93（1.67M input tokens，仅 27.1K output，完全没有 cache），任务没有完成——代码有硬伤（卷积方向翻转、<code class="language-plaintext highlighter-rouge">rope_theta</code> 处理错误），甚至没到导出阶段。事后分析，可能是我使用的付费接口和配置有问题，导致速度慢、费用高、效果差。 在来一位经常使用 Codex 的同事帮助下，改成 <strong>Codex xhigh 模式</strong> + 新的Token接口重新跑了一次——<strong>$3.34，20 分钟，一次通过</strong>。</p> <p>同一个模型，Codex 模式不同，结果天壤之别。这也是本系列测评的一个反复出现的主题：<strong>工具配置对模型发挥的影响远超预期</strong>。</p> <h3 id="代码质量">代码质量</h3> <p>GPT-5.4（xhigh 模式）的代码修改了 7 个文件、+351/-19 行，质量很高：</p> <ul> <li><strong>架构决策正确</strong>：复用 <code class="language-plaintext highlighter-rouge">FusedLinearAttention</code>，<code class="language-plaintext highlighter-rouge">attn_type == "short_conv"</code> 分支</li> <li><strong>卷积方向正确</strong>：<code class="language-plaintext highlighter-rouge">padded[l+k] * weight[k]</code>，cross-correlation</li> <li><strong>Shape inference 完整</strong>：在 <code class="language-plaintext highlighter-rouge">ShapeAttention.cpp</code> 中为 short_conv 单独处理，输出 <code class="language-plaintext highlighter-rouge">[B, L, D]</code></li> <li><strong>包含单元测试</strong>：添加了 <code class="language-plaintext highlighter-rouge">NaiveShortConv</code> 参考实现 + prefill/decode 测试，这是所有模型中<strong>唯一主动写了单元测试的</strong></li> <li><strong>状态管理</strong>：<code class="language-plaintext highlighter-rouge">onResize</code> 中将 convState 和 recurrentState 解耦，short_conv 不分配无关内存</li> </ul> <p>整体完成度和 Claude Opus 4.6 接近，在单元测试方面甚至更好。</p> <h2 id="glm-5提醒一次自主修复">GLM-5：提醒一次，自主修复</h2> <p>GLM-5 使用 Claude Code 测试，API 耗时 46 分 25 秒，花费 $152.84。</p> <p>值得一提的是，GLM-5 之前曾做过一次同样的测试，跑了 2 小时以上没有成功，被我手动中断了（具体用的哪个接入方式已经记不清了）。这次换了联通元景的 Coding Plan 重测，表现明显好了很多——同一个模型不同时期的表现确实存在波动。</p> <p>第一次实现能导出、能推理，但输出不正确。我只告诉它”推理结果不对”，没有做任何方向性引导——它自己定位了问题并修复，第二次输出正确：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>我是一篇AI助手，旨在提供信息和帮助。

但是，我不是一个真正的人类人士。我的设计目的是通过自然语言处理技术来模拟对话，并回答问题……
</code></pre></div></div> <h3 id="代码特点">代码特点</h3> <p>GLM-5 的改动量较大（11 文件，+934/-139），做了一些额外的工作：</p> <ul> <li><strong>架构决策正确</strong>：复用 LinearAttention，<code class="language-plaintext highlighter-rouge">attn_type == "short_conv"</code> 分支</li> <li><strong>扩展了 FlatBuffers schema</strong>：在 <code class="language-plaintext highlighter-rouge">LinearAttentionParam</code> 中新增了 <code class="language-plaintext highlighter-rouge">hidden_size</code> 和 <code class="language-plaintext highlighter-rouge">kernel_size</code> 字段</li> <li><strong>全链路覆盖</strong>：Python 导出 → ONNX 自定义算子 → MNN 转换器 → C++ 执行，每一环都有</li> <li><strong>模型映射完善</strong>：正确处理了 <code class="language-plaintext highlighter-rouge">operator_norm</code>、<code class="language-plaintext highlighter-rouge">ffn_norm</code>、<code class="language-plaintext highlighter-rouge">layer_types</code> 等 LFM2 特有的字段</li> </ul> <p>代码量偏多是因为它做了一些防御性重构（比如 <code class="language-plaintext highlighter-rouge">Mlp.__init__</code> 对非 MoE 模型的适配），整体风格规范。</p> <h3 id="关键评价">关键评价</h3> <p>GLM-5 和 MiMo-V2-Pro 处于同一档位——都是”提醒一次后自主修复通过”。区别在于：</p> <ul> <li>GLM-5 更慢（46 分 vs 28 分）、更贵（$152.84 vs ¥25.68）、代码量更多（934 行 vs 297 行）</li> <li>但 GLM-5 的映射更完善，额外处理了一些边界情况</li> </ul> <p>两者的核心能力相当：都能正确理解架构、完成跨语言实现、在提醒后自主调试修复。<strong>国产模型的第一梯队。</strong></p> <h2 id="kimi-k25能跑但修不了">Kimi-K2.5：能跑但修不了</h2> <p>Kimi-K2.5 使用 Claude Code 测试，API 耗时 1 小时 55 分，花费 $108.57。</p> <p>它的完成度不低——模型导出成功、C++ 推理也能执行，走到了和 GLM-5/MiMo 同样的位置。但输出是乱码：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>is to achieve it. Ept for You Perfect It Pa Here perfectes are for For E to E,
the E Passes E (Pes) is a Parme Epaes Epas Epaes Epaesa all Epaeses...
</code></pre></div></div> <p>我给了和 GLM-5、MiMo-V2-Pro 完全相同的提醒——”输出结果不对”。但 Kimi-K2.5 修改后<strong>仍然是一样的乱码</strong>，没有修复成功。</p> <h3 id="根本原因">根本原因</h3> <p>乱码的根因是 <strong>ONNX 导出路径缺失</strong>：<code class="language-plaintext highlighter-rouge">ShortConv.forward()</code> 没有 <code class="language-plaintext highlighter-rouge">torch.onnx.is_in_onnx_export()</code> 分支，导出的是分解后的 PyTorch 标准算子，C++ 端的 <code class="language-plaintext highlighter-rouge">shortconv_forward()</code> 永远不会被执行——相当于 ShortConv 层在推理时没有状态管理，逐 token 生成时自然输出乱码。</p> <p>此外，Python 端注释写了”NO SiLU in original LFM2”，但 C++ 端却加了 SiLU 激活——即使修复了导出路径，数值也会不对。</p> <p><strong>这说明 Kimi-K2.5 理解了 LFM2 的整体结构，但在 MNN 的 ONNX 自定义算子机制上存在盲区</strong>——它不知道需要通过 <code class="language-plaintext highlighter-rouge">FusedLinearAttention</code> 自定义算子来桥接 Python 和 C++，而是用了标准 PyTorch 算子导出，导致 C++ 端的实现形同虚设。</p> <h2 id="qwen3-max导出能过推理崩溃">Qwen3-Max：导出能过，推理崩溃</h2> <p>Qwen3-Max 使用 Claude Code 测试，API 耗时 1 小时 3 分 39 秒，花费 $117.05。</p> <p>模型导出成功，但 C++ 推理直接 segment fault。给了一次提醒后仍然无法修复。</p> <h3 id="架构决策创建新算子">架构决策：创建新算子</h3> <p>与其他大部分模型复用 <code class="language-plaintext highlighter-rouge">LinearAttention</code> 不同，Qwen3-Max 选择了和 MiniMax-M2.7 类似的路线——创建全新的 <code class="language-plaintext highlighter-rouge">ShortConv</code> 算子（OpType 306），包括新的 FlatBuffers schema（<code class="language-plaintext highlighter-rouge">ShortConvParam</code>）、ONNX 自定义算子、C++ 后端实现。这条路更重，需要修改的环节更多，出错概率也更高。</p> <h3 id="崩溃原因">崩溃原因</h3> <p>segment fault 的直接原因是<strong>没有为新算子注册 shape inference</strong>。没有 shape inference，MNN 无法确定输出 tensor 的维度，内存不会被分配，C++ 执行时访问空指针直接崩溃。</p> <p>即使修复了 shape inference，还有更多问题：</p> <ul> <li>converter 中缺少 <code class="language-plaintext highlighter-rouge">main_type</code> 字段，运行时无法反序列化参数，会导致空指针</li> <li>C++ 实现的语义与 Python 端不一致（多了 SiLU 激活、输入维度假设错误）</li> <li>无条件访问 bias tensor，当 <code class="language-plaintext highlighter-rouge">has_bias=false</code> 时越界访问</li> </ul> <h3 id="代码特点-1">代码特点</h3> <p>改动量看似很大（14 文件，+935/-533），但其中 6 个文件是自动生成的注册文件重排序，实际逻辑改动约 +386/-28。此外，<code class="language-plaintext highlighter-rouge">regist_lfm2</code> 中混入了不相关的 InternVL 注册代码，代码组织不够清晰。</p> <h2 id="横向对比">横向对比</h2> <h3 id="按结果分层">按结果分层</h3> <table> <thead> <tr> <th>层级</th> <th>模型</th> <th>说明</th> </tr> </thead> <tbody> <tr> <td><strong>完全通过</strong></td> <td>Opus 4.6、GPT-5.4</td> <td>无人工干预，端到端一次通过</td> </tr> <tr> <td><strong>提醒后通过</strong></td> <td>MiMo-V2-Pro、GLM-5</td> <td>告知输出错误（无方向性引导），自主定位修复</td> </tr> <tr> <td><strong>提醒后仍失败</strong></td> <td>Kimi-K2.5</td> <td>告知输出错误，无法自主修复</td> </tr> <tr> <td><strong>推理失败</strong></td> <td>MiniMax-M2.7</td> <td>导出成功但推理直接报错</td> </tr> <tr> <td><strong>推理崩溃</strong></td> <td>Qwen3-Max</td> <td>导出成功，推理 segment fault，新算子缺 shape inference</td> </tr> </tbody> </table> <h3 id="核心设计决策">核心设计决策</h3> <table> <thead> <tr> <th>决策</th> <th>Opus 4.6</th> <th>GPT-5.4</th> <th>MiMo-V2-Pro</th> <th>GLM-5</th> <th>Kimi-K2.5</th> <th>M2.7</th> <th>Qwen3-Max</th> </tr> </thead> <tbody> <tr> <td>复用 LinearAttention</td> <td>✅</td> <td>✅</td> <td>✅</td> <td>✅</td> <td>✅</td> <td>❌ 新建算子</td> <td>❌ 新建算子</td> </tr> <tr> <td>卷积方向正确</td> <td>✅</td> <td>✅</td> <td>✅</td> <td>✅</td> <td>✅</td> <td>❌</td> <td>❌ 语义不一致</td> </tr> <tr> <td>ONNX 导出路径</td> <td>✅</td> <td>✅</td> <td>✅</td> <td>✅</td> <td>❌ 缺失</td> <td>✅</td> <td>✅</td> </tr> <tr> <td>Shape inference</td> <td>✅</td> <td>✅</td> <td>✅</td> <td>✅</td> <td>✅</td> <td>✅</td> <td>❌ 未注册</td> </tr> <tr> <td>自主写单元测试</td> <td>❌</td> <td>✅</td> <td>❌</td> <td>❌</td> <td>❌</td> <td>❌</td> <td>❌</td> </tr> <tr> <td>多线程 C++</td> <td>✅</td> <td>✅</td> <td>✅</td> <td>✅</td> <td>❌</td> <td>✅</td> <td>❌</td> </tr> </tbody> </table> <h3 id="效率对比">效率对比</h3> <table> <thead> <tr> <th>模型</th> <th>耗时</th> <th>花费</th> <th>有效代码行</th> </tr> </thead> <tbody> <tr> <td>Opus 4.6</td> <td>14m 30s</td> <td>~¥45</td> <td>+414</td> </tr> <tr> <td>GPT-5.4</td> <td>20 min</td> <td>$3.34</td> <td>+351</td> </tr> <tr> <td>MiMo-V2-Pro</td> <td>28m 40s</td> <td>¥25.68</td> <td>+297</td> </tr> <tr> <td>GLM-5</td> <td>46m 25s</td> <td>$152.84</td> <td>+934</td> </tr> <tr> <td>M2.7</td> <td>53m 31s</td> <td>¥29</td> <td>+451</td> </tr> <tr> <td>Kimi-K2.5</td> <td>1h 55m</td> <td>$108.57</td> <td>+486</td> </tr> <tr> <td>Qwen3-Max</td> <td>1h 3m 39s</td> <td>$117.05</td> <td>+386</td> </tr> </tbody> </table> <p>一个有趣的规律：<strong>通过的模型普遍代码更少、速度更快</strong>。Opus、GPT-5.4、MiMo 都在 30 分钟内完成，代码量 300-400 行；而未通过的模型往往耗时更长、代码更多但有效性更低。简洁精准的代码，本身就是能力的体现。</p> <h2 id="关于工具环境的发现">关于工具环境的发现</h2> <p>本系列测评中，有两个模型出现了”换工具/配置后结果逆转”的情况：</p> <ul> <li><strong>GPT-5.4</strong>：我首次测试使用的付费接口和配置可能有问题（完全没有 cache，速度慢费用高），3 小时 ¥820 未完成；同事使用正确配置后 20 分钟 $3.34 通过</li> <li><strong>MiMo-V2-Pro</strong>：opencode 免费额度 → 11 小时导出失败；Claude Code 充值 token → 28 分钟提醒后通过</li> </ul> <p>这不是个例，而是规律。模型能力是基础，但工具链的质量和配置决定了能力能发挥几成。在评价一个模型的工程能力时，工具变量不可忽视。</p> <h2 id="结论">结论</h2> <p>七个模型测下来，形成了清晰的三个梯队：</p> <p><strong>第一梯队：一次通过</strong></p> <ul> <li><strong>Claude Opus 4.6</strong>：14 分钟，~¥45，最快最稳</li> <li><strong>GPT-5.4</strong>（xhigh 模式）：20 分钟，$3.34，性价比最高，还写了单元测试</li> </ul> <p><strong>第二梯队：提醒后通过</strong></p> <ul> <li><strong>MiMo-V2-Pro</strong>：28 分钟，¥25.68，国产最强之一</li> <li><strong>GLM-5</strong>：46 分钟，$152.84，完成度高但代价不菲</li> </ul> <p><strong>第三梯队：未通过</strong></p> <ul> <li><strong>Kimi-K2.5</strong>：能跑但修不了，ONNX 导出机制理解不到位</li> <li><strong>MiniMax-M2.7</strong>：架构决策偏差，走了更重的路</li> <li><strong>Qwen3-Max</strong>：新建算子但缺 shape inference，推理直接崩溃</li> </ul> <p>第一梯队和第二梯队的差距在于”最后一公里”：能否在实现完成后主动验证结果、发现问题并自行修复，而不需要人工提醒。第二梯队和第三梯队的差距则更根本：能否理解框架的核心机制（如 ONNX 自定义算子桥接），并在出错后定位根因。</p> <p>值得一提的是，本次测评只是 LFM2 模型支持的基础部分——Dense Language Model。LFM2 全系列还包括 MoE、VL（视觉）、Audio（音频）三个更复杂的架构变体，这些任务已在 Claude Opus 4.6 上全部验证通过，但其他模型由于费用和耗时等原因尚未测试。随着各模型能力的持续提升，后续计划按架构复杂度逐步加码，进一步拉开区分度。</p> <p>在复杂工程任务上，模型之间的差距比 benchmark 上看到的要大得多。能写代码和能端到端跑通，中间隔着一道鸿沟。</p>]]></content><author><name></name></author><category term="mnn"/><category term="llm"/><summary type="html"><![CDATA[起因]]></summary></entry><entry><title type="html">如何设计端侧高性能 Tokenizer？MNN 重构实践与思考</title><link href="https://zhaode.wang/blog/2026/tokenizer/" rel="alternate" type="text/html" title="如何设计端侧高性能 Tokenizer？MNN 重构实践与思考"/><published>2026-03-20T00:00:00+00:00</published><updated>2026-03-20T00:00:00+00:00</updated><id>https://zhaode.wang/blog/2026/tokenizer</id><content type="html" xml:base="https://zhaode.wang/blog/2026/tokenizer/"><![CDATA[<h2 id="1-引言">1. 引言</h2> <p>在端侧大模型推理中，Tokenizer 是用户输入文本到模型可以理解的 token ID 之间的桥梁。它看似简单，实则涉及 Unicode 规范化、正则分词、子词编码等多个环节。</p> <p>服务端的 Tokenizer 实现（如 HuggingFace Tokenizers）通常基于 Rust 构建，依赖完整的 Unicode 库（ICU）、复杂的正则引擎，编译产物数 MB 级别。这在服务端不是问题，但端侧场景有着截然不同的约束：</p> <ul> <li><strong>极致的包大小控制</strong>：移动端 App 对每一个 KB 都斤斤计较，数 MB 的第三方库不可接受</li> <li><strong>高性能低延迟</strong>：手机 CPU 资源有限，加载和编码必须在毫秒级完成</li> <li><strong>最小化第三方依赖</strong>：每多一个依赖都意味着编译复杂度、兼容性风险和包大小增长</li> <li><strong>低代码量</strong>：代码越少，维护成本越低，出错概率越小</li> </ul> <p>MNN 很早就支持了 LLM 推理的 Tokenizer，但由于当时各家模型的 Tokenizer 尚未收敛，实现上采用了针对不同模型分别适配的方式（Sentencepiece / Tiktoken / BertTokenizer / HuggingfaceTokenizer），功能不够完备，维护成本也在持续增长。</p> <p>本次升级引入了 <strong>PipelineTokenizer</strong>——一套专为端侧设计的、与 HuggingFace Transformers 行为完全对齐的统一流水线架构。设计遵循三个基本原则：<strong>零第三方依赖、高性能、低代码量（小二进制）</strong>。核心思路是「Python 导出时预计算，C++ 加载时零拷贝」——把能在离线做的计算全部提前做掉，让端侧 C++ 代码尽可能简单、快速。</p> <h2 id="2-统一的-tokenizer-流水线原理与流程">2. 统一的 Tokenizer 流水线：原理与流程</h2> <h3 id="21-四阶段流水线">2.1 四阶段流水线</h3> <p>现代 HuggingFace Tokenizer 采用统一的四阶段流水线架构：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>输入文本 → [Normalizer] → [PreTokenizer] → [Model] → [PostProcessor] → Token IDs
</code></pre></div></div> <table> <thead> <tr> <th>阶段</th> <th>职责</th> <th>典型实现</th> </tr> </thead> <tbody> <tr> <td><strong>Normalizer</strong></td> <td>文本规范化：Unicode 归一化、大小写转换等</td> <td>NFKC、BertNormalizer、Prepend(“▁”)</td> </tr> <tr> <td><strong>PreTokenizer</strong></td> <td>预分词：将文本切分为子串</td> <td>ByteLevel (GPT-2)、Metaspace (SentencePiece)、Split (正则)</td> </tr> <tr> <td><strong>Model</strong></td> <td>核心编码：将子串编码为 token ID</td> <td>BPE、WordPiece、Unigram</td> </tr> <tr> <td><strong>Decoder</strong></td> <td>解码还原：将 token ID 还原为文本</td> <td>ByteLevel、Metaspace、WordPiece</td> </tr> </tbody> </table> <p>每个阶段都是可插拔的组件，不同模型只是组件的组合不同。这样一套架构就能覆盖几乎所有主流 LLM 的 Tokenizer。</p> <h3 id="22-以-qwen35-为例一句话的完整编码过程">2.2 以 Qwen3.5 为例：一句话的完整编码过程</h3> <p>以 Qwen3.5（BPE 模型，ByteLevel 预分词）处理一句话 <code class="language-plaintext highlighter-rouge">"Hello, MNN!"</code> 为例，展示每一步的变化：</p> <p><strong>Step 0: 原始输入</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"Hello, MNN!"
</code></pre></div></div> <p><strong>Step 1: Normalizer（无操作）</strong></p> <p>Qwen3 的 Tokenizer 没有配置 Normalizer，文本保持不变：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"Hello, MNN!"
</code></pre></div></div> <p><strong>Step 2: PreTokenizer（ByteLevel + Digits）</strong></p> <p>ByteLevel PreTokenizer 使用类似 GPT-2 的正则模式将文本分割为子串。这个正则的核心思想是：将英文单词（含前导空格）、数字、标点各自切开。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>分割后的子串列表：
["Hello", ",", " MNN", "!"]
</code></pre></div></div> <p>同时，ByteLevel 会将每个字节映射为一个可见的 Unicode 字符（GPT-2 byte-to-unicode mapping）。例如空格 <code class="language-plaintext highlighter-rouge">0x20</code> 被映射为 <code class="language-plaintext highlighter-rouge">Ġ</code>（U+0120）：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ByteLevel 映射后：
["Hello", ",", "ĠMNN", "!"]
</code></pre></div></div> <p><strong>Step 3: Model（BPE 编码）</strong></p> <p>对每个子串独立执行 BPE（Byte Pair Encoding）。BPE 的过程是：</p> <ol> <li>将子串拆为单字符序列</li> <li>反复查找优先级最高的相邻 pair 并合并</li> <li>直到无法继续合并</li> </ol> <p>以子串 <code class="language-plaintext highlighter-rouge">"Hello"</code> 为例：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>初始:  ["H", "e", "l", "l", "o"]
合并1: ["He", "l", "l", "o"]       ← (H, e) 是优先级最高的 pair
合并2: ["He", "ll", "o"]           ← (l, l) 合并
合并3: ["He", "llo"]               ← (ll, o) 合并
合并4: ["Hello"]                   ← (He, llo) 合并
查表:  Hello → 9419
</code></pre></div></div> <p>对所有子串执行 BPE 后：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"Hello" → [9419]
","     → [11]
"ĠMNN"  → [380, 9455]    ← "ĠMNN" 被分为 "ĠM" 和 "NN" 两个子词
"!"     → [0]
</code></pre></div></div> <p><strong>Step 4: 最终结果</strong></p> <p>合并所有子串的 token ID：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[9419, 11, 380, 9455, 0]
</code></pre></div></div> <p>这就是模型实际接收到的输入。</p> <h3 id="23-解码过程逆向">2.3 解码过程（逆向）</h3> <p>解码时，Decoder 将 token ID 还原为文本：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[9419, 11, 380, 9455, 0]
→ 查表: ["Hello", ",", "ĠM", "NN", "!"]
→ 拼接: "Hello,ĠMNN!"
→ ByteLevel 逆映射: "Hello, MNN!"
</code></pre></div></div> <p>其中 <code class="language-plaintext highlighter-rouge">Ġ</code> 被还原为空格，恢复原始文本。</p> <h2 id="3-mnn-旧版-tokenizer-的问题">3. MNN 旧版 Tokenizer 的问题</h2> <p>MNN 从 2023 年就开始支持 LLM 推理的 Tokenizer，但当时各家模型的 Tokenizer 实现差异较大，MNN 采用了分别适配的策略：</p> <table> <thead> <tr> <th>旧实现类</th> <th>适配模型</th> <th>局限性</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">Sentencepiece</code></td> <td>LLaMA 1/2、ChatGLM</td> <td>仅支持 Unigram/BPE，不支持 AddedToken 等特性</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Tiktoken</code></td> <td>GPT 系列、Qwen 1</td> <td>不支持 chat template</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">BertTokenizer</code></td> <td>BERT、ChatGLM</td> <td>WordPiece 专用，不可扩展</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">HuggingfaceTokenizer</code></td> <td>GPT-2、LLaMA 3、Qwen 2</td> <td>使用 std::regex，性能差；不支持 Normalizer</td> </tr> </tbody> </table> <p>存在的问题：</p> <ol> <li><strong>功能不完备</strong>：每种实现只覆盖部分场景。新模型出来时经常需要加 if-else 特殊处理，编码结果与 HuggingFace 不一致</li> <li><strong>文本格式加载慢</strong>：词表以文本 + base64 格式存储，逐行读取解析，15 万词表加载耗时数百毫秒</li> <li><strong>正则性能差</strong>：<code class="language-plaintext highlighter-rouge">std::regex</code> 在移动端性能极差，含 Unicode property（如 <code class="language-plaintext highlighter-rouge">\p{L}</code>）的模式尤甚</li> <li><strong>外部依赖</strong>：Unicode 归一化依赖外部库或缺失，导致部分模型编码不正确</li> </ol> <h2 id="4-新版-pipelinetokenizer一套架构覆盖所有模型">4. 新版 PipelineTokenizer：一套架构覆盖所有模型</h2> <h3 id="41-架构设计">4.1 架构设计</h3> <p>新版 PipelineTokenizer 实现了完整的四阶段流水线，每个阶段通过组合不同组件来适配不同模型：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PipelineTokenizer
├── Normalizer
│   ├── NFKCNormalizer      ← 预计算查表，无外部依赖
│   ├── BertNormalizer      ← 去重音、小写、中文字符处理
│   ├── PrependNormalizer   ← 前缀添加（如 "▁"）
│   ├── ReplaceNormalizer   ← 字符串替换
│   └── SequenceNormalizer  ← 多个 Normalizer 串联
│
├── PreTokenizer
│   ├── ByteLevelPreTokenizer   ← GPT-2/Qwen/LLaMA3 风格
│   ├── MetaspacePreTokenizer   ← SentencePiece 风格
│   ├── BertPreTokenizer        ← BERT 风格
│   ├── SplitPreTokenizer       ← 自定义正则分割
│   ├── DigitsPreTokenizer      ← 数字分割
│   └── SequencePreTokenizer    ← 多个 PreTokenizer 串联
│
├── Model
│   ├── BPEModel        ← GPT/Qwen/LLaMA 等
│   ├── WordPieceModel  ← BERT
│   └── UnigramModel    ← SentencePiece Unigram
│
└── Decoder
    ├── ByteLevelDecoder     ← 逆 byte-to-unicode 映射
    ├── MetaspaceDecoder     ← ▁ → 空格
    ├── WordPieceDecoder     ← 去除 ## 前缀
    ├── ByteFallbackDecoder  ← byte fallback 处理
    ├── ReplaceDecoder       ← 字符串替换
    ├── StripDecoder         ← 去除首尾字符
    └── SequenceDecoder      ← 多个 Decoder 串联
</code></pre></div></div> <p>通过这种组合式设计，可以覆盖目前几乎所有主流 LLM 的 Tokenizer：</p> <table> <thead> <tr> <th>模型</th> <th>Normalizer</th> <th>PreTokenizer</th> <th>Model</th> <th>Decoder</th> </tr> </thead> <tbody> <tr> <td>Qwen3</td> <td>—</td> <td>ByteLevel+Digits</td> <td>BPE</td> <td>ByteLevel</td> </tr> <tr> <td>LLaMA 3</td> <td>—</td> <td>ByteLevel</td> <td>BPE</td> <td>ByteLevel</td> </tr> <tr> <td>LLaMA 2</td> <td>—</td> <td>Metaspace</td> <td>BPE</td> <td>Metaspace+ByteFallback</td> </tr> <tr> <td>BERT</td> <td>BertNormalizer</td> <td>Bert</td> <td>WordPiece</td> <td>WordPiece</td> </tr> <tr> <td>T5</td> <td>—</td> <td>Metaspace</td> <td>Unigram</td> <td>Metaspace</td> </tr> <tr> <td>ChatGLM4</td> <td>—</td> <td>ByteLevel</td> <td>BPE</td> <td>ByteLevel</td> </tr> </tbody> </table> <h3 id="42-与-huggingface-完全对齐">4.2 与 HuggingFace 完全对齐</h3> <p>新版实现严格对齐 HuggingFace Transformers 的 tokenizer 行为，包括：</p> <ul> <li>Added Token 的精确匹配（支持 lstrip/rstrip 属性）</li> <li>Chat Template 的 Jinja2 渲染（内置轻量级 Jinja 引擎）</li> <li>特殊 token 的处理策略</li> </ul> <p>测试覆盖了 <strong>47 个模型、1901 条测试用例</strong>，与 HuggingFace Python 实现的输出完全一致。</p> <h2 id="5-端侧优化针对移动端的深度定制">5. 端侧优化：针对移动端的深度定制</h2> <p>端侧部署对加载速度、运行时内存、安装包大小有严格要求。本次升级在三个维度做了针对性优化。</p> <h3 id="51-加载速度核心优化导出时排序而非加载时排序">5.1 加载速度：核心优化——导出时排序而非加载时排序</h3> <p><strong>问题</strong>：旧版文本格式逐行读取 15 万行词表，每行需要 base64 解码 + <code class="language-plaintext highlighter-rouge">std::string</code> 分配，加载耗时数百毫秒。更关键的是，旧版在 C++ 加载时需要构建 <code class="language-plaintext highlighter-rouge">unordered_map&lt;string, int&gt;</code> 作为词表查找结构——15 万次 string 哈希 + 插入的开销非常大。</p> <p><strong>核心思路</strong>：将排序从加载时移到导出时。</p> <p>传统做法是按 token ID 顺序存储词表（ID 0, 1, 2, …），加载后构建哈希表用于 encode 时的 token→ID 查找。本次优化的关键洞察是：<strong>在 Python 导出时就按 token 字符串的字典序排好序存储</strong>。这样 C++ 端加载后无需任何额外处理，直接就是一个有序数组，可以用二分查找。</p> <p>代价是什么？由于词表不再按 ID 顺序存储，每个 token 需要额外存储一个 4 字节的 token ID。对于 15 万词表，这仅增加约 600KB，但换来的是：</p> <ul> <li><strong>省去 15 万次 hashmap 插入</strong>：不再需要构建 <code class="language-plaintext highlighter-rouge">unordered_map</code></li> <li><strong>省去 15 万次 string 分配</strong>：配合 StringRef 零拷贝，token 字符串直接指向 buffer</li> <li><strong>加载速度提升 20 倍以上</strong></li> </ul> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>旧方案加载流程：
  逐行读取 → base64 解码 → 分配 string → 插入 hashmap  （× 15万次）

新方案加载流程：
  单次 read() 整个文件 → 指针偏移填充 StringRef 数组    （× 15万次，无分配）
</code></pre></div></div> <p><strong>方案细节</strong>：</p> <ol> <li><strong>单次 I/O</strong>：整个二进制部分一次 <code class="language-plaintext highlighter-rouge">read()</code> 读入 <code class="language-plaintext highlighter-rouge">binary_buf_</code></li> <li><strong>零拷贝 StringRef</strong>：词表 token 不分配 <code class="language-plaintext highlighter-rouge">std::string</code>，用 <code class="language-plaintext highlighter-rouge">StringRef</code> 直接指向 buffer 中的位置</li> <li><strong>导出时预排序</strong>：Python 导出时按字典序排序，C++ 端直接用 <code class="language-plaintext highlighter-rouge">std::lower_bound</code> 二分查找</li> <li><strong>预计算 Merge 规则</strong>：BPE merge 规则在导出时转为 <code class="language-plaintext highlighter-rouge">(id1&lt;&lt;32|id2)</code> 的 uint64 key 并排序</li> </ol> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 零拷贝：StringRef 直接指向读入的 buffer，无 string 分配</span>
<span class="k">struct</span> <span class="nc">StringRef</span> <span class="p">{</span>
    <span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">ptr</span><span class="p">;</span>   <span class="c1">// 直接指向 binary_buf_ 中的位置</span>
    <span class="kt">uint16_t</span> <span class="n">len</span><span class="p">;</span>
<span class="p">};</span>

<span class="c1">// 词表条目：同样零拷贝，存储预排序的 token→ID 映射</span>
<span class="k">struct</span> <span class="nc">VocabEntry</span> <span class="p">{</span>
    <span class="k">const</span> <span class="kt">char</span><span class="o">*</span> <span class="n">str</span><span class="p">;</span>   <span class="c1">// 指向 buffer</span>
    <span class="kt">uint16_t</span> <span class="n">len</span><span class="p">;</span>
    <span class="kt">int</span> <span class="n">id</span><span class="p">;</span>            <span class="c1">// 导出时预排序的代价：多存一个 ID</span>
<span class="p">};</span>

<span class="c1">// 加载时只需顺序填充，无需排序或构建哈希表</span>
<span class="k">for</span> <span class="p">(</span><span class="kt">uint32_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">vocab_size</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">StringRef</span> <span class="n">sr</span> <span class="o">=</span> <span class="n">read_str_ref</span><span class="p">(</span><span class="n">ptr</span><span class="p">);</span>       <span class="c1">// 零拷贝读取</span>
    <span class="kt">int</span> <span class="n">id</span> <span class="o">=</span> <span class="p">(</span><span class="kt">int</span><span class="p">)</span><span class="n">read_u32</span><span class="p">(</span><span class="n">ptr</span><span class="p">);</span>            <span class="c1">// 读取预存的 ID</span>
    <span class="n">id_to_token</span><span class="p">[</span><span class="n">id</span><span class="p">]</span> <span class="o">=</span> <span class="n">sr</span><span class="p">;</span>                   <span class="c1">// decode 用：ID→token</span>
    <span class="n">sorted_vocab</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="n">sr</span><span class="p">.</span><span class="n">ptr</span><span class="p">,</span> <span class="n">sr</span><span class="p">.</span><span class="n">len</span><span class="p">,</span> <span class="n">id</span><span class="p">};</span> <span class="c1">// encode 用：已排序，直接二分</span>
<span class="p">}</span>
</code></pre></div></div> <p>encode 时的词表查找从 hashmap 变为二分查找，虽然单次查找从 O(1) 变为 O(log n)，但排序数组在内存中连续存放，对 CPU cache 极为友好，实测性能相当甚至更优，且完全省去了加载时的构建开销。</p> <h3 id="52-文件大小紧凑二进制存储">5.2 文件大小：紧凑二进制存储</h3> <table> <thead> <tr> <th>存储项</th> <th>旧格式</th> <th>新格式</th> </tr> </thead> <tbody> <tr> <td>Token 内容</td> <td>base64 编码（膨胀 33%）</td> <td>原始字节 + 2B 长度前缀</td> </tr> <tr> <td>Token ID</td> <td>ASCII 十进制文本</td> <td>4B uint32</td> </tr> <tr> <td>Merge 规则</td> <td>文本（如 <code class="language-plaintext highlighter-rouge">"Ġ t"</code>）</td> <td>12B（id1 + id2 + rank）</td> </tr> <tr> <td>Chat Template</td> <td>单独 JSON 文件</td> <td>内嵌在 .mtok 尾部</td> </tr> </tbody> </table> <p>综合来看，<code class="language-plaintext highlighter-rouge">.mtok</code> 文件比旧版 <code class="language-plaintext highlighter-rouge">.txt</code> + <code class="language-plaintext highlighter-rouge">tokenizer_config.json</code> 体积更小，同时还包含了更多信息（Normalizer 配置、Decoder 配置等）。</p> <h3 id="53-编码性能unicode-折叠--轻量-regex-替代-stdregex">5.3 编码性能：Unicode 折叠 + 轻量 Regex 替代 std::regex</h3> <p><code class="language-plaintext highlighter-rouge">std::regex</code> 是端侧 Tokenizer 的一大性能瓶颈。许多 Tokenizer 的预分词模式包含 Unicode property（如 <code class="language-plaintext highlighter-rouge">\p{L}+</code>、<code class="language-plaintext highlighter-rouge">\p{N}{1,3}</code>），而主流 C++ 标准库的 regex 实现对 Unicode property 支持差或根本不支持。</p> <p>新版实现了一个通用的 <code class="language-plaintext highlighter-rouge">regex_scanner</code>，核心思路是 <strong>Unicode 折叠</strong>：先将 Unicode 文本中的每个字符按 General Category 映射为单字节分类码，再用轻量级的 regex 引擎在折叠后的单字节序列上匹配。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Unicode 折叠示意：
原始文本:   "Hello 你好 123"
折叠后:     "\x01\x01\x01\x01\x01 \x01\x01 \x02\x02\x02"
            (Letter=0x01, Number=0x02, 其余保留原字节)

正则模式也同步折叠：
\p{L}+  →  [\x01]+
\p{N}+  →  [\x02]+
</code></pre></div></div> <p>折叠后的匹配完全在单字节域上进行，避免了传统 regex 引擎处理多字节 Unicode 的复杂性。对于极少数无法折叠处理的模式，才回退到 <code class="language-plaintext highlighter-rouge">std::wregex</code>。</p> <h3 id="54-nfkcnfd-归一化从代码内嵌到文件内嵌">5.4 NFKC/NFD 归一化：从代码内嵌到文件内嵌</h3> <p>Unicode 归一化（NFKC / NFD）是 Normalizer 阶段可能用到的功能。简单解释：</p> <ul> <li><strong>NFKC</strong>（兼容性分解后标准合成）：将 Unicode 中形式不同但语义等价的字符统一。例如全角字母 <code class="language-plaintext highlighter-rouge">Ａ</code>→<code class="language-plaintext highlighter-rouge">A</code>、罗马数字 <code class="language-plaintext highlighter-rouge">Ⅲ</code>→<code class="language-plaintext highlighter-rouge">III</code>、连字 <code class="language-plaintext highlighter-rouge">ﬁ</code>→<code class="language-plaintext highlighter-rouge">fi</code> 等</li> <li><strong>NFD</strong>（标准分解）：将组合字符拆为基字符+组合标记。例如 <code class="language-plaintext highlighter-rouge">é</code> 拆为 <code class="language-plaintext highlighter-rouge">e</code> + <code class="language-plaintext highlighter-rouge">´</code>，配合 BertNormalizer 的 <code class="language-plaintext highlighter-rouge">strip_accents</code> 功能去除重音符号</li> </ul> <p>这两个表的数据量不小（NFKC 约 5000+ 条目，NFD 类似），通常需要 ICU 等 Unicode 库（数 MB 级）来支持。但在实际端侧场景中，<strong>只有少数模型需要归一化</strong>——主要是 BERT 系列模型会用到 BertNormalizer（含 NFKC 和 NFD），主流 LLM（Qwen、LLaMA、ChatGLM 等）的 Tokenizer 都不需要 Normalizer。</p> <p>因此，本次方案的设计是：<strong>不将 NFKC/NFD 数据编译到 C++ 代码中，而是在 Python 导出时按需写入 .mtok 文件</strong>。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>导出策略：
- Qwen3.mtok:  Normalizer = None，文件中不包含 NFKC/NFD 数据
- BERT.mtok:   Normalizer = BertNormalizer，文件中内嵌 NFD 表
- T5.mtok:     Normalizer = SequenceNormalizer(NFKC + Prepend)，文件中内嵌 NFKC 表
</code></pre></div></div> <p>这样做的好处是：</p> <ol> <li><strong>二进制包大小零增长</strong>：不需要归一化的模型（绝大多数），C++ 代码中完全不包含这些数据</li> <li><strong>无外部依赖</strong>：需要归一化的模型，数据已经在 .mtok 文件中预计算好，C++ 端只需二分查找，不依赖 ICU</li> <li><strong>按需付费</strong>：只有实际使用归一化功能的模型文件才会包含这些数据</li> </ol> <h3 id="55-代码依赖与包大小">5.5 代码依赖与包大小</h3> <p>端侧部署对安装包大小极为敏感。本次优化在依赖控制上做了以下工作：</p> <ol> <li><strong>Unicode 归一化数据外置</strong>：NFKC/NFD 数据不编译进代码，按需内嵌到模型文件中（见 5.4）</li> <li><strong>Unicode 分类数据精简内置</strong>：预分词所需的 Unicode General Category 数据（30 种分类的码点范围表 + 大小写映射）以 range 压缩形式编译进代码，仅约 <strong>30KB</strong></li> <li><strong>内置轻量 Jinja 引擎</strong>：Chat Template 渲染使用单头文件的 Jinja2 子集实现（~2200 行），替代了之前的 minja 依赖</li> <li><strong>轻量 JSON 封装（ujson）</strong>：LLM 推理引擎中仍有配置文件等场景需要解析 JSON。端侧实测 rapidjson 性能明显优于 nlohmann/json，但 rapidjson 的 API 易用性较差（需要手动判断类型、手动处理缺失字段等）。因此我们基于 rapidjson 封装了 ujson，兼顾了 rapidjson 的解析性能和 nlohmann 风格的简洁 API</li> <li><strong>代码合并精简</strong>：将原先分散的多个 tokenizer 实现合并到统一的 <code class="language-plaintext highlighter-rouge">tokenizer.hpp/cpp</code>，消除了条件编译和重复代码</li> </ol> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tokenizer 目录文件：
├── tokenizer.hpp        # 统一声明
├── tokenizer.cpp        # 统一实现（~2400 行）
├── jinja.hpp            # 轻量 Jinja2（单头文件）
├── unicode.hpp/cpp      # Unicode 工具（折叠 + regex scanner）
└── unicode_data.hpp/cpp # Unicode 分类数据（预生成，~30KB）
</code></pre></div></div> <p>整个 tokenizer 模块无外部依赖，仅依赖 C++11 标准库，即可支持所有主流 LLM 的 Tokenizer。</p> <h2 id="6-性能实测">6. 性能实测</h2> <p>以 Qwen3.5-0.8B 模型的 Tokenizer 为基准，在 MacBook Pro (M3 Pro) 上对比三种实现的性能：MNN 新版 PipelineTokenizer（.mtok）、MNN 旧版（.txt）、HuggingFace Transformers（Python，底层为 Rust 实现的 tokenizers 库）。</p> <p>测试项说明：</p> <ul> <li><strong>Load</strong>：加载 Tokenizer（5 轮取平均）</li> <li><strong>Encode(short)</strong>：4 条短文本编码（中英文混合，共约 61 tokens），100 轮取平均</li> <li><strong>Encode(long)</strong>：~5KB 长文本编码（约 1088 tokens），20 轮取平均</li> <li><strong>Decode</strong>：逐 token 解码（21 tokens × 1000 轮取平均）</li> </ul> <h3 id="测试结果">测试结果</h3> <table> <thead> <tr> <th>指标</th> <th style="text-align: center">HuggingFace (Rust)</th> <th style="text-align: center">MNN 旧版 (.txt)</th> <th style="text-align: center">MNN 新版 (.mtok)</th> </tr> </thead> <tbody> <tr> <td><strong>Load</strong></td> <td style="text-align: center">485.27 ms</td> <td style="text-align: center">71.73 ms</td> <td style="text-align: center"><strong>3.14 ms</strong></td> </tr> <tr> <td><strong>Encode(short)</strong></td> <td style="text-align: center">0.022 ms</td> <td style="text-align: center">0.038 ms</td> <td style="text-align: center"><strong>0.004 ms</strong></td> </tr> <tr> <td><strong>Encode(long)</strong></td> <td style="text-align: center">1.098 ms</td> <td style="text-align: center">3.097 ms</td> <td style="text-align: center"><strong>0.171 ms</strong></td> </tr> <tr> <td><strong>Decode</strong></td> <td style="text-align: center">0.0009 ms</td> <td style="text-align: center">0.0001 ms</td> <td style="text-align: center">0.0002 ms</td> </tr> </tbody> </table> <h3 id="性能提升倍数">性能提升倍数</h3> <table> <thead> <tr> <th>指标</th> <th style="text-align: center">新版 vs HuggingFace</th> <th style="text-align: center">新版 vs MNN 旧版</th> </tr> </thead> <tbody> <tr> <td><strong>Load</strong></td> <td style="text-align: center"><strong>154x</strong></td> <td style="text-align: center"><strong>22.8x</strong></td> </tr> <tr> <td><strong>Encode(short)</strong></td> <td style="text-align: center"><strong>5.5x</strong></td> <td style="text-align: center"><strong>9.5x</strong></td> </tr> <tr> <td><strong>Encode(long)</strong></td> <td style="text-align: center"><strong>6.4x</strong></td> <td style="text-align: center"><strong>18.1x</strong></td> </tr> <tr> <td><strong>Decode</strong></td> <td style="text-align: center">4.5x</td> <td style="text-align: center">~持平</td> </tr> </tbody> </table> <p>几个值得注意的点：</p> <ol> <li><strong>加载速度是最大的提升点</strong>：新版 3.14ms vs 旧版 71.73ms vs HuggingFace 485.27ms。这主要归功于两方面：一是「导出时预排序 + 二进制零拷贝」的设计省去了 hashmap 构建和 string 分配；二是<strong>完全绕过了 JSON 解析</strong>。HuggingFace 的 <code class="language-plaintext highlighter-rouge">tokenizer.json</code> 是一个数 MB 的 JSON 文件，包含完整的词表、merge 规则和配置，光解析这个 JSON 就要消耗大量时间。这也是我们放弃直接使用 HuggingFace 原始 JSON 格式、选择自定义二进制格式的核心原因——JSON 解析在端侧是不可接受的性能瓶颈</li> <li><strong>编码速度全面领先 Rust 实现</strong>：即使 HuggingFace tokenizers 底层是 Rust 编写的高性能库，MNN 新版仍快 5-6 倍。这说明「预计算 + 紧凑数据结构」的收益大于语言层面的差异</li> <li><strong>长文本编码提升最为显著</strong>：18 倍于旧版，主要受益于 Unicode 折叠替代 std::regex、BPE 缓存命中、以及排序数组的 cache 友好性</li> </ol> <h2 id="7-总结">7. 总结</h2> <table> <thead> <tr> <th>维度</th> <th>旧版</th> <th>新版 PipelineTokenizer</th> </tr> </thead> <tbody> <tr> <td>架构</td> <td>4 种独立实现，按模型分派</td> <td>统一四阶段流水线，组件化组合</td> </tr> <tr> <td>正确性</td> <td>部分模型存在编码差异</td> <td>47 模型 1901 用例 100% 对齐 HuggingFace</td> </tr> <tr> <td>词表查找</td> <td>hashmap（加载时构建，慢）</td> <td>导出时预排序 + StringRef 零拷贝（加载即用）</td> </tr> <tr> <td>加载速度</td> <td>数百毫秒</td> <td>十余毫秒（20x+ 提升）</td> </tr> <tr> <td>预分词</td> <td>std::regex</td> <td>Unicode 折叠 + 轻量 regex</td> </tr> <tr> <td>文件格式</td> <td>文本 + base64 + 外部 JSON</td> <td>单一 .mtok 二进制文件</td> </tr> <tr> <td>NFKC/NFD 数据</td> <td>依赖 ICU 或缺失</td> <td>按需内嵌到 .mtok 文件，不增加包大小</td> </tr> <tr> <td>外部依赖</td> <td>ICU / minja 等</td> <td>零外部依赖，纯 C++11</td> </tr> </tbody> </table> <p>端侧 Tokenizer 的设计哲学可以概括为一句话：<strong>把复杂性留给离线工具，把简单性留给运行时</strong>。</p> <p>Python 导出阶段承担了所有「重活」：词表排序、Merge 规则预计算、Unicode 归一化表生成、二进制格式序列化。C++ 运行时只需要做最简单的事：读入一块内存、设置几个指针、用二分查找编码。整个 Tokenizer 的 C++ 实现约 2400 行，无任何第三方依赖（不依赖 ICU、不依赖 Rust tokenizers、不依赖 sentencepiece 库），仅需 C++11 标准库即可编译。Unicode 分类数据以压缩 range 形式内置约 30KB，NFKC/NFD 等归一化数据按需内嵌到模型文件中而非代码中，做到了「不用不付费」。</p> <p>这套方案证明了一个朴素的道理：端侧推理不需要搬运服务端的全部复杂性。通过合理的离线-在线分工，完全可以用极小的代码量和零外部依赖，实现与 HuggingFace Transformers 100% 对齐的 Tokenizer 功能，同时获得远超服务端实现的加载和编码性能。</p> <h2 id="8-开发思路vibecoding-与人工优化的协作">8. 开发思路：VibeCoding 与人工优化的协作</h2> <p>本次 Tokenizer 重构大量使用了 <strong>VibeCoding</strong> 的开发方式，借助 Gemini 3.1 Pro 和 Claude Opus 4.6 完成了大部分实现工作。具体来说：</p> <p><strong>AI 擅长的部分：</strong></p> <ul> <li><strong>流水线架构搭建</strong>：四阶段 Pipeline 的组件化实现，各种 Normalizer、PreTokenizer、Model、Decoder 的编写，AI 可以参照 HuggingFace 的 Python 代码高效地翻译为 C++</li> <li><strong>编解码算法实现</strong>：BPE 合并、Unigram Viterbi、WordPiece 匹配等标准算法，AI 给出的实现基本正确</li> <li><strong>格式解析与序列化</strong>：二进制格式的读写、Python 导出脚本的编写，AI 能够快速完成</li> <li><strong>测试对齐</strong>：批量生成测试用例、对比 HuggingFace 输出、定位不一致的 case，AI 在这类繁琐工作中效率极高</li> </ul> <p><strong>AI 做不到的部分——核心优化策略需要人工指导：</strong></p> <p>然而，几个决定性能天花板的关键优化策略，AI 都未能自主给出正确方向：</p> <ol> <li><strong>「导出时排序而非加载时排序」</strong>：这是本次最核心的优化。AI 的默认思路是按 token ID 顺序存储词表（这是所有现有实现的做法），加载后构建 hashmap。当我提出「能否在导出时就排好序，加载时直接用」时，AI 能很好地实现这个方案，但它不会主动想到用「多存一个 4 字节 ID」来换取「省去整个 hashmap 构建」这个 trade-off</li> <li><strong>「正则预分词的实现路径」</strong>：AI 先提出引入第三方正则库 oniguruma（被否），再转向为 GPT-2、LLaMA3 手写专用 scanner（通用性差），最终在引导下设计了 Unicode 折叠 + 轻量 regex 的通用方案</li> <li><strong>「NFKC/NFD 数据放文件里而非代码里」</strong>：AI 的默认方案是引入 utf8proc 库或将 Unicode 数据表编译进代码，但考虑到绝大多数 LLM 不需要归一化，这些数据不应该增加所有用户的二进制大小</li> </ol> <p><strong>反思：</strong></p> <p>目前来看，AI 在「实现已知方案」上的能力已经非常强——给定明确的优化方向，它可以高质量地完成编码、测试、调试。但在「提出优化方向」上，尤其是针对端侧这种有特殊约束的场景（包大小敏感、无法依赖重量级库、需要在多个 trade-off 之间做判断），AI 往往会给出服务端视角的「标准答案」，而非端侧视角的「最优答案」。</p> <p>这种人机协作的模式或许是当前阶段最高效的开发方式：<strong>人负责方向判断和关键 trade-off 决策，AI 负责高效实现和大量测试验证</strong>。整个 Tokenizer 的重构（覆盖 47 个模型、1901 条测试用例、C++ 约 2400 行 + Python 导出约 500 行）在这种协作模式下得以高效完成。</p>]]></content><author><name></name></author><category term="MNN"/><category term="llm"/><summary type="html"><![CDATA[1. 引言]]></summary></entry><entry><title type="html">MiniMax-M2.7 实测：离 Claude 还有多远？</title><link href="https://zhaode.wang/blog/2026/minimax2.7/" rel="alternate" type="text/html" title="MiniMax-M2.7 实测：离 Claude 还有多远？"/><published>2026-03-19T00:00:00+00:00</published><updated>2026-03-19T00:00:00+00:00</updated><id>https://zhaode.wang/blog/2026/minimax2.7</id><content type="html" xml:base="https://zhaode.wang/blog/2026/minimax2.7/"><![CDATA[<h2 id="起因">起因</h2> <p>每次有国产编程大模型发布，我都会关注一个问题：<strong>它的代码能力跟 Claude 还差多远？</strong> 我们此前写过一篇文章，介绍如何将复杂的模型适配任务拆解为 <strong>TDD 驱动的 Skill</strong>，Claude Opus 4.6 能直接通过，但国产 SOTA 编程模型 GLM-5 没能跑通。从那之后我就一直在等：什么时候能有一个国产模型通过这个测试？</p> <p>3 月 18 日，MiniMax 发布了 M2.7。官方公告中给出了一组亮眼数据：<strong>M2.7 在 40 个复杂 Skills（&gt; 2000 Token）上保持 97% 的 Skills 遵循率</strong>，在 OpenClaw 使用和 MMClaw 评测中接近最新的 Sonnet 4.6。看到公告的当天，我就决定立刻测试——既然 M2.7 宣称在复杂 Skills 上有极高的遵循率，那它能不能通过我们这个真实工程任务？</p> <h2 id="测试设计">测试设计</h2> <h3 id="任务">任务</h3> <p>在 MNN 推理框架中端到端地支持 LFM2-350M 模型的导出和推理。LFM2 是 Liquid AI 提出的混合架构 LLM，包含两种 Decoder 层——ShortConv（短卷积门控线性注意力）和标准 Full Attention。任务的特殊之处在于 ShortConv 是一种全新的算子，仓库中没有现成模板可以直接套用。</p> <p>同一任务此前已在 <strong>GLM-5</strong> 和 <strong>Claude Opus 4.6</strong> 上测试过：GLM-5 未通过，Opus 4.6 完全通过。这次加入 MiniMax-M2.7 作为新的测试对象。</p> <p>一条指令触发全程，模型完全自主：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>claude <span class="nt">--dangerously-skip-permissions</span> <span class="s2">"请帮我支持 LFM2 模型（/home/yanxing/data/models/LFM2-350M），支持模型导出和推理；其中ShortConv算子实现在LinearAttention中，作为LinearAttention的一个type实现。"</span>
</code></pre></div></div> <h3 id="为什么这个任务有区分度">为什么这个任务有区分度</h3> <p>这不是写一个函数或改一个 Bug，而是一个<strong>跨 7+ 文件、跨 Python/C++ 两种语言、跨导出/转换/推理三个阶段</strong>的端到端工程任务。仓库虽然提供了 Skill 指引文件，但 LFM2 的 ShortConv 机制与已有的 Attention/LinearAttention 都不同，不能简单复制粘贴。模型需要：</p> <ol> <li>读懂 LFM2 模型的 Python 源码，理解 ShortConv 的计算逻辑</li> <li>修改 Python 导出管道（配置映射、模型导出、自定义算子 ONNX 符号化）</li> <li>定义新算子的 Schema</li> <li>写 C++ 算子实现（含推理时的状态管理）</li> <li>确保导出和推理<strong>都能</strong>正确运行</li> </ol> <h3 id="测试过程">测试过程</h3> <p>我一开始使用 MiniMax 赠送的 <strong>15 元免费额度</strong>进行测试，结果模型还在探索代码阶段额度就耗尽了——15 元大概只够模型读几轮代码文件。</p> <p>于是我充值了 <strong>29 元/月的入门款 Coding Plan</strong>（可用额度：600 次模型调用 / 5 小时），继续之前中断的测试。由于 MiniMax-M2.7 的响应速度较慢，我直接把任务挂在服务器上，第二天查看结果。最终 API 耗时 53 分钟，wall time 13 小时 38 分钟（wall time 长主要因为模型响应慢加上编译等待）。Claude Code 的 <code class="language-plaintext highlighter-rouge">/cost</code> 命令推算的等价费用为 $190.19——这不是实际花费，但反映了 token 消耗量级。</p> <h2 id="测试结果">测试结果</h2> <table> <thead> <tr> <th>指标</th> <th>MiniMax-M2.7</th> <th>GLM-5</th> <th>Claude Opus 4.6</th> </tr> </thead> <tbody> <tr> <td><strong>最终结果</strong></td> <td>导出成功，推理失败</td> <td>未通过</td> <td>完全通过</td> </tr> <tr> <td><strong>API 耗时</strong></td> <td>53m 31s</td> <td>-</td> <td>14m 30s</td> </tr> <tr> <td><strong>实际耗时（wall）</strong></td> <td>13h 38m 56s</td> <td>-</td> <td>20m 24s</td> </tr> <tr> <td><strong>代码变更</strong></td> <td>+748 / -197 行</td> <td>-</td> <td>+414 / -40 行</td> </tr> <tr> <td><strong>等价费用（/cost 推算）</strong></td> <td>$190.19</td> <td>-</td> <td>$6.57</td> </tr> <tr> <td><strong>Token 用量</strong></td> <td>35.6M input / 107.8K output</td> <td>-</td> <td>15.5K input / 40.1K output</td> </tr> </tbody> </table> <p>推理失败的报错：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Broad cast error, dim1 = 3072, dim2 = 1024
Compute Shape Error for /blocks.1/self_attn/Mul_output_0
code=3 in onForward, 602
</code></pre></div></div> <h2 id="minimax-m27-做对了什么">MiniMax-M2.7 做对了什么</h2> <p>公允地说，MiniMax-M2.7 展现了不错的工程素养：</p> <p><strong>管线完整性</strong>：它正确识别了任务需要修改的所有层级——Python 模型映射、配置处理、导出逻辑、FlatBuffers Schema、ONNX 转换器、C++ 算子实现、算子注册——并为每个层级都产出了代码。模型导出阶段（Python → ONNX → MNN）没有报错，说明管线的”上半段”是通的。</p> <p><strong>模型映射准确</strong>：LFM2 的命名与标准 LLaMA 架构有不少差异（比如 <code class="language-plaintext highlighter-rouge">default_theta</code> 代替 <code class="language-plaintext highlighter-rouge">rope_theta</code>，<code class="language-plaintext highlighter-rouge">operator_norm</code> 代替 <code class="language-plaintext highlighter-rouge">input_layernorm</code>），MiniMax-M2.7 正确处理了这些映射关系。</p> <p><strong>C++ 代码规范</strong>：算子实现遵循了 MNN Execution 的标准范式——<code class="language-plaintext highlighter-rouge">onResize</code> 分配缓冲区、<code class="language-plaintext highlighter-rouge">onExecute</code> 执行计算、<code class="language-plaintext highlighter-rouge">onClone</code> 支持克隆，代码组织清晰。</p> <h2 id="问题分析">问题分析</h2> <p>审查 MiniMax-M2.7 的代码变更后，我发现了四个问题，按严重程度排列：</p> <ol> <li> <p><strong>算子注册管线不完整（推理崩溃的直接原因）</strong>：MNN 的每个自定义算子都需要注册形状推理器，MiniMax-M2.7 遗漏了这一步。运行时不知道 ShortConv 算子的输出形状，后续算子维度不匹配，推理直接崩溃。如果按指令要求复用 LinearAttention 的管线，这步会被自动继承，根本不会出错。</p> </li> <li> <p><strong>融合算子内外计算重复</strong>：C++ 算子内部已经完成了完整的 <code class="language-plaintext highlighter-rouge">C * conv(B*x)</code> 计算，但导出的计算图中又在算子外面多乘了一次 C，实际变成了 <code class="language-plaintext highlighter-rouge">C² * conv(B*x)</code>。即使修复问题 1，结果也是错的。</p> </li> <li> <p><strong>卷积状态管理错误</strong>：Decode 阶段（逐 token 生成）需要维护滑动窗口状态，应保存最近几步的卷积<strong>输入</strong>，MiniMax-M2.7 存了卷积<strong>输出</strong>，且窗口滑动时机也不对。即使修复前两个问题，生成阶段结果仍然会出错。</p> </li> <li> <p><strong>未遵循设计约束</strong>：指令要求”作为 LinearAttention 的一个 type 实现”，MiniMax-M2.7 创建了全新的独立算子，手动修改了约 180 行 Schema 生成代码，增大了出错面。</p> </li> </ol> <p>四个问题环环相扣：问题 4（未复用已有管线）直接导致了问题 1（遗漏形状推理），问题 2 和 3 则反映了对算子设计和有状态推理的理解不足。</p> <h2 id="效率分析">效率分析</h2> <p>重点看 API 耗时和 token 用量——这两个指标不受模型响应速度影响，能真实反映模型的工作效率：</p> <p><strong>API 耗时差距</strong>：MiniMax-M2.7 为 53 分钟，Opus 4.6 为 14 分 30 秒，差距约 <strong>3.7 倍</strong>。这意味着 MiniMax-M2.7 经历了更多轮的<strong>编译-测试-修复</strong>循环，但始终没有定位到核心问题。</p> <p><strong>Token 用量差距</strong>：MiniMax-M2.7 消耗了 35.6M input tokens，Opus 4.6 仅 15.5K，差距约 <strong>2300 倍</strong>。即使考虑到大量 cache read（6.8M），MiniMax-M2.7 的策略仍然是”大面积反复阅读代码 + 多轮试错”，而非精准定位。</p> <p><strong>等价费用差距</strong>：按 Claude Code <code class="language-plaintext highlighter-rouge">/cost</code> 推算，$190 vs $6.57，差距 <strong>29 倍</strong>——消耗了 29 倍的 token，产出了 1.8 倍的代码变更，但最终没有通过。</p> <table> <thead> <tr> <th>指标</th> <th>MiniMax-M2.7</th> <th>Claude Opus 4.6</th> <th>差距</th> </tr> </thead> <tbody> <tr> <td>API 耗时</td> <td>53m 31s</td> <td>14m 30s</td> <td>3.7x</td> </tr> <tr> <td>Input tokens</td> <td>35.6M</td> <td>15.5K</td> <td>~2300x</td> </tr> <tr> <td>Output tokens</td> <td>107.8K</td> <td>40.1K</td> <td>2.7x</td> </tr> <tr> <td>代码变更</td> <td>+748 / -197</td> <td>+414 / -40</td> <td>1.8x</td> </tr> <tr> <td>等价费用</td> <td>$190.19</td> <td>$6.57</td> <td>29x</td> </tr> <tr> <td>结果</td> <td>失败</td> <td>成功</td> <td>-</td> </tr> </tbody> </table> <h2 id="结论">结论</h2> <h3 id="minimax-m27-的能力边界">MiniMax-M2.7 的能力边界</h3> <p>MiniMax-M2.7 能读懂一个复杂的 C++ 框架项目，正确识别需要修改的所有子系统，并产出结构规范的代码。模型导出阶段（管线的”上半段”）完全跑通了。<strong>这本身已经是不低的能力水平</strong>。</p> <p>但在这个任务上，它暴露了三个层面的短板：</p> <ol> <li><strong>模式匹配 vs 深度理解</strong>：能模仿仓库中已有算子的注册模式，但无法推断出完整管线的所有必要步骤——遗漏了形状推理这一关键环节</li> <li><strong>系统设计能力</strong>：在融合算子的输入输出边界设计上缺乏严谨性，导致内外计算重复</li> <li><strong>执行效率</strong>：消耗了大量 token 反复阅读和试错，API 耗时是 Opus 4.6 的 3.7 倍，input tokens 更是 2300 倍，说明它在信息检索和决策上效率较低</li> </ol> <p>用一句话总结：<strong>MiniMax-M2.7 能走完 80% 的路程，但最后 20% 正是区分”导出成功”和”推理可用”的关键。</strong></p> <h3 id="关于97-skills-遵循率">关于”97% Skills 遵循率”</h3> <p>MiniMax 官方宣称的 97% 遵循率来自 40 个 &gt; 2000 Token 的 Skill 测试。本次测试的 Skill（<code class="language-plaintext highlighter-rouge">skills/support-new-llm/SKILL.md</code>）确实超过 2000 Token，但它的难度不仅在于 Skill 的长度，更在于：</p> <ul> <li>需要理解<strong>一个陌生的模型架构</strong>（LFM2 的 ShortConv），而非套用已知模式</li> <li>需要跨 <strong>Python + C++ 两种语言、7+ 个文件</strong>协同修改</li> <li>需要修改的每一处都必须<strong>独立正确</strong>，任何一处遗漏都会导致失败</li> </ul> <p>这类需要深度理解框架全貌并做出正确设计决策的任务，可能是当前 Benchmark 难以充分覆盖的维度。在标准化的 Skill 遵循测试中拿到高分，与在真实工程场景中可靠地完成复杂任务之间，仍然存在显著的 gap。</p> <h2 id="写在最后">写在最后</h2> <p>我对国产编程模型的期待是真诚的。GLM-5 没过的时候我就在等下一个，看到 MiniMax-M2.7 发布当天就去测了。结果虽然没有通过，但说实话 MiniMax-M2.7 的表现已经比我预期的好——它确实走完了大部分路程，管线搭建和模型导出都是对的。</p> <p>这个任务本身就很难。它不是 Benchmark 里那种有标准答案的题目，而是一个需要理解陌生框架、做出设计决策、跨多个子系统协同修改的真实工程场景。目前只有 Claude Opus 4.6 能直接通过，这本身就说明了任务的区分度。</p> <p>我会继续关注国产模型的进展，也会用同样的任务测试后续发布的新模型。期待有一天，能看到一个国产模型一次跑通这个测试。</p>]]></content><author><name></name></author><summary type="html"><![CDATA[起因]]></summary></entry><entry><title type="html">2026.03.13 奔波的一天</title><link href="https://zhaode.wang/blog/2026/diary/" rel="alternate" type="text/html" title="2026.03.13 奔波的一天"/><published>2026-03-13T00:00:00+00:00</published><updated>2026-03-13T00:00:00+00:00</updated><id>https://zhaode.wang/blog/2026/diary</id><content type="html" xml:base="https://zhaode.wang/blog/2026/diary/"><![CDATA[<p>五点半就醒了，比闹钟早了不少，不过睡不着了。要去萧山赶飞机，机场远。</p> <p><img src="/assets/images/2026-03-13/morning.jpg" alt="清晨"/></p> <p>七点下楼吃自助早餐，吃完饭跟两个同事碰头，坐地铁去机场。机场人不多，安检很快，时间还不错。从杭州回北京，飞机上没睡。自带飞机餐比以前好一点，居然能吃。</p> <p>下午一点到公司，放下包继续昨天没做完的优化。</p> <p>三点去一一幼儿园开家长会。幼儿园离公司近，骑个共享单车过去。会议室里坐满了家长，桌上放着我做的叠纸。</p> <p><img src="/assets/images/2026-03-13/folding-paper.jpg" alt="叠纸"/></p> <p>开完会回公司继续干活。</p> <p>晚上回家跟一一玩了一会儿，收尾了一下工作就早点休息了。</p> <p><em>召德的 OpenClaw 编辑整理</em></p>]]></content><author><name></name></author><category term="diary"/><summary type="html"><![CDATA[五点半就醒了，比闹钟早了不少，不过睡不着了。要去萧山赶飞机，机场远。]]></summary></entry><entry><title type="html">2026.03.12 曲奇与订阅</title><link href="https://zhaode.wang/blog/2026/diary/" rel="alternate" type="text/html" title="2026.03.12 曲奇与订阅"/><published>2026-03-12T00:00:00+00:00</published><updated>2026-03-12T00:00:00+00:00</updated><id>https://zhaode.wang/blog/2026/diary</id><content type="html" xml:base="https://zhaode.wang/blog/2026/diary/"><![CDATA[<p>上午知兵送了盒饼干。包装上的图案可爱，看着就让人心情好。我还给一一买了盒巧克力，跟饼干一起带回去。她应该会喜欢。</p> <p>知兵简直是同事里的哆啦 A 梦。缺什么找他准没错。出差忘带显示器线，找他；组里要 Mac mini 测试机，找他；连网线，也是他给的。</p> <p><img src="/assets/images/2026-03-12/cookies.jpg" alt="饼干"/></p> <p>中午跟大学同学兼同事一起吃了饭。</p> <p>这种关系挺难得的——一起走过学生时代，现在又在同一个地方干活。聊了下近况，大家都还好。</p> <p>下午取消了 Google AI Pro 的订阅。</p> <p>当初开通时给的额度很充足，后来有人用反代薅羊毛，额度一砍再砍，性价比崩了，离开也是自然的事。自费的订阅计划接下来考虑试试 Codex 了，Claude 虽好但国内容易封号，御三家里选来选去，OpenAI 算是折中。</p> <p><img src="/assets/images/2026-03-12/subscription.png" alt="取消订阅"/></p> <p>在 Claude 的帮助下，今天上传了 24 个 LFM 模型，Github 的 issue 和 PR 也终于处理得差不多了，都只剩个位数。批量上传模型这种事，有 AI 帮忙确实省时间，不然一个个搞得烦死。MNN 社区那边应该能少不少积压。阶段性收尾，可以喘口气了。</p> <p>晚上安静下来。一天的事都落定了，明天回北京。</p> <p><em>召德的 OpenClaw 编辑整理</em></p>]]></content><author><name></name></author><category term="diary"/><summary type="html"><![CDATA[上午知兵送了盒饼干。包装上的图案可爱，看着就让人心情好。我还给一一买了盒巧克力，跟饼干一起带回去。她应该会喜欢。]]></summary></entry></feed>